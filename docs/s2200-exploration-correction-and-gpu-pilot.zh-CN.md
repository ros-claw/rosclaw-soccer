# S2200 起：探索配置更正与神经运控 GPU 小试

日期：2026-09-16。仍未通过连续比赛考试，没有合格宣传片；所有实验限于 SIM_ONLY。

## 必须保留的错误更正

S2200 原计划扩大原生踢球策略探索，将 `logstd` 设为 -3。但加载后的父检查点本来就是 -3，而不是从模型构造默认值误推的 -4。该更改实际没有效果；此前“标准差提高约 2.7 倍”的描述不成立。

独立比较 S2192 与 S2200 的 62 个同名 NPZ 文件，所有数组精确相同，包括检查点、推理样本、物理轨迹和训练 rollout。实际标准差倍率为 1。S2200 保留为重复对照，不算新的探索数据或能力提升。原始运行文件、日志和清单不改写。

- 原 S2200 清单：`sha256:f040c06f99acb61d0b5c326cdf2f57c30fd33c238ee8f6d79541567c309c125b`。
- 追加更正 `s2200-broader-native-exploration-v1/exploration-correction.json` 文件 SHA256：`7395fc853b36a4f207e7b686289e64047744aa614a6bb6dcc83e379509ef1676`。

新增 `training/exploration_change.py`：检查真实加载的逐维参数及实际采样器裁剪区间，拒绝无效变化、裁剪后的无变化或任一维下降；只返回声明，不修改模型、不宣称学会技能。18 项测试覆盖这些边界。相关 PPO、奖励与完整踢球学习合计 99 项测试通过，针对性 mypy 和改动文件 ruff 通过。

S2204 才是真正的 -3 → -2 初始化对照，标准差约从 0.04979 到 0.13534。采集器从检查点重建每步均值、固定种子的高斯噪声、动作和 logp，必须逐项精确一致。初始确定性物理轨迹已与 S2192 精确一致。训练尚在运行，结果另行追加；参数改动本身不算训练成果。

## GPU 小试：先验证控制器，而不是盲目扩大并行数

S2202 用固定 SONIC 默认关节姿态加受限 PD，CPU 和 GPU 都倒地；该试验不能作为平衡控制基线。其清单为 `sha256:31860e2accf5da37243bebf708161516934fcb7ef46651e9bec292a85f50e898`，失败证据保留。

S2206 改为已有冻结 RoboNaldo 神经网络步行策略，记录初始 LSTM 构造、每个副本独立历史；不在运动中重置状态。零物理速度换算为归一化命令约 `[-0.27272725, 0, 0]`，不能错误地传全零归一化值来声称静止。

在物理 GPU 2 上执行 16 个相同初态副本，每个 5 秒、250 个控制步、2500 个物理步，合计 40000 个世界物理步；另跑原 CPU MuJoCo 的独立神经运控对照。

| 指标 | S2206 实测 |
|---|---:|
| CPU / GPU 身体通过 | 均通过 |
| 最大根位置差 | 0.00002737 m（限 0.005 m） |
| 最大关节位置差 | 0.00115483 rad（限 0.01 rad） |
| GPU 仿真与推理墙钟 | 4.13 s，不含初始化 |
| CPU 单副本墙钟 | 0.766 s，不含初始化 |
| 新优化器步数 | 0 |

清单：`s2206-gpu-neural-locomotion-pilot-v1/qualification.json`，`sha256:9eeac944bdbdf5dbd5af45d33c24701110d089a2723095740c8c1d65266ab8fd`。

这不是 16 个独立任务，也不是四卡新训练，更不是八人比赛通过。模型为既有预训练模型；GPU 物理使用明确记录的单体训练场景，球门与共享足球场不同，不声称共享场景接触精度已认证。最终足球考试继续以原 CPU 物理为依据。不停止其他人的 GPU 进程，不删除既有证据。随后将测试运动命令，不能由站立通过推断奔跑和触球也通过。

本轮使用 ROSClaw / SimForge 技能约束了仿真边界、小规模 GPU 先行验证与物理证据记录；没有硬件调用或 Core 工作区变更。原始证据保存在外部证据目录，不提交大文件。

## S2208 / S2210：运动验证的通过范围与反例

再各跑 16 副本 × 5 秒，与独立 CPU 单副本闭环比较。每项一个固定初态、一个固定命令；没有优化器更新。

| 物理命令 | CPU / GPU 身体 | 最大根位置差 m | 最大关节差 rad | 原限值比较 |
|---|---|---:|---:|---|
| 前进 0.3 m/s | 均通过 | 0.0001723 | 0.0047300 | 通过 |
| 前进 0.6 m/s | 均通过 | 0.0001720 | 0.0025202 | 通过 |
| 横移 0.25 m/s | 均通过 | 0.0001703 | 0.0011018 | 通过 |
| 转向 0.5 rad/s | 均通过 | 0.0041522 | 0.0900242 | **关节差失败** |

表中是命令，不冒称实际跟踪速度。原脚本比较字段沿用了 `standing_comparison_passed` 名称，运动实验应读作同一误差与身体门槛比较，不应误解成静止试验。限值仍为 0.005 m / 0.01 rad，没有因转向失败而放宽。不能由这三项通过宣称整个足球 GPU 后端已通过。

S2210 把同一条 CPU 转向实测观察序列分别交给单副本 CPU、16 副本 CPU、16 副本 GPU 的相同冻结神经策略。最大目标关节输出差分别约 0.000000954 rad、0.0006208 rad。这只能隔离同输入下批量/设备推理差异，不能排除微小差异经闭环接触放大，也不能单独断定物理后端是唯一原因。

S2210 清单：`sha256:dcedc3149c3db54e6400cc97bc0566825141e03c72e18e3af874c31a4b697858`。转向物理清单：`sha256:eacc59fd193d48965ade495d18aff9441788675bb4ed40ac158c16cb213922b8`。失败保留，尚不扩大为高精度足球 GPU 训练认证。

## S2204 完成：有效探索变化仍未带来射门通过

两代各 8 条新轨迹，每条另有独立物理重放。父模型与两个确定性考试也各重放一次，共 38 次物理执行；两代有效学习帧分别 778、752，优化器步数 28、24，合计 **1530 帧、52 步**。两代训练身体均为 7/8 通过，16 条训练均未穿过有效门框；两个确定性考试身体与真实脚触球通过，但均没有目标平面观测。记录中的 `10.0` 仍只是缺失惩罚哨兵，不是测得误差十米。

独立审计重新校验全部 38 份物理文件、重建奖励、精确重放两次 PPO 更新，并验证 Core 更新证据。学习确实发生，**但技能考试没有提高，也没有晋升**。

- S2204 完成清单：`sha256:a7855942c1082635724bfe9066235b3e33949ec69758a5596836c0f6412e2882`。
- 独立审计：`sha256:c8d40b000547c6d35ed065967cd1165af6b3a6f31d3e764d75b1887ca264449e`。

## S2212：复用时序探索模型，但不把探索叫作成长

复用现有 `reference_relative_kick_learning`，583 维输入包含真实上一动作减去冻结参考均值的 29 维历史。冻结参考不随优化变化；采样的是有显式历史条件的高斯分布，不是采样后偷偷平滑动作。

先验证相关系数 0.8 的确定性轨迹精确等于原父模型，相关系数 0 的随机轨迹精确等于 S2204 对应轨迹。再做 4 条系数 0.8、条件 `logstd=-2` 的固定种子探索，每条双重放。相同的是条件创新噪声，**不是边际扰动幅度**。四条中两条身体失败；其余两条虽身体通过，一条出现己方非足部触球，全部未进球。

共 12 次物理执行，**优化器步数为 0**。独立审计重建每步历史、冻结参考推理、采样动作和 logp，并核对所有物理文件。相关模块 28 项测试通过。不采用该配置，不扩大为大训练。

- S2212 清单：`sha256:bf8bc5f6f4766c48d685141009733d6446ed522af3cb888a5d7dee39abf7401a`。
- 审计：`sha256:534859bfb67667a2d5f2f2cd94a25094d6a9e72929aa341835c60c6612d2a104`。

## S2214：转向目标序列重放仍暴露后端差异

把 CPU 转向时的神经目标关节序列交给 GPU，每个物理步继续重算受限 PD。独立 CPU 重放该目标序列与原 CPU 轨迹逐项完全一致，先确认序列本身没有重建错误。GPU 则最大根位置差约 0.19991 m、关节差约 0.29219 rad，超过原限值。

这是没有高层反馈纠偏的目标序列重放，不是自主导航，也不能拿来录宣传片。它说明同一目标序列下也存在积累偏差，不能只归因于神经推理的微小设备差异；但尚未定位到唯一物理原因。对精密转向与足球接触继续保留 CPU 考试，不放宽门槛。

S2214 清单：`sha256:5ebbf34f306ffcb672798fae7cee88f744c07fa1b13129448728b4d8a6025ab9`。后续 S2216 将检查当前共享链路与历史单体参考之间的进入相位/融合时间差异，尚未完成，不计作成果。
