# S938：为什么角色一直想射门，却没有第二脚

## 已核实的结论

当前 4v4 仍未达到宣传标准。S938/S939 是不改变控制输出的诊断，
不是新训练成绩。八个身体、关节、球的轨迹与 S936 完全一致。

此前“只接了一次射门”的判断不完整：实验还包含 S718 的第二次射门
适配器，使用 reference 20、S286 actor 和 S716 成败样本入场记忆。
但安装了专家，不等于它获得过一次合格入场。

另一个初步怀疑也被排除：第一次踢球执行器瞄准偏侧 +0.75 m，
但之后战术角色仍要求 `(-1.25, 0, 0)`。因此第二次射门没有被
“偏侧目标不受支持”挡住。必须检查实际调用参数，不能从第一次
执行目标推断后续角色意图。

## 真实调用中的条件统计

S938 记录 1000 次第二技能适配器调用的完整身体状态、球状态、
意图、目标、候选控球身份、前次技能终态。S939 独立重建几何条件，
逐帧核对 `motor-state.npz`，再检查八身体和足球的物理轨迹一致性。

| 条件 | 前次恢复完成且距首次入场至少 4 秒后的 360 帧 |
|---|---:|
| 请求 SHOOT | 320 |
| 具有当前候选控球身份 | 360 |
| 目标与第二技能中路合同相容 | 320 |
| 前后球位符合 0.30–0.40 m | 36 |
| 横向球位误差不超过 0.12 m | 0 |
| 身体朝向符合原有技能域 | 0 |
| 身体平面速度不超过 0.15 m/s | 233 |
| 球速不超过 0.10 m/s | 0 |
| 高度、直立程度和球高分别满足要求 | 各 360 |
| 所有几何条件同时成立 | 0 |
| 真正执行第二次射门 | 0 |

这些是控制帧数，不是独立比赛成功率。后面的成败记忆查询尚未被
走到，因此不能把当前失败归因于记忆拒绝，更不能放宽记忆阈值。

## 下一项有边界的干预

通用 `world.planar_entry_target.planar_entry_root_target` 将某个技能的
身体坐标系接触偏移和期望朝向，转换成导航根节点目标。它不认识
G1、不授予控球权、不写物理状态，也不判断碰撞可达性。

原有“靠近球”的导航目标不保证满足某个射门技能的入场域。S940
因此只研究准备动作：保留原角色意图、当前候选控球身份、所有
对手、球场边界、邻居避碰、运动限幅和射门入场门，导航到原技能
要求的位置和朝向。第二次射门执行在这一对照中明确关闭，先检验
准备能力，避免将未资格验证的衔接直接投入比赛。

这是解析导航干预，不是新 RL、肌肉记忆训练或球技晋升。接下来
必须用真正到达的身体/球状态重新考核专家，再检验独占控制交接
与实际第二脚；不能把影子调用或几何条件满足写成射门成功。

新增组件的旋转/平移等变性、接触点重建和非法输入检查通过；
连同既有地面截球几何回归，52 项测试通过。模块 mypy 和全局 ruff
通过。全仓回归为 2484 passed、29 skipped、11 failed；逐项核对后，
失败身份集合与 S936 的 11 项完全相同，没有将这些存量失败删去。

## S940/S941：准备动作的成对结果

首 640 个控制帧的身体与球轨迹保持不变。之后实际应用 320 次
受限导航准备指令；八身体保持原有安全门通过，机器人间接触为 0。
第二次射门明确没有执行。

| 恢复后 360 帧中的条件 | 原方案 | 准备方案 |
|---|---:|---:|
| 前后球位合格 | 36 | 105 |
| 横向球位合格 | 0 | 253 |
| 朝向合格 | 0 | 192 |
| 身体低速合格 | 233 | 191 |
| 球速合格 | 0 | 0 |
| 意图/身份成立且除球速外全部几何条件成立 | 0 | 68 |
| 全部入场条件成立 | 0 | 0 |

这支持“导航应服务于具体技能入场几何”的局部判断，但没有证明
完整射门提升；身体低速合格帧反而减少，不能声称所有指标更好。
S942/S943 将两个方案均延长至 24 秒，且均关闭第二次射门，分别
要求前 20 秒物理轨迹与对应原实验完全一致。目的是区分准备改善
与单纯多等待球减速，不是修改球速或降低原来的 0.10 m/s 门槛。

## S944：延长时间没有打通第二次射门

两条 24 秒回放均完成，前 20 秒各自与原方案逐字节一致；八身体
均安全，机器人间接触均为 0。但两组均没有一个完整合格的第二
射门入场，也没有执行第二次射门。

准备组在约 20.1 秒改为战术回传，几何准备随原来的 SHOOT 意图
结束而退出。这里不能强制保留射门意图来制造成功。更早在
12.8 秒就已经出现过恢复后的回传请求，所以后续考试选择的是
这条连续轨迹里**最早**满足原有阶段条件的回传请求，而不是挑一个
预先知道效果好的帧。

## S947–S949：旧传球专家不能直接承担新回传

初态来自 S943 frame 640 / 12.8 秒，目标为当时战术角色声明的
`(2.9557347855, 2.6328874532, 0)`，不是事后调整目标。
恢复实际 LSTM h/c、上一动作及对应命令后，32 路相同输入的
基础模型观测误差为 0，动作最大误差为 `5.3644e-7`，目标最大
误差为 `1.3411e-7`。这只是神经推理组件核对，不是物理迁移认证。

随后使用 1 个精确初态和 31 个新球位/球速小扰动，保留旧 1.2 m/s
传球目标、首次足部加速、无非足部接触、无再触球和 0.1 m 目标
平面门。隔离 CPU 后端仍采用旧 float32 状态/力矩计算接口，不
代表实际八人世界逐步物理一致，也没有对手或动态接球队友。

| 冻结模型/解析消融 | 身体安全 | 自身非足部碰球 | 合格传球 | 精准传球 |
|---|---:|---:|---:|---:|
| 原 S458 完整模型 | 32/32 | 32/32 | 0/32 | 0/32 |
| 只保留导航残差 | 32/32 | 32/32 | 0/32 | 0/32 |
| 只保留关节残差 | 32/32 | 28/32 | 0/32 | 0/32 |
| 两种残差都清零，保留基础步态和名义导航 | 32/32 | 32/32 | 0/32 | 0/32 |

四组初始身体、球状态和目标逐项相同，消融只改指定输出通道。
这不是新的训练或四组独立比赛。结果否定了“仅重新调用现有传球
模型就足够”的假设，并提示检查准备路径，而不是降低碰撞门。

原名义路径从根节点直接走向接触侧，初态这条线段距球心最近仅
约 0.0675 m。这个几何诊断不是机器人碰撞证明，但解释了为什么
应当单独研究绕到球另一侧，而不是继续叠加出脚残差。

## S950：绕球准备探针

新增通用 `world.annular_waypoint` 几何组件：给定接触中心和期望
接触侧，提出径向退开、圆弧绕行、最后靠近的目标点。它不操作
身体/足球，不授予控球权，不承诺全身避碰；实际执行仍由冻结的
基础步态负责。可选的调用方阶段滞回默认关闭，不是运动许可。

第一组 32 状态、8 秒准备探针关闭所有关节/导航残差，保留完整
基础模型历史，没有新 PPO 更新。32/32 身体安全，0/32 自身
非足部碰球，但也没有合格传球。原样本的阶段仍在绕行/靠近之间
反复切换，不能当作成熟准备技能或完整比赛进步。

S951–S954 比较仅阶段滞回、仅球速前馈、两者结合和原样复跑。
这仍是准备阶段的解析干预，不是学习成绩。

S955 审计确认原样复跑的物理数组与 S950 完全相同，四组初始
身体、球状态和目标也逐项相同。准备诊断采用明确的新指标：
处于 approach 阶段，根节点目标误差不超过 0.07 m、朝向误差不
超过 0.15 rad、平面速度不超过 0.2 m/s、根高度大于 0.72 m、
直立度大于 0.98、此前没有非足部碰球。它不是既有传球入场许可。

| 解析准备方案 | 曾达到准备诊断的案例 | 非足部碰球 | 平均阶段切换次数 |
|---|---:|---:|---:|
| 原样复跑 | 13/32 | 0/32 | 5.97 |
| 仅滞回 | 14/32 | 0/32 | 2.00 |
| 仅球速前馈 | 29/32 | 0/32 | 4.63 |
| 两者结合 | 31/32 | 1/32 | 3.13 |

四组均无合格传球。两者结合出现新非足部碰球，不能因为准备
数量最高就选择它。后续 S956 采用仅球速前馈，检验准备到冻结
传球模型的连续衔接；不重置身体、球、LSTM 或首球加速/再触球
记录。准备超过 6 秒仍未达到条件的案例不执行传球，保留为失败。

最新全仓回归：2506 passed、29 skipped、11 failed，失败身份集合
与 S940 完全相同。新增环形目标组件、滞回选项和相关几何测试的
定向回归共 74 passed；两模块 mypy 和全局 ruff 通过。

## S956–S963：连续链路与真实 PPO 续训

S956 将球速前馈准备和原有 S458 传球网络连续运行 11 秒。32 个
案例中 23 个在 6 秒内达到准备诊断并执行网络，9 个未入场仍计为
失败。身体、球、LSTM、非足部接触和首球再触球记录均不在切换时
清零。结果 31/32 身体通过原有边界、6/32 非足部碰球、0 合格传球。

S957 在真正入场时按当前球位置重新绑定到同一个队友目标的方向，
没有移动目标或重置物理状态。结果 32/32 身体通过、8/32 非足部
碰球、仍然 0 合格传球。保存了 23 个真实入场的身体和循环网络状态。
S958 从准备开始逐步重算，23 个入场的 hidden、cell、上一动作和
上一基础目标均零误差；这是历史链路复算，不是完整物理后端等价证明。

S959 用这些真实状态开展 4 次 actor-critic PPO 更新，S961 完整恢复
模型、Adam 和随机数状态再做 16 次更新。训练保留小幅球位/球速扰动，
没有降低传球速度、精度、首球接触和身体门槛。基础步态网络冻结；
学习的是 29 个关节目标残差和 3 个导航残差，不是直接关节力矩策略。

| 固定开发集检查点 | 身体边界通过 | 非足部碰球 | 合格定向传球 | 0.1 m 精确首传 |
|---|---:|---:|---:|---:|
| 原网络 | 23/23 | 7/23 | 0/23 | 0/23 |
| 更新 4 次 | 23/23 | 3/23 | 0/23 | 0/23 |
| 更新 20 次 | 23/23 | 0/23 | 0/23 | 0/23 |

最后模型虽然没有非足部碰球，首球有效接触中的最大向前球速只有
0.6964 m/s，未完成任务。不能称为成功成长或选择它拍宣传片。这
23 个开发状态来自同一原始场景的局部扰动，不是 23 场独立比赛；
另外 9 个未准备成功的案例没有被计入训练分母，也没有被删除。

S960、S962 分别逐次独立重放全部 20 次优化，模型、Adam、随机数
状态和导出权重完全相同。S961 开始的确定性物理数组也与 S959
第 4 次检查点相同。证明更新和恢复真实可复算，不证明学会踢球。
S961 报告 SHA256：`707dd0bb775f0285a2034bf1438331d3bf318a4f4f75867b2222d2892e48deb4`。

S963 转向检查入场几何：同一个旧模型在 S943 第 220 帧的真实首传，
球相对身体位于 `[0.4031, 0.1359]` m，目标相对身体朝向为
`-0.9498` rad；之前通用准备的精确案例却把球放在右前方，身体近似
正对目标。这是可检验的分布差异，不是已确认的唯一失败原因。
原首传证据中有效峰值 1.2322 m/s、无自身非足部接触、无迟到再触球、
身体边界通过；这些仍不构成完整传球精度证书。

接下来只把这个单样本用作准备位置对照，通过实际导航到达，不复制
关节姿态、瞬移身体或移动球。单样本几何原型不是学习模型，更不是
通用入场分布；若对照有效，再扩大真实轨迹覆盖和训练。

新增证据均位于 `/code/rosclaw/phase8_evidence/`：

- `s956-prepared-return-pass-chain-v1/`
- `s957-rebased-return-pass-chain-v1/`（含 `entry-snapshots.npz`）
- `s958-prepared-return-warm-state-audit-v1/`
- `s959-prepared-return-pass-ppo-v1/`
- `s960-prepared-return-ppo-replay-v1/`
- `s961-prepared-return-pass-continuation-v1/`
- `s962-return-pass-continuation-replay-v1/`
- `s963-pass-entry-prototype-v1/`

## S964–S966：准备位置与坐标方向的配对对照

S964 实际导航到 S963 的相对站位，再运行同一个冻结旧模型。
S965 保持相同准备过程，在真实入场时将世界向量特征变换到原首传
目标方向，并将有界导航指令变换回物理世界。循环步态历史保持真实。

新通用组件 `training/planar_feature_frame.py` 不硬编码 G1 或足球
索引；调用方必须显式指定互不重叠的二维世界向量列。组件保留梯度，
调用时才导入 Torch，不修改输入。实验只旋转根节点线速度、球相对
位置、球线速度和目标方向的 XY；不旋转局部角速度、重力、关节或
原始动作潜变量。导航残差滤波历史固定在策略坐标中，不来回重标记。
这项组件测试不是策略旋转等变或通用迁移能力证明。

两组都是 32 个原始案例，仅 9 个在时限内真正入场；均 32/32 身体
通过、0/32 自身非足部接触、1/32 合格定向出球、0/32 精确首传。
因此不晋升，不宣称坐标变换带来球技提升。S966 证明所有初始数组、
入场时间、每个案例入场前物理轨迹完全一致，未入场案例全程完全
一致；两组九个入场的 LSTM 和上一动作也独立重算零误差。

准备过程中案例 19 有足部碰球，未通过入场；没有把准备误碰转换成
重新计分的“首传”。后续 S967 使用九个已审计入场状态做四次新的
有界 PPO 更新，另 23 个准备失败案例仍然保留，不将训练子集成绩
冒充 32 案例完整链路成绩。

- `s964-prototype-return-pass-v1/`
- `s965-canonical-prototype-return-pass-v1/`
- `s966-prototype-pair-audit-v1/`（含两组独立循环状态审计）
- `s967-prototype-return-pass-ppo-v1/`（训练结果以实际报告为准）

S967 四次更新结束：固定九状态开发集的合格定向出球由 2/9 到
3/9，但精确首传由 1/9 到 0/9，不能只汇报前一个指标。各代均
9/9 身体通过、0/9 非足部接触。S968 的四次优化逐项精确复算通过。

孤立技能基线的 1/9 精确首传也不能替代 S964 完整链路的 0/32：
案例 31 的目标平面误差约 0.05469 m，但在 250 帧技能窗口后继续
运行，球最终越过边线；完整链路判失败。首个导航指令相同、首帧
qpos 差异不超过 5.97e-8，也不代表后续物理逐位相同。不同时间
窗口、不同批尺寸的统计不能直接拼接成提升。

本次全仓回归为 2523 passed、29 skipped、11 failed（49.39 秒）。
失败身份集合已与 S954 XML 独立比较，完全相同；未修改旧证据或
跳过这些失败。新组件与相邻传球/几何定向测试 84 passed，全局
ruff、新模块 mypy 和新增文件格式检查通过。首轮定向测试曾因
未带固定 Core 路径无法导入 `rosclaw.continual`；修正测试环境后
重跑。当前并非最新 Core 的整合验收。

继续实验 S969 从 S967 第四代完整 Adam/RNG 状态恢复，计划再做
16 次有限更新，全部检查点保留。后续必须重新运行准备到传球的
完整链路与新扰动考试，才能讨论能力晋升。

## S969–S977：拒绝退步候选，比较因果学习窗口

S969 的第 20 代开发集是 0/9 合格定向出球、0/9 精确首传。
预先指定最后一代进行 S971 完整 32 案例复验，结果同样为 0/32
和 0/32；不替换任何运行模型。S970 的 16 次优化精确复算通过，
再次说明“更新可复算”和“能力提高”是两件事。

S972 诊断发现出球前样本只占约两成。不能据此断言后续样本无用：
球离脚后关节仍影响平衡。S973 因而只做四次配对分支：从同一个
第四代模型、Adam、随机数状态恢复，额外追加因果二值位选择优化
样本，保留全段 GAE、原奖励、接触门和稳定性验收。

通用组件 `training/binary_learning_window.py` 将学习元数据与策略
输入分离。新增位不会传给 actor/critic，不新增参数或消耗随机数；
训练保存带 `learner.` 前缀的状态，执行导出仍是原 139 维模型。
全选窗口的优化结果与原算法逐位一致；被窗口排除的样本若伪造
likelihood 仍会被拒绝。组件本身不决定窗口因果性，也不授予执行权限。

S974 验证首个配对 rollout 的原 139 维观测、动作、奖励、likelihood
和活动标记完全相同，四次模型/Adam/RNG 更新精确重放。额外位按
整数 2 ms 物理步还原，与实际已观测 departure 状态一致。S972 的
浮点时刻诊断在首批边界处多计 1 个出球前样本，不能用它替代此
整数物理步审计；没有修改旧诊断文件掩盖差异。

固定第八代进行 S975/S976 配对完整链路，不选择事后最优代：

| 同期第八代 | 实际入场 | 身体通过 | 非足部碰球 | 合格定向出球 | 精确首传 |
|---|---:|---:|---:|---:|---:|
| 出球前学习窗口 | 9/32 | 32/32 | 0/32 | 2/32 | 0/32 |
| 原完整学习窗口 | 9/32 | 32/32 | 0/32 | 2/32 | 1/32 |

因此不宣称学习窗口方法改善了传球，也不把 1/32 当作合格整体比赛。
S977 继续审计两边初态、入场前物理、未入场全段和循环状态。
下一步检验移动入场速度分布，仍通过真实导航到达，不写身体姿态。

最新全仓：2537 passed、29 skipped、11 failed，失败身份与 S965
完全相同。新增学习窗口和相邻传球测试 26 passed；全局 ruff 与
新组件、PPO 模块的 mypy 通过。所有成果仍为 SIM_ONLY。

相关证据根目录仍为 `/code/rosclaw/phase8_evidence/`：

- `s970-prototype-continuation-replay-v1/`
- `s971-learned-prototype-full-chain-v1/`
- `s972-pass-credit-window-audit-v1/`
- `s973-predeparture-window-pass-v1/`
- `s974-predeparture-window-replay-v1/`
- `s975-window-full-chain-v1/`
- `s976-control-full-chain-v1/`
- `s977-window-full-chain-audit-v1/`
- `s973-repository-tests.xml`

## S978–S984：移动入场与超时收尾

S978 把原首传记录中的身体相对球速度用于 approach 前馈，并将
新的准备诊断从“根速度小于 0.2 m/s”改为“与观测入场相对速度的
误差不超过 0.15 m/s”。这是改变实验准备条件，不是收紧已有许可，
也不是新学出的速度模型。原接触、精度、身体和首球门保持不变。
10/32 入场，仍 0/32 精确首传，且有新的非足部接触，不晋升。

发现旧实验中超过 6 秒未入场的案例仍继续准备导航。S980 改成
超时输出零导航，反而让滚动的球接近停住的腿；S982 使用实际球位、
球速的间距保持收尾，目标根球距离 0.65 m、最大 0.4 m/s，目标点
裁剪到场内。不重新入场或重新获得首球计分，不写入身体/球状态。

| 22 个未入场案例的后续处理 | 足部误碰球 | 非足部碰球 |
|---|---:|---:|
| 继续准备导航 | 22/22 | 1/22 |
| 直接零导航 | 1/22 | 6/22 |
| 测量驱动的间距收尾 | 0/22 | 0/22 |

S984 确认前三组的前 301 个控制步完全一致，已经入场的 10 个案例
全段物理完全一致，收尾平面速度不超过 0.400001 m/s。三组均
32/32 身体通过、0/32 精确首传；剩余一个非足部接触发生在已入场
案例，并未被修好。这个结果只支持孤立实验的失败收尾改进，不支持
多人场景避障或足球能力晋升。S981 因局部变量遮蔽辅助函数在控制
循环前失败；原记录保留，S982 是新运行而非覆盖修补。

S979 已生成并查看全部九个已入场案例的触球审查图，共三张；另外
23 个未入场案例仍在统计中。图只用于检查姿态，不是宣传成果或
新的接触证据：`s979-return-pass-contact-review-v1/review-1.png` 至
`review-3.png`。当前姿态仍不能证明运动自然性达标。

继续开展单 GPU 扩样试运行：只映射物理 GPU 3，其 CUDA 查询剩余
显存 44,435,898,368 bytes。系统 `nvidia-smi` 仍因 595.91 用户库
与内核驱动不匹配而失败，未修改系统驱动。S983 缺 Warp、S985 缺
ONNX Runtime，均保留失败；S986 的旧 MJWarp 实际阻尼校验失败，
拒绝训练。S988 隔离加载既有 S722 修正版 MJWarp 3.13 / Warp
1.17，未修改系统驱动或用户环境，实际阻尼校验通过后才运行。

## S988–S996：CUDA 扩样、拒绝候选与恢复边界

S988 将九个已审计入场状态各复制 16 份，144 个环境，做一次
PPO 更新（264 个 optimizer step）；这不是 144 个独立场景。
开发集受控传球从 48/144 降到 16/144，精确传球两者均 0/144。
S990/S991 使用新种子、相同初始状态的 CPU 对照：32 个球位置与
速度扰动，仍只有九种身体上下文。受控传球 10/32 → 7/32，
0.1 m 目标平面精度 2/32 → 2/32；均 32/32 身体通过、无非足部
接触、各一次晚期重触球。S992 独立重建首球计分与 2 ms 平面
过线，候选未晋升。不是完整准备流程、多人实战或实际球门精度。

S989 发现更新前检查点漏存 CUDA RNG，审计失败。S993 从原父级
检查点逐批重放全部 250 次随机动作采样，每批动作逐项完全一致，
随后重放更新，模型、Adam、CPU/CUDA RNG 和导出权重均完全一致。
旧检查点没有被补写。新增通用 `training/torch_rng_snapshot.py`
显式捕获调用者指定的 CUDA 设备及 CPU RNG，恢复前验证结构和
后端状态；不枚举或触碰其它 GPU，不授予设备执行许可。

S994 原计划继续八次更新，但恢复基线与 S988 的完整物理数组
不一致，严格检查当场中止，**没有开展后续更新**。最大 qpos
分量差 0.4012、qvel 分量差 7.5958，不能用目标平面误差小来掩盖
身体轨迹差异。没有修改原运行的门槛或宣称严格物理恢复成功。

S995 使用冻结父策略，在 S990 的同一批 32 个 CPU 初始状态上
单独运行修正 CUDA 后端。S996 审计：安全、受控、精确、过线及
在场标签逐例一致，目标平面误差最大差 0.00008113 m；但 qpos
分量最大差 0.4125、qvel 最大差 9.7357，完整轨迹仍不等价。
这仅支持该冻结策略与这批上下文的筛选诊断，不认证整个后端，
不证明故障原因，更不能代替 CPU 新案例及完整链路验证。

证据目录：`s988-corrected-cuda-prototype-pass-pilot-v1`、
`s992-gpu-candidate-cpu-pair-audit-v1`、
`s993-cuda-rng-reconstruction-replay-v1`、
`s994-corrected-cuda-pass-continuation-v1`（中止现场）、
`s996-gpu-pass-screening-boundary-v1`，均在
`/code/rosclaw/phase8_evidence/`。当前没有新的模型投入比赛，
也没有足以作为完整 4v4 宣传成果的片段。

## S997–S999：首球结束后的运控收尾

新实验在观测到首球有效接触窗口关闭后，将 actor 原始残差目标
变为零，再经过原有平滑/幅度限制交回冻结运控。没有直接更改关节
姿态或球状态，不重置 LSTM，不重新计分。这是分析式 successor，
不是新学出的模型；与当前运行时“接球队友触球后释放”的条件不同，
不能称作修复了训练/运行不一致。

S997 对 S990 的 32 个新扰动案例，S998 对完整 11 秒准备流程做
成对实验。S999 重建接触关闭时刻，确认干预前物理和命令逐项完全
一致；完整流程中未入场者全段也相同。两组原有传球、安全、过线和
在场标签均未变化。以下仅为恢复诊断，不是新的通过门：

| 末两秒指标 | 32 个入场扰动：原→收尾 | 完整准备中 9 个入场者：原→收尾 |
|---|---|---|
| 身体角速度 P95（rad/s） | 1.1958 → 0.00816 | 1.1854 → 0.00279 |
| 关节加速度 RMS（rad/s²） | 25.5906 → 4.4202 | 24.0361 → 0.00052 |
| 根平面速度 P95（m/s） | 0.4306 → 0.00659 | 0.4215 → 0.00073 |

稳定下来不等于主动接应。扰动组仍 2/32 精确传球，完整准备仍
0/32；完整组 23/32 没有入场的失败仍计入。没有接球队友/对手，
不能从静止稳定推断连续比赛变好。证据：
`/code/rosclaw/phase8_evidence/s999-causal-pass-release-audit-v1/result.json`。

通用随机状态组件及前述记录已推送 `c443792`。全仓回归
`s994-repository-tests.xml`：2555 passed、29 skipped、11 failed；
11 个失败身份与 S973 完全一致，无新增失败，不能写成全仓全绿。

## S1000–S1009：困难入场扩样仍未通过

S1000 将两个真实到达的入场族合并：S964 的九个 prototype 状态，
加上 S957 的 23 个 geometric 状态，分别保留已审计的 LSTM 历史。
每个状态四个副本，共 128 环境。两族来自相关准备实验，原 64 次
准备包含 32 次未入场失败，不是 64 场独立比赛。

新 Adam 1e-5、固定四轮 PPO，加入首球关闭后的平滑收尾；与旧
实验同时改变了课程、学习率和收尾，属于探索，不作单变量因果归因。
实际执行 240+240+240+232 次更新。S1001 逐项复现模型、Adam、
CPU/CUDA RNG 及导出，误差为零。开发集最终 4/128 精确传球不算
泛化；S1003 新 CPU 考试中父级与候选均 0/32 精确传球，拒绝晋升。
S1002 先尝试 64 环境，被 CPU 适配器的 32 环境上限阻止；未绕过
限制，S1003 使用新运行、每个真实入场状态一次新扰动。

S1005 在相同父级、种子、学习率和收尾基础上，用观测到的首球
接触关闭位选择训练样本。该位不进入策略，完整 GAE 和所有 live
样本的 likelihood 校验保留。它不同于 S973：包含完整首次接触，
只在残差目标确实被撤掉后停止优化相关样本。四轮执行
176+176+184+176 次更新；S1006 完整复现，并从 2 ms 接触/速度
重新得到每个学习位。CUDA 完整物理不具备位级重现资格，不能把
两次运行的数值差异全归因于选择位。S1007 同批 CPU 考试仍为零
精确传球，也没有晋升。

| 新 CPU 32 个相关入场状态 | 身体通过 | 非足部接触 | 受控传球 | 0.1 m 精确 | 晚期重触 |
|---|---:|---:|---:|---:|---:|
| S1003 父级 | 31 | 5 | 2 | 0 | 0 |
| S1003 普通 PPO | 32 | 2 | 1 | 0 | 0 |
| S1007 关闭窗口 PPO | 32 | 3 | 2 | 0 | 1 |

S1004/S1009 独立校验成对初始数组和目标平面/首球计分。

S1008 将分析式收尾放入实际八机器人共享球场，不改变原 actor、
250 帧窗口、队友接触验证或故障锁存。S1016 确认前 245 帧身体、
球和力矩完全一致，首传足部峰值仍为 1.23219 m/s；后续队伍轨迹
改变，球出边线，蓝中场在第 615 帧出现场地避让不可行，12.32 秒
中止而非原来的 20 秒。两者 legacy `safe/passed` 都为 true，
**不能据此宣布整场通过**。此修改未投入运行方案。

## S1010–S1016：将探索示范学习进反馈网络

S1000 第二轮随机训练中只有两条记录符合精确首传标准。S1010
将这两条的原始动作序列放到 CPU 物理重放：均满足原门，误差
0.05422 m、0.00718 m。筛选了成功训练记录，不能说成功率 100%。
动作只用于示范资格验证，不作为按案例/帧查表的部署策略。

S1011 在相同 CPU 重放中记录真实策略观测；与 S1010 物理数组
哈希相同。新增通用蒸馏组件对显式 139/32 合同的支持，默认 133/29
及其保护约束不变，禁止靠输入形状隐式迁移模型。S1012 使用两段
有效接触前缀（37+38 个样本），加上仅从训练集抽取的 512 个父级
行为保留样本；两组各占一半权重，固定 128 步、学习率 1e-4。
仅 actor 更新，critic/noise 冻结，经既有训练权重租约审计；不是
最新 Core 运行时集成或运动许可。S1015 独立手工重放 Adam，导出
逐项完全一致。

加权 MSE 0.008824 → 0.007985，但这不代表动作完成。S1014 从
两个训练初态使用网络当前观测反馈、不再查原动作序列，1/2 精确。
S1013 另一个新种子考试：父级与蒸馏模型均 1/32 精确，受控传球
3→4，非足部接触 4→6，晚期重触 0→1，拒绝晋升。仍未形成可靠
的可复用精准传球专家，未输出宣传片。

蒸馏新增六项测试，专项 19 passed；全仓
`/code/rosclaw/phase8_evidence/s1012-repository-tests.xml`：
2561 passed、29 skipped、11 failed。失败身份与 S994 完全相同；
ruff 和该模块 mypy 通过。完整回归仍不是全绿。

各阶段证据在 `/code/rosclaw/phase8_evidence/s1000-*` 至
`s1016-*`，共享世界在 `s912-shared-evidence/s1008-causal-pass-release-shared/`。
所有失败运行与旧模型保留。下一步是先改善示范的反馈复用与困难
入场覆盖，再考完整准备/队伍链路，不能用拟合误差或孤立稳定性
替代连续传接射门表现。

## S1017–S1034：记忆扩充与学生分布补样

S1017 保持数据、父级、学习率和保留权重不变，将拟合延长到
1024 步。MSE 降至 0.005662，S1020 完整复现，但 S1019 两个训练
初态的闭环精确率从短拟合的 1/2 降到 0/2。S1018 在相同 S1013
诊断来球上也是 0/32，拒绝晋升。拟合误差不是动作效果。

S1023 复用已有 `local_action_memory` 与 `entry_support`：按当前
139 维观测和已有技能相位检索动作，不输入原案例 ID 或全局帧号。
这是非参数反馈记忆，不是神经网络巩固。两个训练初态恢复 2/2，
物理数组与原 CPU 示范逐项相同；32 个混合入场诊断仍仅 1/32 精确。
覆盖门支持八个，另外 24 个回退父级的全段物理完全一致。S1021
完成四方案成对审计；S1023 通用 boundary 继承了一句过时的
“neural mean”，其 method/memory_boundary 及 S1021 已明确纠正
实际方法为非参数检索，原文件未覆盖。

S1022 固定父网络，采集四批各 128 环境的时间相关动作探索
（AR 系数 0.9、零初始噪声），没有 PPO 更新。相关噪声不能拿
边际 Normal 概率伪装成 on-policy likelihood，归档故意移除
logp/value。512 次尝试中两条满足精确门；S1026 用保存的显式
CPU/CUDA RNG，逐批重建全部 250 次原始动作，完全一致。
S1024 将两条候选在 CPU 重放，均通过，误差 0.05687、0.07455 m。
这仍是筛选后的训练示范，不是 100% 成功率或部署策略。

S1025 将四条 CPU 示范并入记忆库，旧两条反馈复现均保留；另一个
新种子 102501、32 个相关状态的对照中，受控传球 4→7、精确
1→2，均 32 个身体通过、4 个非足部接触、零晚期重触。
S1033 独立重建平面与首球计分。改善很小，困难入场仍没有解决，
不晋升，也不是宣传达标。

参考 [Ross 等人的 DAgger 论文](https://proceedings.mlr.press/v15/ross11a.html)，
动作会改变之后遇到的状态，单纯在示范分布拟合不等于闭环表现。
S1027 让 S1012 学生独自控制新的 CPU 训练来球，记忆只给标注建议；
入口及逐相位覆盖筛选仍只是经验相似度，不是专家正确性证明。
这批实际轨迹 0/32 精确，没有改标为成功。S1029 取其中 285 个
接触窗口未关闭且仍 live、无非足部/晚期重触/出界的覆盖样本，
加上 75 个真实示范与 512 个当前父级行为保留样本，权重分别
0.25/0.25/0.5，固定 512 次 actor-only 更新。S1028 在执行前因
生成代码重复关键字失败，保留旧脚本，S1029 为新运行修正。

S1031 手工重放拟合完全一致；S1032 逐项重建学生实际动作、
记忆建议、覆盖位和全部训练选择。它是 DAgger 思路启发的建议
聚合，不宣称满足论文的专家/理论假设。S1030 新种子 103001
考试：父级 32 身体通过、3 受控、0 精确、1 非足部；候选
31 身体通过、1 受控、0 精确、2 非足部，拒绝晋升。S1034
成对计分审计通过不改变这一失败结论。

当前继续寻找可跨状态复用的动作素材，不把退步模型投入比赛。
所有这些实验仍是局部入场/传球，不是 4v4 整体能力提升的证明。

## S1035–S1039：连贯动作搜索与 CPU 复验

S1035 对 32 个已保存的入场状态各运行 8 个探针，共 8 轮、
2048 次 GPU 仿真。冻结父策略，在四段成功示范提取的方向和
导航方向构成的 7 维空间搜索整段保持的动作偏置；不是逐帧
白噪声 PPO，也没有更新 actor/critic。每轮保留所有失败、
采样随机状态、参数分布和受约束的学习更新记录。S1036 精确
重建全部提案和分布更新；这不等于重新验证微步物理。

各轮精确传球数为 2、4、1、5、8、4、7、7（每轮 256 次）。
S1037 预先选择最后一轮全部 7 个精确候选回 CPU 重放，7 个均
身体通过、无自身非足部碰球、无迟触，传球目标平面误差
0.00486–0.08066 m。这里只证明选出的示范可复现；绝不是
7/7 的随机成功率，也不是比赛进球率。

S1038 将这 7 段示范加入原有 4 段记忆，用新种子 103801
做 32 个成对入场实验。4 段记忆对照到 11 段候选：受控传球
3→5、精确传球 0→1、身体通过均为 31、自身非足部均为 4、
迟触均为 1。S1039 独立重建目标平面计分并核对初始输入。
候选仍不满足晋升或宣传条件。动作素材增加不等于自主技能
已经可靠；没有把离线成功录像当成整体球队已经成长。

相关完整结果位于 `phase8_evidence/s1035-coherent-pass-search-v1`、
`s1036-coherent-pass-search-audit-v1`、`s1037-coherent-pass-cpu-replay-v1`、
`s1038-coherent-pass-memory-{control,candidate}-v1` 和
`s1039-coherent-memory-pair-audit-v1`（均在 `/code/rosclaw` 下）。
S1038 候选首次启动因命令中的依赖目录拼写错误在导入前失败；
纠正环境路径后使用未改动的脚本运行，未产生旧物理证据覆盖。

### S1040–S1044：固定偏置与再次神经固化

S1040 用实际入场观测选择一个固定偏置，叠加在持续反馈的
冻结神经策略上，避免逐帧切换示范；重复 S1038 输入进行诊断，
32 次中 4 受控、1 精确、31 身体通过、4 非足部、1 迟触。
这不是新的独立留出考试，也不是已训练的通用路由能力。

S1041 将全部 11 段 CPU 合格示范的 410 个接触闭合前样本，
与 512 个父级保留样本一起做 256 次 actor-only Adam 更新。
权重拟合误差 0.003589→0.002643；critic 与噪声参数保持冻结。
S1043 手工精确重放更新通过。S1042 新种子 104201 成对考试：
父级到候选的受控传球 1→2、精确均为 0、身体通过 32→31、
非足部 4→3、迟触 1→2、场内 32→30，候选拒绝晋升。
更低拟合误差仍没有带来可靠的实际闭环。未制作或发布宣传片。

本轮结束时检查挂载：系统盘 `/` 剩约 473 MB，`/code` 剩
4.7 GiB，`/data` 剩约 36 GiB；三者不是同一个挂载。
保留已有 4 GiB 训练空间保护，不清理用户文件。扩大训练前
需解决系统盘紧张和输出存储安排。所有本轮任务已完成并保存，
没有宣称后台仍在训练。

## 证据

- S938：`/code/rosclaw/phase8_evidence/s912-shared-evidence/s938-second-option-admission-audit-shared/`
- S939：`/code/rosclaw/phase8_evidence/s939-second-option-admission-audit-v1/result.json`
- S938 report SHA256：`15177454fff4aa2a5c1845e8c1133d1df3e72e46f1091da03c1f507fcd9ecdb4`
- 1000 帧入场记录 SHA256：`e4982e496fa64d0608460cf5869f4f6f588f9c23623e8d8479dd91929f2b402b`
- 审计脚本：`s532-delayed-handoff-cpu-v1/audit_second_option_admission.py`
- S940：`/code/rosclaw/phase8_evidence/s912-shared-evidence/s940-second-entry-preparation-shared/`
- S941：`/code/rosclaw/phase8_evidence/s941-second-entry-preparation-audit-v1/result.json`
- S940 report SHA256：`c16b5dd6d1a7d33dfb0523e0f79b9e5c04ee2e4b2e5eb694992b3a5c65cc5b32`
- 全仓测试：`/code/rosclaw/phase8_evidence/s940-repository-tests.xml`
- S944：`/code/rosclaw/phase8_evidence/s944-second-entry-24s-paired-audit-v1/result.json`
- S947：`/code/rosclaw/phase8_evidence/s947-actual-return-pass-cpu-exam-v1/`
- S949：`/code/rosclaw/phase8_evidence/s949-return-pass-channel-audit-v1/result.json`
- S950：`/code/rosclaw/phase8_evidence/s950-annular-return-preparation-v1/`
- S955：`/code/rosclaw/phase8_evidence/s955-annular-preparation-comparison-v1/result.json`
- 最新全仓回归：`/code/rosclaw/phase8_evidence/s954-repository-tests.xml`
- 内部准备片：`/code/rosclaw/phase8_evidence/s945-second-entry-preparation-review-v1/second-entry-preparation-review.mp4`

旧第二技能的独立考试只验证真实足部加速、原有身体边界和无自身
非足部碰撞，不验证球门 0.1 m 精度或 4v4 比赛成功。旧高球射门
也主要来自定位球或专门入场，不能直接当作当前运动来球专家。

所有工作均为 SIM_ONLY；未操作真实机器人，未修改安全门或借此
授予执行许可。当前实验适配路径不是最新 ROSClaw Core 的发布能力。
