# RSI-M0 第一步实施记录（2026-09-24，SIM_ONLY）

这是 `/code/rosclaw/rosclaw_football/rosclaw_soccer_r1之后实施大纲0924.md`
第 72–73 节的第一段工程事实，不是 M0 完成声明。

## 父代与数据资格

- 基线 Soccer `c67df1b69204337dc79c38ad24cc4f21fc50e615`；R1 原证据保留在
  `/data/rosclaw_overflow/receiving-mechanism-reboot/m0/aim-teacher-calibrate-v1`，
  不搬动、不改写。`rsi.r1_import` 重新校验协议/银行/轨迹/物理回执的哈希与进球链。
- 本地导入回执：
  `/data/rosclaw_overflow/receiving-mechanism-reboot/rsi-m0-r1-parent-import-v1.json`。
  4 个宣称场景、12 次物理执行，**只有 1 条唯一物理轨迹**，
  `report_hash=sha256:e350aed432ea57e8d7388aa98e1ee6a70a552d69fb0dbd8aa4cbbfac5e3981eb`。
  全部标为 `CONSUMED_DEV`、`teacher_active=true`、`training_authorized=false`，
  不作为 Fresh/Sealed，也不允许声称四个成功泛化种子。
- 以前 ±2 cm 只有原点成功（1/7），较大网格 0/8；不能把 R1 称为成功盆。

## 外部参考与许可

`references/REFERENCE_MANIFEST.yaml` 固定 ProtoMotions、SONIC、MAPPO、verl、
verl-vla 的完整 Git SHA、源码许可文本哈希、权重许可差异和审核文件。
`scripts/bootstrap_references.py` 已在
`/code/rosclaw/rosclaw_football/repos` 校验 5/5 个 checkout；仅补缺项目，
不会覆盖已有项目。Dream-RSI 当前列在 pending，仅参考理念，不假设代码可用。

## Isaac Lab 隔离烟测

- Isaac Sim 6.0.1 + Isaac Lab 源码子模块 `a8554968`，使用现有 Soccer venv 的
  Torch 2.13.0+cu130，并将缺少的 `lazy_loader`、`warp-lang`、`gymnasium`、
  `prettytable` 安装在外置路径
  `/data/rosclaw_overflow/isaaclab-smoke-deps-20260924`。未修改共用 Isaac 安装。
- GPU 3 单卡，无界面，原有其他 GPU 进程未停止。8 个足球刚体并行 120 步：
  `min_forward_displacement_m=1.460823`、
  `mean_angular_speed_rad_s=12.987093`、
  `min_ball_height_m=0.109996`、状态有限；这是滚动的物理 smoke，非射门。
- SONIC 仓库预制 `g1_29dof.usd` 经 Git LFS 下载后解析不到唯一
  `ArticulationRootAPI`；原文件 SHA-256 为
  `84629d40e3514f48a07a4b13cda54303f771b1ed02a41ac8a9c896529ce4227a`。
  禁止直接将其用于训练。
- 改用同仓库 `g1_29dof.urdf`
  (`56d0c7b4e44351ee8e89e7b1f1fcc8b02d5ec5cd191e43239d2b06f039e75860`)
  与网格，由 Isaac Lab 官方 URDF 转换器生成外置 USD 资产。生成目录约 7.8 MB，
  主 USDA 为
  `cfbcca7b005c418697d35f762d81ec389d6242a0b4e73d889c25384724d879e8`；
  完整重现还须保留该目录的 payload 文件。预制配置含不存在的手指正则，
  只为 smoke 移除 `hands` actuator；其余官方 G1 29DoF 控制器配置仍未针对本资产校准。
- 4 个并行 G1+球、120 步，29 关节、有限状态；左髋目标跨环境相差 0.5 rad，
  终态相差 0.3953 rad，证明该仿真控制输入能改变关节输出。
  **机器人根高度降至 0.458–0.498 m，尚未稳定站立**；没有触球、
  learned actor 或 R1 成功。PhysX 输出了 Articulation path 和 TGS 警告，
  必须在接触训练前解析/验证；不能用这些 smoke 分数晋升。
- Isaac Kit 的 `simulation_app.close()` 在失败路径会掩盖 Python 异常的退出码；
  两个 smoke 脚本现明确输出 `*_ERROR` 并退出 2，拒绝伪通过。

最小复现命令（在 Soccer 仓库根目录；GPU 3 仅为本次运行选择）：

```bash
export PYTHONPATH="/data/rosclaw_overflow/isaaclab-smoke-deps-20260924:/code/rosclaw/rosclaw_darwin/reference_projects/IsaacLab-Arena/submodules/IsaacLab/source/isaaclab:/code/rosclaw/rosclaw_darwin/reference_projects/IsaacLab-Arena/submodules/IsaacLab/source/isaaclab_assets:${PYTHONPATH:-}"
export CUDA_VISIBLE_DEVICES=3 ACCEPT_EULA=Y
export PYTHONEXE=/code/rosclaw/rosclaw_soccer_s2/.venv/bin/python
/data/omniverse/isaac-sim-6.0.1/python.sh scripts/rsi_isaac_ball_smoke.py --device cuda:0 --num-envs 8 --steps 120
/data/omniverse/isaac-sim-6.0.1/python.sh scripts/rsi_isaac_g1_ball_smoke.py --device cuda:0 --g1-usd /data/rosclaw_overflow/rsi-g1-usd-20260924/g1_29dof/g1_29dof.usda --num-envs 4 --steps 120
```

由于 PhysX 路径警告和身体下沉，以上第二条仅是“关节有因果响应”的
集成检查。要用于训练还需明确初态、接触体、增益、资产版本和动作上限。

## 合同与下一道门

`rsi/contracts.py` 增加意图、身体观察（含根部线/角速度）、SIM_ONLY 动作建议、
模型/物理/身体/关节映射哈希绑定、明确动作种类与维度、物理 episode 分区。
它们没有硬件执行权，现阶段也不是 learned actor。现有球队 SONIC 观察是
完整足球世界 `qpos=43/qvel=41`，其中包含球的自由度；不能把它直接冒充
29 关节的独立 Athlete 本体观察。接入时必须显式拆分/校验索引与受控关节。

下一轮先做同资产站/走/停的父策略测量、关节映射/增益和 PhysX 接触审计，
再定义一人一球的动作/观察/奖励与初始化分层。必须**训练前承诺** Train、
Consumed、Fresh、Sealed。只有实训 actor 在无教师 Fresh 中扩大完成域，
且 MuJoCo 独立审计、安全、保留率、失败自动课程及二代候选全部过门，
才能把 RSI-M0 标为 COMPLETE。目前状态：**NOT COMPLETE**。
