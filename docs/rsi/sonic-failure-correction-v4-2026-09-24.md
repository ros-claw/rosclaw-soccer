# SONIC 右脚失败修正 v4：任务成功，但尚非宣传级运动

本轮全部是 **SIM_ONLY / CPU MuJoCo 500 Hz**；没有真实机器人、外加球力或像素评分。冻结 SONIC 只提供身体运动基础，v4 是从失败样本蒸馏的**有界上层横向速度选择器**，不是端到端关节力矩小脑，不能声称已经学会自然射门。

## 失败 → 修正 → 新球位考试

v3 在未训练的 `x=2.10 m` 四球位取得右脚先触球进门 `3/4`，`y=0.09 m` 一球右脚接触但未进门。固定这个失败后，在 DISCOVERY 分区只扫描该球位横向速度 `0.06/0.08/0.09 m/s`；三次均脚先触球进门。v4 将 `y=0.09 → 0.09 m/s` 作为一个局部校正结点，保持其他四个已验证结点。训练脚本要求独立重建 500 Hz 触球及整个球过门，拒绝伪造报告、错误血缘及覆盖已有 checkpoint。

模型：`/data/rosclaw_overflow/rsi-sonic-contact-selector-20260924-v4.json`，哈希 `sha256:23126ae9179e8493d54d07410e5e32be72e4e75581cec7f8c97a52a44d80901e`。新考试在此前未执行过的 `x=2.15 m`、`y=0.055/0.095/0.135/0.175 m` 上运行原始 SONIC 与候选各 4 次，起始物理状态逐对一致，代码和模型在执行过程中冻结。结果：候选 **4/4 右脚先触球并整个球入门**；原始 SONIC **0/4**（其中一次虽越过门线，但先由小腿触球，按预先定义不算）。候选峰值球速 `2.41–2.52 m/s`，这是近距离地滚球，绝非高难度大力射门。

原始证据：`/data/rosclaw_overflow/rsi-sonic-contact-selector-holdout-20260924-x215-v4/`。独立验证哈希：`sha256:19661e80874b1efcb13fc4d0f6ae7b470a9f521bb1f935333cb05f42635990f1`。26.47 秒 1080p 物理轨迹可视化：`/data/rosclaw_overflow/rsi-sonic-contact-x215-fresh-four-goals-20260924-1080p-v2.mp4`，SHA-256 `a6443f013820293f68773fcacc777679ffdf6bf1be01422dd0f20b7018ab772f`；v2 镜头让 G1、球、球门同框，不更动物理轨迹。画面标记 `SIM ONLY / NOT PROMOTED`，并显示完整 4/4 与 0/4 的对照，不用于评分。这里使用的是 **2.4 × 1.6 m 人形训练球门**，不是标准成人球门，不能混为同一难度。

## 反证：现有 SONIC 助跑＋旧式踢球拼接链

为尝试获得真正摆腿和较强球速，复用了仓库中的严格回放 free-kick 链。基线真实踢球接触、球速 `7.33 m/s`、球过线，但切换到触球需 `2.744 s`、目标误差 `0.949 m`、助跑最低骨盆高 `0.684 m`、踢球阶段倾斜 `0.581 rad`，整条证据 **未通过**。缩短 bridge 并提高 kick phase 起点把等待缩到 `1.092 s`，球速却仅 `1.38 m/s`；另一组合等待 `1.666 s`、球速 `4.42 m/s` 且未进门。由此可知单纯缩短过渡帧会破坏有效触球，不应剪辑掩盖或当作训练突破。

该链的三个原始试验分别在 `/data/rosclaw_overflow/rsi-sonic-free-kick-reuse-20260924-v1/`、`...-phase220-bridge20/`、`...-phase190-bridge30/`，还有 `...-phase150-bridge20/`。第一段内部诊断视频明确标记 `REJECTED SONIC CANDIDATE`：`/data/rosclaw_overflow/rsi-sonic-free-kick-reuse-20260924-v1/diagnostic-NOT-QUALIFIED.mp4`。不能把它们当作合格宣传片。

## 质量门与下一步

这轮只能说明**有限球位上的接近/脚先触球决策获得了可验证改进**。视频仍像走近地滚球，球门画面和摆腿观感不足，身体动作也未达到顶级球员；4/4 仅 4 个近邻球位，不能外推到高角度、长距离、守门员或完整比赛，更不能拿同一球位 DISCOVERY 结果冒充 FRESH。下一步重点应是连续身体策略：在 SONIC 动作空间中学习带接触相位的摆腿和随后的动态恢复，用新距离、初速度、左右脚、目标高度和对手干扰做真正隔离的考试；同时增加视觉质量门，避免只凭进球率提前推出宣传片。
