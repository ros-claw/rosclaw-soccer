# 下一阶段总纲：执行索引，不是验收完成报告

更新于 2026-09-21。历史 17/128 只作公开研究参考。没有新 Champion，
没有打开 Sealed Bank，也没有开始学生或 PPO 扩训。

## 当前推进门

| 总纲环节 | 已有证据 | 尚不能声称的内容 |
| --- | --- | --- |
| R0 冻结及银行分离 | 128 旧课认证、17 历史成功保留；独立 DEV256 实测 10/256、逐例重放 | 旧 128 不是盲测；10/256 不是与 17/128 配对比较 |
| M0 四接口可达性 | 1760 次有回执执行；110 完整搜索组、27 个四路配齐初态；原批次已排空停止 | 未执行 146 组不能记失败；完整 64×4 未完成，不能排底座能力名次 |
| 搜索正对照 | 先验提前提案 A0 6/8；同解精确嵌入 A1 6/6；随机 A1 未恢复正例 | 说明原搜索 assay 不足，不证明 A1 比 A0 差，也非因果教师 |
| 冻结 SONIC 身体 | 前行、侧移、支撑切换与刹停正对照，全部独立重放 | 静态参考抬踝未实际卸载；基础运动不等于接球 |
| E2 状态反馈教师 | 人类参考、当前脚部 IK、摆腿参考及局部关节权限检查均有原始失败证据 | 未资格通过；没有以视频、末态球慢或首次触球冒充通过 |
| E3 DAgger → recurrent → asymmetric PPO | 顺序和证据边界保持 | 未训练，不能将诊断仿真次数报成学习样本或权重更新 |
| 战术并行线 | SkillCorner 分比赛先验；256 新物理课程和 16 重放；能力模型在 DEV 有覆盖审计 | 邻域模型在 116 覆盖课上 Brier 不优于常数基线，不接成合格 TacticalWorld/MAPPO |
| Core 通用能力 | Research Coach、sealed 账本、Practice 集成及私有几何表面检查进入 Core PR #587 | PR 尚需人工门控；契约、记账与本地存储不等于完整自动研究服务或 OS 级 sealed 隔离 |

## 实施入口

- [原始启动快照](receiving-mechanism-reboot.md)：历史状态，不能当作当前进度。
- [物理与控制器恢复](receiving-state-replay.md)：区分物理 checkpoint 与完整控制器续接。
- [动作接口](receiving-oracle-interfaces.md)、[动作权限](receiving-action-authority.md)：界面和请求到执行的证据。
- [搜索敏感性与经验补录](receiving-search-sensitivity.md)：正对照、嵌套解、停止原搜索、Practice 更正。
- [参考与接触教师探针](receiving-reference-teacher-probes.md)：静态/自然参考、接触对象及局部权限实验。
- [实测技能模型](measured-receiving-capability-pilot.md)：覆盖未知值、概率校验失败，不伪造 ready/duration。
- [人类战术意图](tactical-intent-pilot.md)、[路径先验](tactical-waypoint-pilot.md)：与机器人能力资格分开。

## 最近完成的停止判据

`m0/shin-clearance-local-oracle-v1/complete.json`：两课各九个预声明膝踝组合，
含对照与独立重放，共 36 次，全部安全、全部未成功，零增益门未通过。
首个考试窗口的非脚接触没有消除；停止本局部调整族，不追加幅度扫描。

`m0/transferred-a3-authority-v1/complete.json`：另四次只观测的运行，旧数组
逐项相同；两例各 3000 物理子步。请求残差被抑制的样本均为零，关节安全
投影改变样本均为零；力矩裁剪分别为零和八个关节样本。未测到额外非 PD
任务力矩。因此不能把这两例失败解释为接口吞掉了请求。

下一项 `m0/causal-velocity-pacing-v1` 是预先声明的原动作／完整导航命令／
当前状态随球导航三个条件，最多 12 次，不更换身体、考试或模型权重。
仅在原有导航保护上游提出有界增量，不从电机内部绕开场地避碰。
必须超过两个对照才有继续价值；预算和启动不代表已完成或成功。

后续该导航试验完成 12 次，所有条件安全、严格重放一致，反馈仍未带来
成功增益，停止该族。随后 `m0/predictive-task-teacher-v1` 则回到已见
成功教师力矩的线索：比较原控制、观测透传、当前状态 Jᵀ/Kp 转换和常速
预测 100 ms 后的同类转换。预测只操作私有数据，不读真实未来，不施加
额外 task torque。幅度/滤波/速率界限不变，但这是每步反馈而非原四节点
时间参数化，不能混入 M0 计数；最多 16 次，须先精确通过透传对照。

v1 因注册电机观察器与 A0 控制权冲突，在教师执行前被拒绝。实际只有四次
无观察器基线完成，不能计为 16 次教师失败；保留 partial-execution-inventory。
随后将只读反馈提案与电机控制权分离，不绕过原检查。新接口只接收不可变
当前状态，输出 12 维 ±0.1 rad 期望残差，仍由原 cursor 滤波和限速；
输入/身份变化、非有限输出和时钟漂移会终止离线实验。它不是不可信代码沙箱。

`m0/predictive-task-teacher-v2/complete.json` 实际完成 16 次。透传物理逐项
相同，独立重放一致，全部安全，但当前状态和 100 ms 常速预测均未救回两课。
manifest `db45268c9ac3f650f1f5c17583ab0f87dd792ac1ecc2e3b34d08af957b413ac3`。
预测检查发现常速外推不处理支撑约束，部分踝原点被预测至地面以下；实际未来
踝原点仍在约 0.033 m 以上。该后验对照含未来执行动作，不是固定动作动力学验收。
`forecast-diagnostic.json` 也明确：已记录 xpos 与步后 qpos 有一个物理子步的
时序差，不能假定其同刻；比较预测时统一由对应 qpos 重算 FK。

进一步核查显示，当前教师以最近脚接触推算 capture，而原控制器保留首次
接球事件。两课原 capture 活跃各 30 帧，教师猜测各 46 帧，有 16 帧不一致。
新增只读事件上下文，保留原 capture 起点、持续时间、脚和方向；不重新授予
接球、不重置控制器、不修改考试。下一项 v3 固定四条件、最多 16 次，比较
此语义修正，要求三种旧对照先精确复现 v2，再判断是否存在真实成功增益。

v3 完成 16 次，三个旧对照精确复现，仍然零成功增益；其 manifest 为
`b94cd0b3935ddcd408c95a11044ba511b0d2c720b2c6cd57f17cfdf1ee11e209`。
Core `438e86dd` 新增通用 `feedback_teacher_contract_hash` 和教师资格判断，
保留旧 campaign 哈希。已验证的 v3 回执接入 Research Coach，原 M0 搜索
灵敏度失败仍优先路由 SEARCH_OR_ASSAY；不伪造 oracle 通过来解锁学生。
Core 定向验证为 278 passed、4 skipped，PR #587 未自行合并。

`m0/contact-aware-predictor-v1` 用相同状态的私有单体 MuJoCo 预测替代常速
FK，完成 148 次 100 ms 预测（含一次重复），不是新增 148 场接球。
两角色近球脚位置平均误差从 0.01456/0.01622 m 降至 0.00602/0.00547 m，
通过预声明 25% 改善、球误差不恶化及踝原点不穿地的初步门。
该模型省略其他球员、策略记忆更新、球网及原求解器 warm-start；对比的是
实际未来闭环轨迹，不是已经证明可逐步恢复的完整世界。

随后 `m0/predictive-task-teacher-v4` 完成 16 场与 890 次私有预测。
前三条件与 v3 的物理数组精确一致；全部安全、独立重放一致，但预测教师
仍零成功增益。manifest
`08e79b9bc85a1037de5b01a6880640649906c6de905e361df6f648e19323e3a9`。
因此停止“预测状态 → Jᵀ/Kp 偏移”微调族。预测精度改善不能冒充控制学习
或接球突破。接下来先核查 200 ms 预测范围，再做有严格候选/执行预算的
短时模型预测动作比较；不改变当前球速、考试、权限限幅，也不启动 PPO。

200 ms 预测复查同样通过初步门，两角色近球脚误差为约 0.02186/0.02066 m，
常速对照约 0.03829/0.04506 m；仍只是近似模型检查。加接触诊断后的预测器
在六个记录状态上与原预测器逐状态完全相同（12 次私有预测）。

`m0/model-predictive-contact-teacher-v1` 完成 16 场、1160 次私有预测：
至多十个候选，每五帧重规划；独立重放和旧对照均精确，但零成功增益。
原设计在规划间保持滤波后偏移，两角色活跃 110/179 帧中分别有 88/143 帧
保持该值。这是额外时间参数化约束，不是原执行器吞掉动作。
随后实现私有预测器内的原 50 Hz 滤波，与原 cursor 的十步序列逐值相同；
其恒定目标退化路径也与原物理预测完全相同（30 次私有预测资格检查）。

`m0/model-predictive-contact-teacher-filtered-v1` 实跑 16 场、2480 次私有
预测，恢复持续追踪期望偏移，保留原幅度和速率限制。仍零成功增益，全部安全、
重放一致；在关键 1.20/1.30 s 查询处，候选集合均预测了非脚接触，不能用
预测球慢来忽略碰腿。没有更换考试、没有新学生或权重更新。

下一步回到模型/控制状态验收：底层 TorchScript 明确包含 `hidden_state`
和 `cell_state`，各形状 `(1, 1, 256)`，另有上一网络动作。保持 PD 目标的
私有模型没有推进这部分运动记忆。先检验独立推理状态恢复与下一步输出复现，
再判断是否能改善预测；尚未证明它是接球失败的主因。

已完成这一步的组件验收，见
[运动记忆与预测边界](locomotion-memory-forecast.md)。两份既有成功课和两份
既有失败课分别完成记忆记录／独立重放，原物理结果不变；从真实身体状态
重新构造输入，合计 1196 次后续推理逐值复现。两个 200 ms 预测对照各
108 次私有预测，失败课近球脚误差从 9.15/18.91 mm 降到 0.39/0.36 mm。
仍不算新接球成功。其后的循环 MPC、净空优先、500 ms 预见及新增表面候选
四轮双案例对照已完成，各 12 次物理执行，全部安全、对照与重放一致，但
每轮均为零救回。停止扩充这类局部恒定偏移候选，转向完整准备动作序列的
资格检查。详见上述运动记忆文档；E2 和学生训练门未开启。

`practice-mpc-memory-v1/complete.json` 将两份既有成功 fixture 和两份已
认证 MPC 失败轨迹，经 pinned Core CLI 的 record/strict verify/distill/
本地 SQLite-compatible ingest/query 串通；成功和失败各查回两条，重复
导入失败不增加条目。预测候选与实测状态明确分字段，不把预测接触写成实际
成功。manifest `ed45031edf6baf79b00406abd849b98bf34b4c084f7d319cae687f4770e941e1`。
这是经验保留与检索，不是新增独立场景或已学习到技能。

## 工程与证据纪律

足球专用转换、具名关节提案与实验留在 Soccer；通用研究与几何检查留在 Core。
原始数据、权重、脚本、轨迹与 SQLite 库全部外置；本文的证据相对路径均相对
`receiving-mechanism-reboot`。保留负例、部分失败和更正附件，不重签历史回执。
Practice v2 的转换器版本竞争必须连同 regeneration audit 阅读，不能只引用
其完成报告。新脚本须检查最终展开源码、课程域及各冒烟步骤的独立退出码。

这份索引不代替总纲的资格门：完整 M0 的可解释结论、稳定反馈教师、学生
跨状态继承、三训练种子与新一次性考试、Receive→Next、校准后的 2v1 和
MAPPO/FSP 仍有实质工作。不能因已经做了很多实验就把这些项打勾。

新的[完整序列对照](sequence-prior-contact-control.md)中，固定跨角色先验
救回两个既有失败课程，短时域在线择优只救回一个。锁定首选仍复现差异，
不把开环成功包装成 E2。六课程连续性及接触时钟对照均仍为 2/6；准备
动作修正消除了两例快球碰腿，却未留住球。随后定位到私有预测遗漏未来
导航反馈，补齐因果导航和[只读邻居上下文](receiving-navigation-context.md)，
并响应预测球逃逸后，四个已见后卫/中场课程由 2/4 到 4/4。16 次执行全部
身体安全且重放一致，仍不是独立盲测或 E2 资格；慢球侧偏扩展正在执行。
学生门保持关闭。

Soccer `b0eac15` 全量测试实跑为 **4910 passed、55 skipped、11 failed**。
失败用例身份集合与 `8c6afd5` 完全一致，均为既有外部历史证据拒绝，未
重签或绕过；仍不是全仓通过。跳过项含未配置外置资产、旧视频及显式 GPU
测试，不能计成功。新转换器与提案契约的针对性 76 项测试全部通过。
全量 XML：`reboot-regression-b0eac15.xml`。

反馈接口提交 `6bef9c2` 全量为 **4932 passed、55 skipped、11 failed**；
XML 用例身份比较确认仍是完全相同的 11 个历史证据失败，没有新增失败。
配置真实外置 G1 资产后，世界相关测试 **6 passed**，不是把缺资产跳过当通过。
capture 上下文扩展的相关测试 **136 passed**，尚不代表新教师物理资格通过。
最终相关验证扩至 **142 passed**；Soccer `4b5f5e5` 全量为 **4946 passed、
55 skipped、11 failed**，XML 比较仍为相同十一项历史证据失败，没有新增。
Core `438e86dd` 的远端自动测试（含 Python 3.11 全量及 3.12/3.13）、类型、
边界和验收均已通过；人工 gate 待审，不自行合并 Core PR。

循环记忆接口 `e0cfc48` 在不可变 worktree 全量运行：**4966 passed、
55 skipped、11 failed**。XML 用例身份比较与 `4b5f5e5` 的十一项失败完全
一致，无新增失败。显式配置真实 G1 资产的世界测试 **6 passed**。
完整仓库依然不是全绿；不重签历史失败证据。记录接口首版的 hash 前缀
集成失败单独保留在 `m0/locomotion-memory-replay-v1/STOPPED.md`。
