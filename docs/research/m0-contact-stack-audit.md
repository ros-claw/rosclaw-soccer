# M0 接球机制复查：先分离控制栈，再比较动作空间

2026-09-21。SIM_ONLY，未晋升策略；R0 仍为冻结研究参照。

## 原始小样本实验

8 个 playmaker 初态、4 种底座，每组 15 次候选执行及 1 次最佳候选精确重放，
共 512 次 CPU MuJoCo 执行。A0 leg12 为 4/8，A1 body29 为 3/8，
A2 SONIC 为 0/8，A3 SONIC 加 residual 为 1/8。

这些不是 64 初态 M0 的结果，也不是学习后的盲测成绩。复查发现：A0/A1
保留旧接触教师的附加力矩，而 SONIC 的全身控制所有权正确排除了该教师。
因此原始比较测的是不同控制栈，不能用来判定动作维度或 SONIC 的能力上限。
原始轨迹和失败记录全部保留。

## 十次因果复查

选两个已有成功轨迹，分别执行五种对照：A0 原候选、提升到 29 维的同一候选、
A0 移除教师、A3 原候选、A3 显式移除教师。

- 两个 A0 成功动作补零扩展为 29 维后，除接口合同哈希外，所有原始轨迹数组
  完全一致；A1 搜索没找到相同成功，不代表其动作空间无法做到。
- 两个 A0 在移除教师后均失去接球成功，但仍满足该实验的安全判据。
- A3 显式移除教师与原 A3 完全一致，验证其原本就没有这一附加控制。

所以扩大搜索前必须控制教师差异，不能直接扩大旧 PPO 训练量。

## 初态银行

64 个实际物理初态覆盖 8 个角色实例、四档来球速度、左右偏移和不同分支时刻。
每个初态均独立重放，保留模型、积分状态、控制前缀、策略和配置哈希。
8 个原始采样点已发生脚球接触；保留这些原件，另将对应分支时刻从第 25 帧
提前到第 5 帧，球的发射参数和随机种子不变。

修正后的银行 64/64 均在首次脚球接触前分支，64/64 原始重放一致。
包含左右单支撑与双支撑；20 个近静止、30 个运动、14 个中间速度状态。
这证明银行可复现，不证明策略能接住其中任何指定比例。
控制器历史靠相同前缀重建，尚不是完整控制器内存的序列化恢复。

重要覆盖限制：原生 R0 全栈在这 64 个新课程中为 **0/64**，不是历史 17/128
考试的重测。16 个分支点还处于初始化第 0 帧，有明显初始化支撑瞬态。
所以这份银行不能单独声称已经覆盖“已知容易成功”的正控制或稳态静止分布；
原始小样本中的成功正控制需单独保留，后续还需补齐该覆盖，不能隐藏这一缺口。

银行合同：`sha256:f33816c3d84850863dd705da9052a4f288365d5d98303f6500e65e8885d4c930`。

## 下一次匹配实验

四底座在同一前缀后均移除旧接触教师；保留原生 R0 全栈轨迹作为独立参照。
SONIC 不再仅搜索导航速度，而搜索 4 个 64 维编码 token 的有界残差节点；
残差 L2 不超过原 token 范数的 10%，且绝对 L2 不超过 0.5，初始渐入 10 帧。
下游关节和力矩边界不变。零残差已在真实模型仿真中验证逐数组不改变旧轨迹。

搜索器不再把“原生策略 incumbent”误当成零向量加入 CEM 高斯拟合。
每底座仍保留 16 次执行的固定预算。单初态冒烟 64 次执行已完成，四底座均未
成功，最佳轨迹均安全并精确重放。完整 64 初态实验尚须完成后再记录结论；
有限预算有界搜索失败不能证明动作空间全局不可达。

## 冒烟失败的进一步分解

以 `receiving_rollout.explain_receiving_window` 逐条拆解，分数与原始判决保持一致。
A1 有 3 个窗口仅缺少触球后至少半秒的观察长度；不能因此改判为成功。
其他底座同时存在小腿触球、无本足接触、末尾离脚过远等失败。
两秒窗口从因果接近条件开始，不因触球重新计时；慢球较晚到达时，该约束会
成为实际难点。它需要策略主动迎球及任务定义审查，而不是随意放宽考试。

编译模型核对：球半径 0.115 m、周长约 0.72257 m，质量 0.410 kg。
因此该冻结遗留场景不能宣称成人标准足球尺寸。球自身 geom condim 为 3，
但显式 floor–ball pair 以及 64 个入口处实际地面接触的 condim 均为 6；
不能只看 geom 参数就断言球没有滚动摩擦。
冻结 R0 发射时角速度为零，不是预设的无滑动滚动释放；入口处名义球底水平
滑动速度覆盖约 0.0011–1.6247 m/s。尚未完成材料或真实足球的摩擦校准。
这些是模型与状态测量，不是修改参数后的能力提高。

原始与新增证据分开保存；尺寸、初始自旋或判决改动均应定义新实验合同。

联合回归：183 passed，48 条上游 TorchScript 弃用警告。
这不是整个仓库测试结果，也不是 E2 闭环接触教师或 E3 学习已经完成。

外置证据相对目录（根为 `receiving-mechanism-reboot`）：

- `m0/oracle-pilot-v1/complete.json`
- `m0/contact-stack-audit-v1/complete.json`
- `m0/initial-states-v1/complete.json`
- `m0/precontact-replacements-v1/complete.json`
- `m0/precontact-bank-v1.json`
- `m0/matched-oracle-smoke-v1/`：独立接口冒烟，结果以完成凭据为准。
- 同目录 `failure-decomposition-v1.json`、`compiled-geometry-audit-v1.json`。
- `m0/ball-entry-audit-v1.json`：64 个入口的世界系球速度、自旋和真实接触维度。

已将真实银行和冒烟凭据交给 Core Research Coach：输出 `NEED_EVIDENCE`。
四库尚未完整绑定，未知项保留为 null，未用冒烟失败伪造完整 M0 失败判决。
这次连接是诊断，不是 Dream 调度器接管，也没有训练或晋升授权。

上述数据不进入 Git，不属于封存考试；任何策略晋升仍须独立门控。
