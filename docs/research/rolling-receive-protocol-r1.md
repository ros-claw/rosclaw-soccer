# 滚动接球协议 R1：设计（2026-09-22）

本文是"滚动接球新协议"路线的正式设计。路线决策由用户在复核
`receive-to-next-action.md` 末节证据后作出：冻结 R0 课堂内接球→下一动作
已被证明为耦合封锁（钉住 + 跟随包络 + 球权/深度/摆腿时序互斥），不再继续
在冻结课堂内寻找单一环节修复。

## 1. 证据基础（只列已核实事实）

- R0 课堂协议（`r0_receiving_configuration`）显式开启
  `loose_ball_capture_hold=True, hold_sec=0.6`：接球后身体被钉住 0.6 s。
  比赛世界默认 `loose_ball_capture_hold=False`，没有这一钉住。
- 教师终局偏好"最死的球"已被重定向评分替代并物理验证：同一候选族、同一
  权限下，球可以 0.2-0.3 m/s 保持滚向进攻方向（m0/redirect-receive-dribble-
  v1/v2，控制组精确复现）。
- S208 已在连续比赛世界完成接（滚动球）→ CAPTURE→ORIENT→PLANT→STRIKE
  →真实射门链（PASS_PHASE_CONDITIONED_STRIKE_SAVE）。
- 相位链两个已核实的 SHOOT 取向缺口：`_controller_strike_stance_metrics` 与
  `_strike_tracking_yaw_error` 的方向都来自
  `(goal.plane_x_m, goal.target_y_m)`；`strike_phase.begin_capture` 仅对
  `MatchRole.FINISHER` 开启；射门租约仅授予 SHOOT 意图。
- PASS 激活路径只有 owned_option（触球后 0.2 s）与 prospective
  （`prospective_enabled=True` 且球权为 None 且当前追球者）；PASS 无租约。

## 2. 协议目标与边界

目标：在显式新协议 R1 中证明 接球（球保持滚动）→ 相位接续 → 真实传出脚
触球 → 队友到达 的完整链，先在极少量已见场景，再逐步扩课。

边界（与 R0 相同纪律）：

- SIM_ONLY；CPU MuJoCo 为最终物理真值；视频不作成功标签。
- 考试不放宽：接球考试、身体安全、`pass_contact_chain`、带球后继考试原样
  复用；相位状态机单调前进、超时/失稳 fail closed。
- R1 是新协议，不是放宽 R0：R0 课堂与 40/128 路线门原样保留为历史参照；
  R1 课程银行独立生成、独立资格验证，不与 R0 成绩拼接比较。
- 教师可用特权仿真信息做可达性，不是部署策略；学生训练（DAgger/PPO）必须
  等 R1 教师资格之后，且一次仅一个球员 plastic。

## 3. 协议组成

### 3.1 世界配置

以连续比赛配置为底（`default_continuous_match_config`，无接后钉住），
显式声明的 R1 差异：

| 项 | R1 选择 | 依据 |
| --- | --- | --- |
| 相位控制器 | 开启（`StrikePhaseConfig`，S208 同款，含 0.36 s 触球预测） | S208 已验证 |
| 相位捕获角色 | 扩展到接球者（不再仅 FINISHER），按场景显式绑定 | 接球者可能是组织者 |
| 相位朝向/站姿目标 | 传球任务目标感知（见 3.2） | 球门取向对 PASS 不适用 |
| 踢球桥 | `pass_enabled=True, bilateral_enabled=True, task_context_bound=True`，默认入场几何不变 | 与既往 PASS 探针一致 |
| PASS 激活 | 首选 prospective（球权 None + 当前追球者）；不新增 PASS 租约 | 不新增所有权语义 |
| 球权保持 | `contact_possession_hold_sec=0.20` 不变 | 不延长球权 |

### 3.2 传球目标感知的相位接续（唯一计划内的源码改动）

为 `_strike_tracking_yaw_error` / `_controller_strike_stance_metrics` 增加
显式任务目标注入：当持球决策为 PASS 且存在
`decision.target_position_m` 时，相位度量与朝向误差以该目标替代球门；
SHOOT 路径与默认值逐位保持不变（冻结 S208 路径）。改动限 soccer 仓、带
定向测试（默认路径哈希不变、PASS 目标注入路径的新几何正确性、禁止硬件
权限），走正常 review+commit。

### 3.3 接球教师

沿用接触教师（S208 的 `default_phase_strike_teacher` 保持了已验证的传球
接法）。R1 的接球终局目标是"卸力后球仍以 0.15-0.35 m/s 滚向下一动作
方向"（重定向评分已物理验证可达），而不是钉死。教师权限边界不变：
有界 12D 残差、原滤波、原速率、原安全投影；私有预测预算显式声明。

### 3.4 考试与证据

每次实际执行必须同时给出：原接球考试结果、相位状态机轨迹（单调性）、
身体安全、`pass_contact_chain` 的完整 500 Hz 触球归属、控制见证、
独立重复全数组一致、介入前前缀与归档对照一致。允许失败；不允许把
"请求了 PASS"或"相位到达 STRIKE"当作传球成功。

## 4. 里程碑与判据

| 里程碑 | 要证明的事 | 退出判据 |
| --- | --- | --- |
| MR1 | 单一已见比赛场景：滚动接球→相位→真实传出脚触球→队友到达 | 2 次独立重复均通过 pass_contact_chain 且原接球考试保留；否则记录并停止该机制 |
| MR2 | 方向/速度小课程簇（≤8 个新场景）同链 | 每场景重复一致；无安全回归 |
| MR3 | R1 课程银行（新 seed 生成，含 DEV/SEALED 分离）资格 | preflight 全部通过；R0 历史参照不动 |
| MR4 | 稳定反馈教师资格（闭环重规划，非播放） | 教师在新场景重复达标后才谈 DAgger |
| MR5 | DAgger 学生→asymmetric PPO（一次一个球员 plastic） | 按总纲配对考试与 3 seed 规则 |

任何一级失败：记录、停止该机制、回到上一级判据；不允许跳过 MR1 直接
扩课或开训。

## 5. MR1 有界探针预定义

- 场景：连续比赛夹具中的一个已见传球→接球场景（S208 同族），接球者
  改为请求 PASS（目标为当时测得的队友）。
- 机制：3.1+3.2+3.3 的全部；prospective 激活；无租约。
- 预算：≤6 次实际执行（2 对照复现 + ≤4 候选）；每次私有预测 ≤9000；
  全部冻结源码并哈希绑定；先写 protocol.json（假设/验收/停止判据）。
- 验收：对照精确复现；候选两次独立重复均通过 pass_contact_chain、
  原接球考试保留、身体安全、相位单调。
- 停止：任一候选不安全/接球考试丢失 → 否决；两次均无传出脚触球 →
  否决并审计相位几何；不做无界参数扫描。

## 6. 已知风险与开放问题

- 相位接续在 PLANT 收敛的是"预测触球点站姿"；传球目标移动时预测误差
  可能使 STRIKE 无法到达——MR1 正好检验这一点，不预设成功。
- prospective 依赖"当前追球者"仲裁；队友/对手更近时接球者可能拿不到
  prospective 资格。若发生，显式记录，不作为失败掩盖。
- 低滚动摩擦（≈0.02 m/s²）下球不会自停；相位链按设计处理滚动球，
  但踢球激活后的 0.6-1.2 s 摆腿延迟内球仍在动，传球精度是真实风险。
- R1 与 R0 成绩不可拼接；R1 课程银行未建成前，任何"成功率"都仅限
  已见小样本，不作为泛化结论宣传。

## 7. 当前状态

协议设计完成（本文）；3.2 的源码改动与 MR1 探针尚未实施。没有正在
运行的实验；没有训练、Sealed 或晋升。

## 8. MR1 物理迭代记录（2026-09-22，均 4 次实际执行，重复逐值一致，全部安全）

实施顺序与结果（源码 main `9d1ba20`，冻结树 soccer-phase-pass-target-v1）：

| 探针 | 机制 | 结果 |
| --- | --- | --- |
| mr1 | 接球后按球权闩锁 PASS | 物理前停止：夹具 finisher 未授权 LEAD_PASS（仅组织者持传球授权）；STOPPED.md 保留 |
| mr1b | 声明夹具能力（basic_ball_play+克隆 LEAD_PASS 绑定） | 完成但无效：探针身份 bug（replace() 使 `c is own` 注入失败），包装 cell 未入世界；四执行为有效 stock 复跑（S208 行为复现：接→相位→射），保留为该用途证据 |
| mr1c | roster 同步能力声明 | 物理前停止：roster 承诺守卫拒绝 hash 变化的 self_model；STOPPED.md 保留 |
| mr1d | roster+cell 同步声明；接球后按球权闩锁 | 闩锁成功（两次重复均 t=3.10 传向 red.playmaker）；相位 CAPTURE→ORIENT；ORIENT 3.4s 超时（中止码4）：接球后球以 0.42-0.47 m/s 滚向与传球目标相反方向，预测站姿深度持续为负（-0.1→-0.66），yaw 误差 2.0+ rad；球在 7.364s 被 blue.playmaker 身体中断；无激活、无接球到达 |
| mr1e | 接球前以 active_receive_source 闩锁 | 该信号被 receiver_commitment_priority 门控恒为 None，与 mr1d 逐值相同，无新信息 |
| mr1f | 来球感知预承诺（无球权+球逼近≥0.2 m/s+距离≤2 m） | control 两次重复：t=2.00 闩锁（来球途中）；接球真实发生且卸力到 0.05 m/s；CAPTURE→ORIENT→PLANT 在 t=3.62 到达 PLANT；站姿深度 0.88-1.06 m 与横向 0.18-0.30 m 均在窗口内，**唯独 yaw 误差停在约 2.0-2.5 rad**，PLANT 3.4s 超时（中止码5）；无激活、无接球到达。prospective 两次：闩锁后 chaotic 轨迹分叉，传球未再发生（无触球） |

诊断（为什么 PLANT 卡 yaw）：传球方向是**回传**（-x，朝己方半场）。红方踢球
reference/镜像机器只覆盖红攻 +x 与镜像蓝攻 -x 的攻击帧，"红方向 -x 踢球"
不在已验证帧内；相位姿态的 yaw 控制因此没有把身体转向传球方向。这与
mr1d 的"球滚离目标"是同一约束的两面：**首条传球链必须让传球方向与
来球滚向、攻击帧三者一致**。本场景自然满足三者的是组织者向前的
组织者→终结者传球（该场景开场实际发生的那脚），而不是终结者回传。

MR1g 设计（待执行）：焦点改为 **red.playmaker**——自然捡起散球后向前传给
red.finisher（目标在攻击方向、踢球帧合法、接球者在前）。需要把相位捕获从
FINISHER 扩展到"PASS 意图的接球者角色"（有界源码改动+测试，SHOOT 路径
不动）。备选：闩锁加控球门控（先停稳再传），保留在 MR1g 失败时启用。

prospective/control 跨配置轨迹出现混沌分叉（同一 flag 从 f20 起改变轨迹），
已在 mr1b/mr1d/mr1f 三次复现：跨 flag 的逐帧对照不可用，模式内重复与
逐模式结果仍确定。此现象疑似桥配置哈希进入某集合迭代序，值得单独核查，
不阻断当前结论。

### mr1g（playmaker 向前传）：闩锁与捕获引导死锁（源码 7e86b16）

相位捕获扩展（PASS 意图触球即捕获）已随 `7e86b16` 合入，56 项 strike/世界
定向回归全绿：stock 场景在触球时还没有 PASS 决策（传球决策在触球后形成），
捕获集合与物理不变。MR1g 探针（playmaker 自然控球→预承诺向前传 finisher）
四次执行全部：t=0.40 闩锁成功（目标 red.finisher，攻击帧合法），但随后
playmaker 在 PASS 意图导航下**退到球后约 0.85 m 站位而不再触球**，球以约
0.1 m/s 缓慢滚走，四次均无传出脚触球、无相位、无激活。诊断：捕获引导
（bootstrap）死锁——捕获要求"PASS 决策时刻的触球"，而 stock 流程里该触球
总是发生在 PASS 决策形成之前；决策变成 PASS 之后，导航改为站位居撤，
不再产生下一次触球。对照 stock：组织者触球→形成传球决策→**继续随球走**
→f84 用接触教师 stroke 传出（32 N）——"决策后随球"正是相位链需要的
引导行为。

下一步（按证据二选一，先廉价验证再做有界执行）：
- MR1h-a：让闩锁后的决策在"下一次触球前"保持随球导航（例如闩锁先只记录
  意图、决策在估计触球距离内才翻转为 PASS），与 stock 的决策后随球一致；
- MR1h-b：世界侧把捕获引导扩展为"PASS 决策新鲜 + 球在近距离内"时允许从
  ORIENT 直接启动（不依赖触球）。这是更深的语义改动，仅在 MR1h-a 失败
  后评估。

MR1 系列至此的已验证增量：传球闩锁（球权/来球两种触发）、声明式夹具能力
与 roster 同步、接球指向传球目标（卸力到 0.05 m/s）、相位推进到 PLANT 且
深度/横向入窗、捕获角色扩展（零回归）、跨配置混沌分叉的系统性记录。
未通过项集中在：回传踢球帧非法（mr1f）与捕获引导死锁（mr1g）。

### mr1h（无相位直接 prospective 激活）：深度/横向合法，朝向无人收敛

mr1g 的 playmaker 站位实测深度 0.73-0.92 m、横向 ≤0.23 m（f80 起全在窗口
内），因此 mr1h 关闭相位控制器直接 prospective 激活。四次执行均未激活：
**yaw 误差停在 0.41-1.07 rad（上限 0.35）**——playmaker 朝向停在 -0.2 到
-0.37 rad，始终不转向传球目标方向。PASS 意图导航给出深度/横向站位但不
负责身体朝向；收敛朝向正是相位 ORIENT 的职责，mr1f 也证明它能做（只是
回传帧非法）。另外球滚向 finisher 后，追球者仲裁在 f240 切到 finisher，
prospective 候选随之失效。

MR1 系列收敛出的最终结构结论：前传链条的所有单元均已单独验证——闩锁
（两种触发）、能力声明与 roster 同步、接球重定向、深度/横向站位、相位
yaw 收敛、捕获扩展零回归——缺的是把三者同时接通的一次引导：

**MR1i（下一有界改动）**：相位控制器开启 + 闩锁 PASS + 捕获引导从"触球"
扩展为"新鲜 PASS 决策 + 无主球在近距离 + 本队追球者"（从 ORIENT 直接
启动；CAPTURE 语义只用于真实触球）。这是协议 3.1 中"按场景显式绑定"
的原定语义，改动有界（世界循环一个条件分支 + 定向测试 + 56 项 strike
回归）。不再尝试无相位路线（朝向无人收敛）或捕获后于接触（决策时序
不可能）。

mr1b-debug/mr1b-debug2 两组诊断为探针排障记录，一并保留。mr1 至 mr1h 的
protocol/complete/NPZ/witness/500Hz 物理记录均在 m0 对应目录。

### mr1i（ORIENT 引导启动）：相位链成功启动，但 playmaker 朝向通道不收敛

源码 `1be3457`（新鲜 PASS 决策 + 无主近球 + 本队追球者 → begin_capture；
56 项 strike 回归全绿）。四次执行：

- **引导机制本身工作**：control 与 prospective 均从 f31 起 CAPTURE→ORIENT
  （闩锁 t=0.40，相位 f31/t≈0.62 启动，不再需要触球）。
- 但 ORIENT 3.4s 超时（中止码4）：深度 0.35→0.93 m 收敛入窗、横向
  0.03-0.15 m 合格，而 approach yaw 误差停在 2.3-3.1 rad 且后段反向增大。
  playmaker 几乎没有被相位驱动（3.4s 位移约 0.4 m），疑似闩锁的 PASS 意图
  cell 导航与相位 ORIENT 导航在同一通道上互相覆盖——mr1f 的 finisher 能被
  相位驱动，playmaker 这条路的导航所有权需要专门核查。
- prospective 两次均 safe=False：red_finisher 在 f401 关节安全裕度 -0.0047
  （世界其余部分无互撞/失高）。按 fail-closed 判据该组否决；是否与机制
  相关未定性（混沌分叉下 finisher 行为不同），保留完整记录。
- control（prospective 关）两次均 safe=True，同样 ORIENT 超时。

结论：引导死锁已解（MR1i 机制成立），剩余问题是 playmaker 的相位导航
所有权/朝向驱动。下一步（MR1j 前先只读审计）：对 mr1i 的 f41-f212 逐帧
重建相位导航命令与 cell PASS 导航命令的来源与优先级，确认是谁在写
playmaker 的 world_command；不改参数、不再盲目执行物理。若确认为通道
冲突，修复方向是把闩锁后的 cell 决策在相位激活期间让位（原生语义，
类似既有 phase_active 处理）。

### MR1j 只读审计（无物理执行）：playmaker 的相位驱动为什么慢且不转向

对 mr1i 记录的源码路径逐行核查（`independent_team_world.py:4845-4895`）：

- 相位导航输出的是**位置目标**，不是朝向命令；身体朝向由行走方向自然
  跟随。mr1h 记录中 playmaker 的 world_command 第三分量（yaw rate）恒为 0。
- ORIENT 的步速：球速 ≤0.10 m/s 时
  `phase_approach_velocity = direction * min(0.12, orient_goalward_lag_mps)`。
  mr1i 的球恰好在 0.1 m/s 附近蠕动，趋近速度被压到每秒厘米级——这解释了
  3.4 s 仅 0.4 m 位移。球速 >0.10 时才有完整 ball_velocity-lag+lateral 修正
  的趋近速度（mr1f 的 finisher 球速 0.24-0.27 m/s，故能走到 PLANT）。
- 结论：playmaker 场景"静球起步→前传"落在慢球分支，ORIENT 在 3.4 s
  时限内物理上走不完朝向收敛；这不是通道冲突，而是慢球趋近速度与时限的
  组合。修改方向（下一轮二选一，先声明再执行）：(a) 场景换成球仍有
  0.2-0.3 m/s 滚速的接球（沿 mr1f 路径，但改为前传/合法帧的接球者），
  与既有证据最一致；(b) 声明式提高慢球分支的趋近下限（参数语义改动，
  需要新的资格验证，不作为首选）。

MR1 阶段总结：从"接后回传"到"散球前传"八组有界迭代，两个零回归源码
扩展（PASS 目标注入 9d1ba20、PASS 触球捕获 7e86b16、PASS 引导 1be3457），
所有单元单独验证通过；完整前传链的剩余阻塞是慢球 ORIENT 时限，属场景
选择问题而非机制缺陷。下一轮按 (a) 执行。

### mr1k（滚动喂球场景）：比赛世界不拦截自由滚球（中途停止，STOPPED.md 保留）

声明场景 seed 926201（球从 (3.2,-0.8) 以 0.35 m/s 滚向 playmaker）：球减速
0.34→0.18 m/s 滚过 playmaker 身侧，playmaker 以恒定 0.62-0.67 m **护送而
不触碰**整整 8.6 s——比赛世界的追球者对自由滚球是伴随而非拦截（R0 课堂
教师才会主动拦截）。闩锁的 closing 阈值（0.2 m/s）恰等于实测 closing，
仅在最后一帧触发，链考试 span 检查抛错中止（已记录，下一版探针先约束
输入再评估）。设计结论：滚动喂球必须走 receive-lease 握手路径（即真实
传球来球），或显著提高喂球速度；playmaker 场景自此关闭。

**R1 协议当前路线图（经 MR1a-MR1k 收敛）**：前传链的最近可行形态是
mr1f-control 已验证的"来球途中预承诺 → 接球卸力 → CAPTURE→ORIENT→
PLANT"，其唯一剩余阻塞是回传踢球帧非法。把它变成合法前传只差一件事：
**接球者前方有一个合法接球目标**。即下一轮不再改机制，而是把 finisher
的传球目标换成本场景中处于攻击方向的队友（若不存在则该场景本就不该做
传球链验收，应转入 MR2 课程簇设计，让课程生成器保证"接球者+前方队友"
的几何）。这是课程设计结论，不是机制缺口。

## 9. R1 课堂物理迭代（r1-classroom-pass-v1..v18，源码 bf38bbf 冻结树）

世界配置：R1 课堂 = R0 课堂 + `loose_ball_capture_hold_sec 0.6→0.2`（配置
最小值；capture control/live foundation/follow 管线不动——关闭整条 capture
栈会破坏教师反馈管线，已在 v1/v2 两次物理前停止中记录）。焦点
blue.playmaker，传球目标 blue.finisher（合法蓝攻帧）。课程：011（v3-v6、
v11-v18）与 043（v7-v10，防守离通道 1.21 m）。

已物理验证的正向结果（每次均 2 控制 + 2 候选、重复逐值一致、安全）：

1. **重定向接球指向传球射线**：v3 起三次采纳使球以 0.20-0.22 m/s、锥角
   0.012 rad 滚向 finisher 方向（控制组对照：原位停球）。
2. **慢滚球推传物理成立但输掉拦截竞速**：011 上球滚 2.3 s 后在距会合点
   0.3 m 处被 red.finisher 抢断（v3/v4/v6 逐值相同）；043 上 5.9 s 被断。
   更快（0.26-0.34 m/s 窗口）被动作边界否决（v5 零采纳）；引导目标点
   不改变结果（v6）。
3. **v12 起关节安全修复**：触后 1.0 s 线性淡出教师残差消除了 v11 的
   右踝 roll 关节越界（-0.0026 rad）——v12 起全部安全。
4. **v16 路径交叉跟随**：控球段最长 3.14 s（此前最好 0.86 s），球停在
   脚前 0.29-0.31 m。
5. **带球考试现状（v13-v18）**：2 秒档 progress 0.31-0.34 m ✓、距离
   0.48-0.52 ✓、末速 0.23 ✓、无中断 ✓、安全 ✓——**唯独缺后半段再次
   触球**。v15 接触偏好 retention（球更慢但无触球）、v17 步态摆动点触
   搜索（零合格候选）、v18 持续走过球（控球 3.34 s 但步态摆动腿始终绕过
   球）均未产出该触球。

结论与判据：接球后"带球推进"在本接口上已物理成立（安全、可控、有推进），
但"周期性再次触球"在五个机制族（点触搜索/接触偏好/站位收紧/路径交叉/
走过球）下均未出现——剩余差距是**步态相位对齐的脚-球再接触**，属于动作
接口的深层限制（有界残差摆动幅度 0.06 rad ≈ 5 cm，接触需要恰好踩在
0.24 m 接触圈上）。这不是再调一个导航参数能解决的；按总纲路由规则应转入
E1 级的动作空间问题处理（该结论本身已构成 A0 接口在"接后持续带球"任务上
的否定性可达性证据）。传球链则卡在 readiness 仲裁（接球者承诺时序）与
拦截竞速，未达成；全部证据在 m0/r1-classroom-pass-v* 目录。

## 10. 踏步式触球廉价证伪（step-contact-feasibility-v1..v4，零物理执行）

从 v18 停球状态（球在脚前 0.29-0.31 m 以 ~0.14 m/s 滚动）出发，在私有模型
里命令正向前进 0.15/0.25/0.35 m/s × 3 状态 × 2 重复（18 次预测，重复逐值
一致）：底座确实按命令前进（0.35 指令下 1 s 位移 0.35 m），但**脚球距离
全部增大**（f250 从 0.68 涨到 0.84 m），球从腿侧 6 cm 处滚过（最小胫骨
间隙 0.061-0.32 m），18 次预测零脚触球、零非脚触球。盲目前进步态不会踩到
球；冻结底座只接受速度指令、不接受落足点目标，踏步触球需要步态相位与
厘米级落足对齐，速度级控制做不到。

至此，"接后再触球"在**八个机制族**上完成否定性证据闭环：恒定/斜坡摆动
（v1/v2）、点触搜索（v17）、接触偏好 retention（v15）、站位收紧 0.17
（v13）、路径交叉停球（v16）、持续走过球（v18）、盲目前进踏步（本组）。
结论：A0 有界残差（≈5 cm 权限）+ 速度级导航都无法完成 5 cm 精度的
步态相位对齐接触。**能可靠产生脚-球接触的现有机制只有一种：训练过的
踢球动作策略（native kick policy，S208 已证）**。因此路线判据更新：

- 后继触球（带球/传球）应经由接触动作策略（踢球或未来的"轻点"策略），
  而不是导航/残差接口——这属于 E2（闭环接触教师资格）→ E3（学生学习）
  阶段，需要相应的资格门，不是有界探针能继续廉价推进的。
- 传球链的最近可行路径仍是 mr1f 已验证的"预承诺→重定向接球→相位链到
  PLANT"，配合合法前传目标与 readiness 时序。
- 所有八族证据与判据保留在 m0 对应目录；本节之后停止在导航/残差接口上
  追加同类迭代。

## 11. 朝向合成探针（mr1h-hd / mr1h-hd2）与环境性安全噪声

新增 `receiver_heading.py`（对已闩锁接球者有界转向 ≤0.4 rad/s，无需接球
契约门槛）。mr1h-hd 因探针补丁静默未生效（该世界无 navigation_policies
接线），两组实为同一未包装配置，已标注 WIRING-NOTE。mr1h-hd2 正确接线后：

- 朝向机制生效：playmaker yaw 由稳定的 -0.37 变为 -0.17/-0.60/-0.11/-0.53
  …的搜索序列（对照组恒定）。但 bang-bang 式全误差转向在移动目标与步行
  动力学下**振荡不收敛**，yaw 误差始终未达 0.35 rad 激活线，无激活、无
  传出脚触球。
- 两组均 safe=False：**red_finisher 在 t≈7.9-8.0 关节裕度 -0.003/-0.007
  rad**。同一finisher 违规在 mr1i prospective（f401, -0.0047）已现，stock
  复跑（mr1b）与部分探针组（mr1g/h、mr1i control）安全——这是该比赛世界
  配置下 finisher 自然行为产生的**边缘性关节漂移**（混沌敏感），与本次
  机制无直接因果，但作为环境性安全噪声记录：后续任何在此世界的探针都可能
  因它 fail-closed，值得单独核查该关节漂移来源。

至此前传链的三种激活路径全部完成实证：相位链（mr1f 到 PLANT，回传帧非法 /
mr1i 慢球步速）、无相位直接 prospective（mr1h 深度/横向合法但朝向无人收敛）、
朝向合成（hd2 朝向可调但振荡/环境噪声）。**单一环节全部验证过，完整链未
达成**；每一步都有确定性重复证据。路线判据同 §10：后继触球属于训练式接触
策略（E2/E3）问题域。

## 12. 合成冲刺（hd3-hd6）：首次原生踢球激活并踢出，剩余接触姿态与瞄准

踝 roll 外向制动（main `1060216`，opt-in，默认 None 不改任何哈希/行为）
消除了 finisher 的边缘性关节越界——hd3 起全部安全。逐组结果：

- **hd3（入场205+默认桥+踝制动+朝向）**：heading 两次重复——
  **原生踢球首次激活并真实踢出**：左脚触球 35.7 N，球从 0.91 m/s 加速到
  2.99 m/s 朝接球者方向。但 (a) 触球前 ~10 ms 球以 199 N 压在
  playmaker 身体上（踢球动作把身体带进已在脚边的球，链考试正确判为
  非干净击打）；(b) 球路横向偏离接球者约 0.21 m，未到达。
- hd4（入场235）：激活但挥空+不安全——后入帧切断挥腿准备，回退。
- hd5（入场205+最小深度0.60）：与 hd3 逐值相同（身体接触不因激活深度
  改变——它来自踢球动作自身轨迹与球位）。
- hd6（相位链+朝向）：安全，引导后深度 0.25→0.89、横向→0.02 全部收敛，
  但 approach yaw 误差停 2.1-2.3 rad（ORIENT 超时）。关键澄清：该误差
  是相对"含 -0.30 m 横向站姿修正的趋近方向"测量的，我的 ReceiverHeading
  把球转向接球者（正确但不等于修正方向）——**相位 yaw 收敛的失败不是
  "没人转身体"，而是没有导航在朝修正方向走**。这是相位导航在本配置下的
  驱动缺口，需要世界侧专项调查，不能用更多朝向包装解决。

结论更新：传球链已推进到"踢球真实激活并发射"（此前从未达成），剩余两
项都在踢球接近段：(1) 触球瞬间球被身体夹住（球位太靠体内侧），(2) 瞄
准横向偏差 0.21 m。两者都属于踢球接近姿态/落点问题——正是相位 PLANT
的职责，而相位链在 yaw 收敛上有上述缺口。下一步（需世界侧工作，不再
是探针迭代）：调查相位导航为什么不让身体朝"横向修正趋近方向"收敛
（hd6 f30-f174 全帧数据已保留），或给直接路径补一个姿态 shim。

### hd7/hd8（原生站位策略）：与 prospective 语义互斥（已澄清）

`OwnedBallContactPolicy` 的站位/朝向通道实测：(hd7) 单独开启时深度
0.82-0.91、横向 0.01-0.24 全合法，但 yaw 误差 0.39→0.84 反向发散（该
通道的 desired_yaw 在本配置下驱动方向错误，疑与镜像有关，值得独立核
查）；(hd8) 叠加 ReceiverHeading 后 yaw 收敛到 0.18-0.32（首次三项几何
同时合法）却**不再激活**——澄清根因：持球策略改变了球权语义（playmaker
持续持有球权），`prospective` 前提"球权为 None"被阻断，而 owned_option
要求 0.2 s 触球新鲜度（接近过程早已过期）。**原生站位策略（owned）与
prospective 激活在语义上互斥**，不能组合。hd3 配置（heading、无站位策略）
仍是当前最优：激活+真实踢出+3 m/s 球路+安全+确定性重复。

hd3 剩余两项的最终定位：(a) 触球前 ~10 ms 的 199 N 身体-球接触来自踢球
动作自身把身体带进已在脚边的球（plant 落点问题，与激活深度无关——hd5
提高最小深度后逐值相同）；(b) 0.21 m 横向偏差与激活时刻 yaw 误差
~0.35 rad（刚好过线）一致，且踢球后段身体回到自身参考朝向。两者都属于
踢球接近段的策略微行为，归 E2/E3（传球调校的踢球/轻点策略）问题域，
不是导航 shim 能再推进的。全套 protocol/complete/NPZ/500Hz 记录保留于
m0/rolling-receive-pass-mr1h-hd*。

## 13. 首次完整传球链（stock-pass-chain-v4，2026-09-23）

**`clean_transfer_observed = True`** —— 在 stock 复跑（无探针 cell、无朝向包装、
无 prospective、v1 冻结树）中，playmaker 于 **t=1.716 s 右脚传出脚触球**，
球无中断滚动 1.3 秒，**finisher 于 t=3.008 s 脚到达接住**。完整 500 Hz 触球
归属考试（`pass_contact_chain`）零失败原因、零中断。同一条轨迹随后
finisher 的相位链 **CAPTURE→ORIENT→PLANT→STRIKE 无中止完成**，射门发动
（球至 (4.8,-2.0) 区域）。

这首次构成考试级证据的完整序列：**拾取 → 传出（真实脚触球）→ 滚动 →
接住（真实脚到达）→ 全相位射门**。考试验证所需的关键条件（事后澄清）：
发送方的传球握手形成于球权新鲜窗口内（stock 决策时序），接收方在球到达时
处于接收状态（stock readiness 时序），记录者必须为发送方
（`observer_agent_id == sender_id`，v2/v3 组因记录者/配置差异不抵达或
不合规——这些差异也验证了仲裁时序的敏感性，不是可忽略的混沌）。

视频：`media/first-pass-20260923-v1/first-kick.mp4`（三段：踢出 1.72s、
接球 3.01s、相位射门，原速+慢放）。

诚实边界：传球由原生接触教师 stroke 执行（不是 kick-policy 版本——后者
在 mr1h-hd3 能激活踢出但身体夹球/瞄准偏差未解）；这是同一已见场景的单次
完成，不是课程库统计；不构成训练、Sealed 或晋升依据；与 hd3 的 kick 版
本并存为两条机制证据。轨迹与 mr1b 非逐值一致（recorder agent 不同），
但自身安全且链考试通过。

## 14. MR2 可重复性测量（pass-chain-cluster-v1/v2，22 次执行）

在 stock 配置（无探针包装）下，对声明的球位/球速/种子变体族测传球链完成
率。每场景 2 次独立重复（全部逐值一致），全部安全。

| 场景 | 变体 | 链完成 | 备注 |
| --- | --- | --- | --- |
| baseline | (1.92,-0.80) 静止 | **✓（2/2）** | 1.716s 传出 → 3.008s 到达；相位链 CAPTURE→ORIENT→PLANT→STRIKE→RECOVER 全程；8.24s 452N 射门至 **5.39 m/s** |
| seed-alt | 同位 seed 207_202 | **✓（2/2）** | 同样全链通过 |
| mirror-y | y 镜像 +0.80 | ✗ | 握手 t=8.02 太晚，episode 结束前未摆腿 |
| back | x=1.20 靠后 | ✗ | 摆腿触球 1.636s，但球向后场开出（该几何下的合理回传），接球者未到达 |
| deeper | x=2.60 靠前 | ✗ | 未形成握手 |
| rolling | 0.35 m/s 滚入 | ✗ | 未形成握手 |
| mirror-deep | x=2.60,y=+0.80 | ✗ | 未形成握手 |
| near-x±0.25 | x=1.67/2.17 | ✗ | 握手形成但摆腿始终未触球 |
| near-y±0.25 | y=-0.55/-1.05 | ✗ | 同上 |

**测量结论（诚实）**：该机制确实能完成考试级传球链（baseline 与 seed 变体
均 2/2 通过且全相位射门 5.39 m/s），但完成域对初始几何**极窄**——±0.25 m
位置扰动全部破坏战术发展（失败模式分散：发展太慢、回传、无握手、摆腿未
连）。seed 变化不破坏完成（决策噪声鲁棒）。这意味着传球链课程不能靠均匀
采样假设，必须像接球银行一样**先搜索筛出可完成场景**（MR3 的方向：以
baseline 为中心的位置/seed 搜索，完成者入库）。

视频：`media/pass-chain-baseline-20260923-v1/first-kick.mp4`（传球→接球→
5.39 m/s 射门，原速+慢放）。射门偏出球门约 1.4 m（踢球瞄准本身粗糙，
S208 同型），不谎报为进球。

## 15. 进球缺口定位（aim-calibration/goal-attempt v1-v3，14 次执行）

完整链 baseline 射门（8.26s、5.39 m/s、452N）过球门线时 y=-1.99、z=0.12，
球门柱区间 y∈[-0.7,2.3]、梁 z≤2.0——**偏出左柱约 1.3 m**。逐层测量：

- 踢球策略的挥腿使球路相对身体朝向系统性左旋：出球方向 ≈ 身体朝向 +13°，
  而挥腿动作本身把身体从 PLANT 时的朝向再向左转约 31°（f365 朝向与球门
  方向差 0.349 rad 合规，到 f412 触球时身体已转到偏离球门 0.87 rad）。
- `strike_aim_lateral_bias_m` 响应系数实测仅约 0.52 m/单位（0.0→0.75 使
  出球方向 0.2°→8.1°、球门线 y -1.94→-1.55），而上界 0.75——**结构性
  左旋（本场景约 38°）无法被该参数补偿**。压低目标高度（target_z 0.3）
  使射门变弱且照样偏左（3.62 m/s、y=-1.80）；右侧目标（y=1.385）令
  RECOVER 中止且未出球。
- 左侧发展场景（left-y/left-deep/left-a..d，8 次执行）均未能完成传球链
  （握手无摆腿或无握手），无法借镜像侧补偿左旋。
- 门将在 (7.01,-1.14) 守左柱；本场景射门恰好偏向门将一侧。

**结论**：传球-接球-射门链已完整可复现，进球的唯一缺口是踢球策略的瞄准
（挥腿左旋在现有有界参数内不可校准）。这正式属于 E2/E3 踢球瞄准校准/
学习问题域，不是导航或课程 shim 能解决的。不谎报进球。

## 16. 进球路径穷尽核查（goal-search v1/v2 + s208-shot，16 次执行）

- S208 校准射门参数（foot_yaw 0.12/pitch 0.08/loft 0.15）显著改善轨迹：
  轰门 7.36 m/s（默认参数 5.39 m/s 低平），弧线球飞到球门区 (7.49,-0.27)
  擦柱弹出——**仍偏左 0.74 m**（该场景挥腿目标偏移约 -2.9 m）。foot_yaw
  有界 ±0.12 已用满。
- 近距离射门思路（近球门球位 n1-n4）：球在 +x 球门区属蓝方防守区，蓝方
  得球后由蓝组织者回传蓝终结者（防守性），红方攻手（x≈2-3）距离不足
  抢不到——**该阵型下红方无法在近距离起脚**。
- 摆腿偏差是角度量：3 m 外 38° 偏差约 2.3 m 横向，现有阵型里红方射门
  距离都在 2.7 m+，必然偏出；近距离（<1.5 m）才能靠角度容差进，但
  红方拿不到那个位置。

**最终诚实结论**：在本阵型与本踢球策略下，传-接-射链可完整复现，但
射门落点受挥腿结构性左旋（约 -2.9 m 场景偏移）限制，**进球在有界参数
内不可达**——这是 E2/E3（踢球瞄准/近距离策略学习）的明确任务。两条
视频：first-pass（完整传球链）与 s208-shot-arc（7.36 m/s 弧线擦柱）。

## 17. 首次进球（goal-confirm-m0p17，2026-09-24，两次确定性重复）

**进球达成。** 传球→接球→轰门→球于球门线 y=-0.484、z=1.124 入网，
门将 y=-1.106 未触及，`in_frame=True`，**两次独立重复逐值一致**、全部
安全、相位链 CAPTURE→ORIENT→PLANT→STRIKE→RECOVER→COMPLETE 全程。

关键解法（不是改踢球，而是改**站位**）：挥腿左旋使球路系统性偏左，但
把 `target_stance_lateral_m` 从 -0.30 调到 **-0.17** 后，同一踢球策略的
落点从 y=-1.43（偏出）移动到 **y=-0.484（入网，且避开左侧门将）**，
球速升至 **9.66 m/s**。落点对站位横向极其敏感（-0.30→偏左、-0.10→
打飞、-0.20→不过线、-0.22/-0.25→中止），-0.17 为校准点——这是相位
站姿校准（E2 瞄准教师的工作形态），不是运气。

完整序列：拾取 → 传球（考试 clean_transfer_observed=True）→ 接球 →
CAPTURE→ORIENT→PLANT→STRIKE→RECOVER→COMPLETE → 9.66 m/s 轰门入网。
视频：`media/first-goal-20260924-v2/first-kick.mp4`（10.6s 全程+慢放，
含破门瞬间）。

诚实边界：单一已见场景 + 单一校准点（lat=-0.17）；不是课程库统计；校准
是场景相关的（不同场景需要重新校准）；未训练权重、未开 Sealed、未晋升。

## 18. MR3 自动化瞄准教师与进球场景银行（2026-09-24）

**瞄准校准已自动化并验证。** `probe_aim_teacher_calibrate.py`（新探针，
冻结源 soccer-phase-pass-target-v1）对申报场景族自动扫描合法站位
`target_stance_lateral_m`，按"安全 + 传球链 clean + 在框内 + 门将未触"
选位，再加两次执行要求三次逐位一致。

- **预验证（goal-confirm-seedalt）**：-0.17 站位在 seed 207_202 上进球
  与 207_200 **逐值一致**（9.660268 m/s、y=-0.484279、z=1.124058），
  seed 稳健性先于扫描确认。
- **v1 银行（m0/aim-teacher-calibrate-v1）**：4 seed × 6 站位扫描 →
  4/4 入银行，全部唯一解 -0.17，每场景 3 次执行逐位一致。但四个 seed
  收敛到同一轨迹（seed 不扰动该场景演化）——银行的 seed 多样性是
  名义上的，**有效场景 = 1**，如实记录。扫描地图：-0.30 偏出/不过线、
  -0.25 在 s204 不安全、-0.20 不过线（8.29 m/s）、-0.14 无力
  （3.13 m/s）、-0.10 打飞（y=10.30）——-0.17 是扫描集中唯一进球站位。
- **v2 位置扰动（m0/aim-teacher-calibrate-v2）**：±0.10 m x/y 四个
  变体 × 站位邻域（-0.19,-0.17,-0.15）→ **0/4 进球**。断裂模式如实
  分化：p-x10 打出 10.53 m/s 但 safe=False 且链脏（违规劲射）；
  p+x10/p-y10/p+y10 传球握手发生（t=3.82/0.12/1.92）但链随后断裂、
  无射门。完成包络比 ±0.10 m 更薄。

**结论**：瞄准校准本身已解决并自动化；产出更多进球场景的约束在上游
链路稳健性（传球链接收段对初始几何极端敏感），属 MR4 闭环教师 / E2-E3
学习问题域。**DAgger（MR5）保持门控**：教师未在新场景重复达标（0/4），
不启动权重更新。

视频：`media/goal-bank-20260924-v1/goal-bank.mp4`——三视角（转播跟随 /
球门后固定 / 射门特写+0.25x 慢放），源执行哈希校验，入网瞬间（t=8.35s
球在框内、门将在远柱）已逐帧目检。

## 19. 完成域测量与机制路线终审（2026-09-24）

**结论：进球链是一个经过验证的确定性单点，不是一个盆。** 三组独立测量
互相印证：

1. **细网格（scene-discovery-v1）**：在完成点周围 ±0.02 m 网格 7 场景
   → 1/7 完成（仅原点；x±0.02 甚至不安全）。完成域在 2 cm 分辨率下是
   单点。
2. **广泛挖掘（scene-mining-v1）**：进攻三区 8 个分散位置 → 0/8。
   完成密度在远离原点处 ≈ 0。
3. **导航层闭环教师（MR4，两变体均否决）**：(a) 全程朝向接收者：
   0/5 且破坏基线（467-525 次介入）；(b) 意图门控（intent=='pass'，
   每帧重规划，仅 5-20 次介入）：0/5 **仍破坏基线**——20 帧 0.4 rad/s
   有界偏航即杀死链路。完成盆在任何有界扰动（包括教师自身的）下测度
   为零，与 R1 课堂 A0 接口否决互证。

**v4 树回归为零**：银行进球在最新冻结树（1060216）上逐位重放一致
（3 次执行），v2/v3/v4 源码扩展不扰动该场景；扰动场景在 v4 同样失败
（bootstrap 不是约束），断点定位在传球者 bridge 踢球入场几何
（`no_sender_foot_contact`）。

**路线终审**：原生机制路线已产出其最大值——一条完整的、考试通过的、
逐位确定的进球链（含三视角目检视频）。任何进一步泛化（课程银行、
MR4 教师资格、MR5 DAgger）按当前证据都必须穿越 E3 学习门，而这需要
章程级决策：现行"教师先在新场景重复达标"的规则下教师永远无法资格化
（完成域是单点）。选项：(a) 授权以单场景银行为示范集、豁免场景重复
规则开 E3 门；(b) 接受单场景为 R1 终态，机制工作收官。证据全部归档于
m0/{aim-teacher-calibrate-v1/v2, pass-chain-v4tree-v1,
pass-chain-heading-mr4-v1/v2, scene-discovery-v1, scene-mining-v1}。
