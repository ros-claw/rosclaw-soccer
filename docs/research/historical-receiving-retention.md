# 从局部成功回到历史能力保留

此项是 M0/E2 的诊断关，不是训练、完整 128 课考试或盲测。
不能将十个已见局部成功外推为原 17 个历史成功全部保留。

## 冻结规则后的回归检查

预先指定两个原有成功：historical-027（蓝方中场，0.85 m/s，侧偏
+0.08 m）和 historical-072（蓝方后卫，0.75 m/s，侧偏 -0.04 m）。
R0 与当前 confirmed-acquisition 教师各独立重放，总共八次实际执行。

| 课程 | 原 R0 | 新教师替换 | 仅关闭旧接触教师辅助 |
| --- | --- | --- | --- |
| historical-027 | 成功 | 失败 | 失败 |
| historical-072 | 成功 | 成功 | 失败 |

R0 逐物理数组精确复现历史；所有条件均身体安全、重放一致，共同干预前缀
相同。最后一列为随后单独四次执行：只从第 30 帧关闭焦点旧接触教师力矩，
保留原 residual actor。由此可知这两份历史成功依赖**复合控制器**，不能
描述为单独神经 residual 已学会接球。

新教师替换仍然丢失历史能力，因此未扩大部署或解锁学生。Core 对认证的
八次记录返回 `STABILITY_PLASTICITY`，训练／晋升许可均为 false。
不是神经灾难性遗忘：本轮根本没有更新神经权重。

旧教师最后物理子步的力矩除以固定 Kp，再加原 actor 偏移，仅作为瞬时
等效角度诊断。027 最大约 0.114 rad，九个记录点超过当前 0.1 rad 界限；
072 最大约 0.0935 rad。没有将这个统计当成可执行计划，也没有因此提高
幅度或放宽保护。旧辅助在 500 Hz 反馈，新 actor 在 50 Hz 输出并滤波，
两者并不等价；幅度差也未被证明是本例失败的原因。

## 失败不是“没有碰球”

027 新教师在约 1.06 s 已有脚部触球，首个考试窗口结束球速约 0.092 m/s，
仍未达到控球考试。后续预测显示脚球距离会到约 0.411 m，而旧局部搜索
只检查接触、身体安全及球速，未在触球后球慢、没有新碰撞时触发距离修正。
25 个有界关节候选、50 次私有重放预测均未消除该距离问题。

也检查了“相位重规划毁掉已合格方案”的替代解释：原时刻和 +12 帧两方案
在同一状态分别预测 0.5/1 s，共八次。旧短预测精确复现，长预测两者均
可行，+12 帧反而改善末态距离；不据此随意冻结相位。

随后只用当前球和脚的位置、球速，在原导航保护上游做受限慢球跟随：
同一状态的半秒预测，脚球距离由 0.411 m 降至 0.308 m，球速不变。
四次预测含原导航对照及独立重放。这只是可行性信号，下一项实际对照
必须先证明关闭新跟随的封装精确复现旧轨迹，再检查救回与保留。

该实际对照已完成八次：关闭跟随时精确复现旧物理数组；开启后两课都出现
合格接球窗口，但 027 在六秒尾段右踝 roll 关节越界约 0.00265 rad，
全程安全不通过。072 保留成功和安全，全部独立重放一致。**新版本拒绝**，
不以早期接球成功代替整段身体安全。

接着检验“稳定接球后交回原 actor”的退出方案：按当前状态连续 0.5 秒
判定稳定，027 在第 85 帧可触发；四次私有预测中，完全交回原 actor
虽预测身体安全，但一秒后脚球距离约 0.401 m，未通过预声明距离关，
因此没有直接启动该退出方案的实际实验。继续准备残差的同期距离约
0.329 m。需要解决稳定控球与动作退出的共同约束，不是简单提前结束视频。

## 触球后的几何维护：局部修复通过，尚未全局合格

保留原有真实脚部触球证据后，控球维护不应要求未来每段预测再次发生碰撞。
改为同时检查脚球距离、球速、球高、身体安全和禁止接触；幅度、滤波、
关节保护及考试阈值均未改变。慢球跟随的第 105 帧首次预测距离超过
0.35 m；原有 24 个有界候选中，一个右髋 roll 偏移通过短、长预测：
半秒距离从 0.366 m 降至 0.335 m，一秒为 0.272 m。这批仅是 55 次
私有预测，不计作独立课程。

随后两门历史课程各开关几何维护并重复，共八次完整实际执行：关闭时
逐物理数组复现前版，包括 027 的尾段不安全；开启时两课均接球成功、
完整六秒身体安全，重放与共同前缀精确一致。027 一次几何更新修复安全
失败；072 无几何更新且保留成功。总计 4626 次私有预测。

这是两个已见历史场景的局部修复，**不是六秒持续控球保证、完整历史
保留、四接口可达性合格或学习突破**。权重未更新，教师未全局合格，
尚不能解锁 DAgger／PPO。下一关预先固定此前十门局部成功，检查组合
慢球跟随与几何维护会不会破坏其他能力，禁止只汇报救回案例。

纯函数 `training.receiving_retention_geometry.receiving_retention_feasible`
已从研究脚本整理到 Soccer，尚未接成默认控制器。它要求调用方明确给出
此前实测脚部触球时间，拒绝未来事件、非有限数及缺失几何字段；不要求
预测域内反复碰球。相关单元测试 88 项通过；对认证原始提案的 3170 个
保存预测重新判定，筛选结果及两次重复执行中的选择全部一致。该核对
是零新增物理执行，不可增加成功场数，亦不能自行认证触球事件来源。

进一步读取完整实测轨迹的因果阶段诊断：027 最长连续控球段由约 1.38 s
增至 1.70 s，最小关节裕量从 -0.00265 rad 改善到 +0.13147 rad；
但末态脚球距离仍约 0.430 m，不是持续控到结束。072 最长约 1.10 s、
末态距离约 0.740 m，未因几何模块而改变。上述时长由 50 Hz 身体原点
诊断计算，不代替原考试，也不证明接后传射 READY；后继能力仍未知。

## 扩展回归发现退步：不扩大部署

十门旧成功的完整对照批次已完成 40 次实际执行、19832 次私有预测：
保留 9/10、退步 1/10，全部记录安全、重放和原对照逐物理数组一致。
case-11（蓝方中场、0.85 m/s、侧偏 -0.04 m）两次均从成功退化为失败，
因此组合版本拒绝扩大。此例几何修正次数为零；实际慢球导航仅在第 61–64 帧产生
提案。更早的第 45 帧，两路径当前物理及运动记忆完全一致，但因为未来
导航预测不同，教师已选择不同偏移。因此不能只归因于晚期几何修正。

固定十个连续慢速／近球采样的迟滞探针执行了 24 次私有预测（不开新的
实际课程、不扫参数）。它消除了短暂导航，半秒预测恢复旧成功方案的
结果，但预声明的一秒条件未通过：球速约 0.409 m/s。旧无跟随方案固定
同一动作预测一秒也失败；实际旧教师会继续重规划，不能由该结果反推
旧真实成功是假的，也不能声称迟滞控制器已通过。保留原失败判据，尚未
启动该候选的实际试验。

时长核对还发现旧课存在原生提前终止，例如 case-11 旧路径记录 203 帧，
新路径 300 帧。`result.safe` 只证明整段已记录轨迹安全，不保证每例均有
六秒。原协议的“full6s”措辞已通过外置更正附件说明，原回执未重签；前述
027／072 均为 300 帧，不受此更正影响。

认证 40 次原始轨迹并重新计算考试、比较重放后，Core Research Coach
返回 `STABILITY_PLASTICITY`，训练和晋升许可均为 false。没有把两个历史
案例的局部修复覆盖掉这次退步。准备好的全部 17 门历史批次未启动。

## 从恒定偏移转向准备／卸力两段动作

保持同一状态、同一候选幅度及原导航，补充终端脚球相对径向速度：局部
离开余量为 `0.35 - 距离 - 0.5 × max(向外相对速度, 0)`。这是线性预测
诊断，不是屏障函数安全证明，不保证未来加速度或换脚，不能代替物理考试。
24 个候选加重放、两条长确认共 52 次私有预测，十条短预测可行，但优先
两条长预测均失败；不声称所有候选长预测都失败。

随后只改变动作的时间结构：两个已选准备方案先运行五个控制帧（0.1 s），
再接原有 24 种有界关节修正，完整预测一秒。两个恒定对照和 48 个组合
各重放，共 100 次私有预测；恒定对照逐值复现，11/48 组合通过原接触、
安全、球速、距离及新增离开余量筛选。未加大幅度或改动考试。

据此建立当前状态触发的两段教师：仅在首次实测触球前、原教师刚选择了
新偏移而长预测余量不足时搜索；五帧后的第二段先作为提案装入，再接受
当时原反馈重规划，可被新的状态修正，不是不可撤销的电机序列。

两门针对性实际对照完成八次，5186 次私有预测：关闭时精确复现旧结果，
开启时 case-11 救回、historical-027 保留，均完整六秒安全、独立重放一致。
case-11 实际在第 35 帧选中膝部准备、第 40 帧回收偏移，而不是把诊断时
第 45 帧或课程 ID 硬编码进控制器。首次触球约 1.12 s，首个考试窗口末
球速约 0.192 m/s。027 未触发两段修正，36 个物理数组与旧版本逐值相同。

对完整轨迹重新计算因果阶段诊断，case-11 从失败组合版本最长约 0.64 s
连续控球增至 4.70 s，且持续到六秒记录结束；末态脚球距离从约 0.620 m
降至 0.317 m。最小关节裕量仍为正（约 0.117 rad），没有关节越界。
这是与刚被拒绝的组合版本比较，不是与全部 R0 课程的总体提升，也仍未
实测接后传球、带球或射门 READY。

冻结规则后的十一门已见课程回归完成：原十门局部成功及历史 072，
开关配对、独立重复共 **44 次实际执行、29068 次私有预测**。
开启两阶段预测的十一门均保留接球成功及原安全标准；救回一门，新增
退步零门。关闭分支与原证据物理轨迹完全一致，独立重复和配对前缀
也逐数组一致。这不是十一门新样本，也不是盲测；尚未据此解锁学生。
下一门控检查全部十七门历史成功，前锋和门将的七门仍使用原 R0，
不能将它们算作新教师学会的技能。
Soccer 的纯诊断 `receiving_departure_margin` 已整理并通过相关 120 项测试，
对认证保存数据的 130 个余量逐值一致；不将该算术核对算作新增物理实验。

### 工程验证

新增纯函数及相关测试通过；全仓在固定 Core 依赖下为 **5168 passed、
39 skipped、11 failed**，XML 比较确认失败身份集合仍是原有十一项历史
证据拒绝。第一遍漏配固定 Core，六项收集错误已保留；第二遍发现五个
Warp 批量上界测试依赖本机恰好安装获准内核，已用不执行、不分配的测试
替身隔离，并增加源码不匹配仍拒绝的测试。未修改生产内核白名单。
跳过数与此前环境不同，不能拿通过数差值冒充新增功能数或 GPU 验证。

补齐接触时钟舍入与离开余量组件后，再次全仓复测为 **5189 passed、
39 skipped、11 failed**；XML 仍为同一历史失败集合。相关 120 项通过，
全仓 lint、修改模块格式及定向类型检查通过，不能表述为全仓全绿。

## 证据与失败记录

证据路径相对外置 `receiving-mechanism-reboot/`，哈希指内部 manifest。

- `m0/historical-teacher-retention-v2/complete.json`：
  `243e77a6f6384a769bd0bb8c19fcd4b2acd352779bb88a3d19f2f4911d79c0a8`。
- `m0/historical-native-suppression-v1/complete.json`：
  `eca792b84c9fa78971dc8c1659fd8d34b0a0ae5c66dc18c2ba21059c330d93d5`。
- `core-historical-regression-observation-v1.json`：
  `86411489c732c35995920b6f85481ebdd444618f7b6802f13aae4ec4164f21ae`。
- `m0/historical-teacher-authority-analysis-v1.json`。
- `m0/historical-phase-viability-v2/complete.json`。
- `m0/historical-posttouch-geometry-v1/complete.json`。
- `m0/historical-slow-ball-follow-v1/complete.json`。
- `m0/historical-follow-teacher-v1/complete.json`（拒绝：027 六秒安全失败）。
- `m0/settled-handoff-feasibility-v1/complete.json`（拒绝：预测控球距离失败）。
- `m0/follow-retention-stage-audit-v1/complete.json`。
- `m0/historical-geometry-retention-v1/complete.json`：
  `298e839b68f4182678a41ec57db5d88cc62e5652d7838d8c7832b2f046f257a9`。
- `m0/packaged-retention-filter-v1.json`：
  `32049de57092bf45fdbb4faf748b907fbca60ff384c01ba3eb2788dfc08aa269`。
- `m0/historical-control-duration-v1.json`：
  `927000bb6361510396c56702d50f656897fd0a2012ac572e59acd1348e4256e6`。
- `m0/geometry-known-retention-v1/complete.json`：
  `6a4b9cf9a60d23341a3784cd15b68221cf27dfea5c37073d8c9d918bef0cec3b`。
- `core-geometry-regression-observation-v1.json`：
  `f75b46be2cfe98b3d0a8488bfe59a41ba6b9e505fd5fc85513ff32ba49a75754`。
- `m0/follow-hysteresis-audit-v1/complete.json`。
- `m0/receiving-departure-margin-audit-v1/complete.json`。
- `m0/receiving-two-stage-retention-audit-v1/complete.json`。
- `m0/two-stage-retention-teacher-v1/complete.json`：
  `06ac2000865a1723f50bc7c50fc06c36a1cbbe82e3f0d70a51f3c021808fe413`。
- `m0/packaged-departure-margin-v1.json`：
  `1e2d78463410281f6df2f6489ae0dca5dd62ee151b91e941fb1c89edd098ac07`。
- `m0/two-stage-control-duration-v1.json`：
  `497716e8fc38630157464fbc56d138d4c6b97e7c1815b5a746b6937115eb759c`。
- `m0/two-stage-known-retention-v1/complete.json`：
  `4c181b0e6161b7eabe806091418c484219131ec44ababff4d450879dad26bf01`。
- `reboot-regression-0cac569-v3.xml`（最终全仓测试）；前两遍失败仍保留。
- `reboot-regression-departure-margin-v1.xml`（加入离开余量后的全仓复测）。

历史保留 v1 在读认证 API 时停止，零物理执行；相位检查 v1 在首个私有预测
结果序列化时停止，零实际执行。两份失败均保留，另开版本修复，不混入成绩。
