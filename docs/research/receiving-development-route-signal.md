# 公开接球路线门，不是学生或比赛晋升

`training.receiving_route_signal.receiving_development_route_signal` 固化总纲
的历史路线标准：完整旧 128 门，原 17 门成功全保留，候选至少 25 门安全
成功，并且没有任何候选不安全课程。接口要求八个角色实例各十六门，
每门只占一行，独立重放不能作为另一门课凑数。

输入绑定完整执行方案的哈希，而不是只绑定未变化的神经权重。逐门明确
`intervention_applied`，将保持原策略的角色与新教师覆盖区分开；未施加
干预的课程不能声称结果改变。拒绝混合方案、缺课、重复课、非布尔标签、
角色不均衡、错误的 R0 成功数量或未通过重放的记录。

复用既有 `receiving_research_branch` 的成对安全/保留检查；调用方仍负责
认证原轨迹、复算原考试、核对独立重放及冻结银行。函数不认证输入布尔值，
也不会仅凭一个 JSON 哈希就宣称验证了物理。既有函数的“可保留研究候选”
允许持平，而新的路线门不把持平或 17→18 描述成路线突破。

返回 `ROUTE_VALIDATED` 也只限已见开发集；教师资格、训练授权、晋升授权
全部保持 false，未评估 blind，不选择 checkpoint 或自动打开 sealed。
E1/E2、学生、独立训练种子、fresh paired blind 和后继动作仍各有独立门控。

相关 42 项测试通过；全仓复测 5223 passed、39 skipped、11 failed，
仍为同一组历史外置证据失败。全仓 lint 与新增模块定向类型检查通过。
当前实际 128 门评测还在运行；本文件记录实现，不提前填写通过成绩。
# 完整旧 128 课：首个路线门已通过

2026-09-21，冻结的两角色两阶段预测教师完成 256 次实际执行（每门课程
独立执行两次）：旧 128 课 **17→40**，新增安全成功 23，原 17 全部保留，
所有候选课程均通过原生安全判断。干预前前缀和独立重放一致，私有预测
154164 次；它们不是训练样本，也没有更新神经网络权重。

教师实际覆盖后卫/中场的 64 门课；前锋/门将的 64 门仍是原 R0 回退。
不能把另一个全角色迁移候选的结果拼进此成绩。

| 发球初速 m/s | 课程数 | 原 R0 成功 | 当前候选成功 |
| --- | ---: | ---: | ---: |
| 0.75 | 48 | 14 | 25 |
| 0.85 | 16 | 2 | 9 |
| 0.90 | 32 | 0 | 3 |
| 1.00 | 16 | 1 | 3 |
| 1.25 | 16 | 0 | 0 |

独立审计重验全部文件哈希、512 份新旧执行轨迹的配对关系、原考试标签
和重放数组。旧 R0 256 份证据是复用，不是此次又执行了 256 次。全程安全
沿用已认证原生执行器判断，不声称由 50 Hz 轨迹重建全部 500 Hz 安全事件。

结果是 `ROUTE_VALIDATED / SEEN_DEVELOPMENT_ONLY`，不是 Champion、盲测、
总体 E1/E2 资格或训练授权。依总纲进入 Receive→Next Action，不继续只卷
静态接球总分。分层进度里的 `ready` 仅是既有课程计数的课程推进提示，
不是接后技能就绪证据或电机权限。

- 执行：`m0/two-stage-public128-v1/complete.json`，
  `9ad444803690962e89b547198ddfb41968a665ff62f1c3130521b79b905dc84f`。
- 独立审计：`m0/two-stage-public128-route-audit-v1.json`，
  `b4bf992211b5237e3635b0649992d1d9a0076ef7cd345b497cd9f7312214c013`。

## Core 通用化

Core `growth.seen_milestone` 抽象为与机器人和任务无关的完整案例银行、
参考成功身份、执行/评分/银行绑定及无退步门。没有写入 G1、足球角色或
17/25/128 常数。认证后的本次领域报告回放到通用组件，计数、救回、保留、
干预/回退划分逐项相同，仍不授权训练或晋升。这是事后可移植性核对，不
追认成事前由 Core 声明或调度的实验。Core 通过 PR #587 审查，不自行合并；
Soccer 当前门没有新增对未发布 Core API 的强制依赖。

核对证据 `core-seen-milestone-portability-v1.json`：
`a67b65e1271a13dbff1bdf4aaabd6b0b7ee4fb0971174789adfb553a17dbf178`。
