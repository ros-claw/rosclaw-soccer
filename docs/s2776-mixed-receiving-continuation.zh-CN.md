# S2776：完整整队复测与真实旧课复习

## 最终结果（2026-09-20 复核）

实验于 9 月 18 日 20:06 完成，独立审计于 20:08 完成，并未自动开启第三轮。
两轮分别采集 192 堂新训练课程，取得 30,730 和 29,497 个有效角色学习样本；两轮均实际执行八个 epochs。
第一轮完整考试 16/128 成功，零新增、零丢失；第二轮 15/128，新增 `exam-078`、丢失 `exam-015` 和 `exam-027`，回滚到初始 16/128 研究参考。
三次完整考试均 128/128 安全，但两轮探索采样各有一堂安全失败，不能概括成全部训练安全。

独立审计确认 764 堂新课程、1,528 次新物理执行，另复用四堂八次旧执行。审计哈希：
`sha256:8103346a3f03108905a17f9178952ed7d651d12605d6e6c2ed3d9021e8e30bc3`。
新增课程模块可合并，但本阶段没有证明接球能力提升，也没有将实验策略晋升为比赛默认策略。
以下为实施方案与历史阶段记录；其中预定数量现已由上述审计确认。

2026-09-18，SIM_ONLY。接续 S2774 的蓝方中场局部候选，不把四个筛查案例当作整队资格。用户清理出的容量在数据盘，因此新原始轨迹、模型和运行档案统一写入该盘，不继续挤占代码盘。

## 为什么换课程，而不只增加难度

上一阶段两轮困难来球更新分别出现一项新增/两项丢失、零项新增/一项丢失。虽然每角色有 512 个自身状态的 KL 复习约束，较慢球和其他侧向位置的旧成功仍退步。此前新采样主要是 0.85/1.0/1.15 m/s、±0.08 m，不覆盖全部旧几何。

本阶段检验的假设是：在同一父模型下重新采集旧课和困难课，让旧技能也进入真实 on-policy 更新，而不仅依赖历史状态的约束。它是待验证的学习方案，不是“增加样本一定提高成功率”的承诺。

每轮 192 堂：

- 128 堂旧公开考试几何复习，全部更换探索种子并重新执行物理。
- 64 堂困难课：八角色 × 四球速 0.85/1.0/1.15/1.25 m/s × 两侧 ±0.08 m。
- 八角色获得相同几何覆盖；旧动作、均值帧和回放不冒充新 on-policy 样本。

## 先验证局部技能能否放进整队

蓝方中场候选首先执行完整 128 课。之前四堂同模型、同课程、同源树的记录经验证复用，另 124 堂实际新执行，各自独立物理回放。候选全部安全且无成功丢失才能成为研究参考；不是比赛默认模型晋升。

若保留局部候选，后续考试必须对比这个新参考，而不能只对比更弱旧参考、任由刚获得的新技能丢失。复习锚点也从选定参考的真实轨迹重建，参考策略、128 条课程哈希和锚点哈希再次绑定到训练配方。

随后两轮各执行 192 堂新采样、八角色 PPO 更新和 128 堂考试。候选出现安全失败或成功丢失则回到选定参考；下一轮更新预算由八个 epochs 降为四个。每次更新重建原采样记录，两次优化权重完全一致，核验八角色 Core Plasticity。控制器仍是冻结行走基础模型加私有腿部残差，不是全身基础模型重训。

## 可复用课程模块

`training.mixed_receiving_curriculum.mixed_receiving_courses` 从显式旧课程构造平衡混合课程：

- 保留每条旧几何及输入次序，但分配新的 uint32 探索种子。
- 拒绝新旧种子重叠、溢出、重复课程种子、NaN/Inf、错误类型、未知角色和越界来球参数。
- 不只检查各角色课数相同，还要求旧几何多重集合一致，防止某角色被悄悄漏掉某类复习。
- 显式声明这些课程是公开开发训练，不是未见留出考试；不触及默认策略或硬件权限。

该模块在隔离 worktree 开发，运行时主仓库源码保持冻结。两轮各 192 条计划已与启动协议逐字段及哈希完全一致。计划一致性不是新物理执行；需等原实验完成后才合并源码。

## 保存与验证状态

### 已完成的初始整队复测

128 堂完整公开开发课程复测已完成：原参考成功 **15/128**，仅替换蓝方中场的候选成功 **16/128**，128 堂全部安全，新增 `exam-027`，旧成功零丢失。独立审计重新评分并重建局部策略组合，确认其余七角色权重不变。实际新增 124 堂、248 次物理执行；四堂八次旧执行只作复用。

因此该候选被保留为两轮训练的研究参考，并非默认比赛模型晋升，更不是接球能力达标。选定策略哈希为 `sha256:94938167fd91feb0f6721ff64d075ca04c555776db7e042e5a18d20685939cab`。原参考、选定参考和四堂复用原始证据已另外备份到本次 `launch-archive`；备份不计新增实验。

收尾服务 `rosclaw-s2776-independent-audit.service` 等待训练服务正常完成后，自动执行独立全量审计。生产任务失败、结果缺失或超过等待上限时停止，不自动启动无边界训练，也不自动合并源码。两轮训练和完整审计完成之前，不将预定总量写成已实现结果。

任务服务 `rosclaw-s2776-mixed-receiving.service`，有界两轮，不承诺跨重启自动恢复。证据目录名 `s2776-mixed-receiving-continuation-v1`。每批最多预留 32 堂磁盘空间，仍保持 2 GiB 底线；原始失败证据不删除。

`initial-decision.json`、`selected-reference.json`、`g*-training.json`、`g*-decision.json`、`continuation.json` 和 `complete.json` 记录实际阶段；独立审计重新核对原始数据、接球评分、有效角色样本数和父模型关系。预定总量为 764 堂新采集、1,528 次新物理执行，另复用四堂八次旧执行，不能在完成前把预定量当作结果。

课程模块及接续相关测试 47 项通过；全仓 Ruff check、改动格式检查、compileall、新模块 mypy 通过。显式配置 G1 资产的隔离工作区全量测试为 **4523 passed、18 skipped、11 failed**，失败仍为既有历史外部证据校验，没有放宽这些校验。
