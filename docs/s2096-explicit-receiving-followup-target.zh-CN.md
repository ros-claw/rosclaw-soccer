# S2096：接球网络显式感知下一动作目标

SIM_ONLY，迁移验证通过；尚未证明新目标特征被训练利用，也未实现连续比赛。

此前 135 维接球输入包括球相对位置、身体朝向、本体观测和冻结基础策略输出，
不包括下一次传球或射门目标。给奖励函数增加目标方向，并不等于给执行网络
提供目标。本次新增显式 `recurrent_receiver_followup_target_138_float32.v3`：
追加 `(followup_target_position_m - pelvis_position_m) / 5` 三维输入，随后沿用
有限值检查和 [-10,10] 裁剪。目标为实例构造时绑定的有界不可变 tuple，进入
合约哈希；不是当前“去接球”的导航路点。改变目标需新的技能实例与合约。

原 133/135 维接口和默认行为保留；旧接口拒绝传入新目标，防止静默忽略参数。
显式迁移只为 actor/critic 首层追加三个零权重列，其他参数逐元素保留。
采样器和 PPO 支持 138/29 契约；这不是从头训练，也不是政策晋升。
仍使用同一帧唯一冻结基础推理，无 LSTM 重置、导航覆盖、球力或根状态修改。

## 验证

- 34 项相关测试通过：旧接口回归、目标输入与身份区分、迁移保留、非法输入、
  新列有实际梯度等。数值单测允差 1e-6，不冒充普遍位级相等。
- 五个源文件 mypy 和针对性 Ruff 通过。
- 全量 3750 passed、29 skipped、11 项已有外部归档依赖失败，未隐藏失败。
- 八机物理场景分别绑定左/右目标，每种独立运行两次；两组均保持前锋 125 帧
  接球通过、身体安全、控制权无重叠。两组完整旧轨迹列均与 S2088 父策略逐
  元素相同。因为新列权重是零，暂时不应出现左右目标行为分化。
- 两组后续射门激活仍为零，因此只证明接口迁移保留，不证明学会目标条件控制。

证据：`/home/dell/rosclaw_soccer_evidence/s2096-followup-migration-replay-v1/qualification.json`

清单：`sha256:f407c905959f79bdb26cce83df2b418ddbcd89cd0926ae1e01582dbd67b02499`。

下一步先比较探索方差对实际接球成功样本的影响，再用包含新特征的真实动作
样本训练。固定目标的开发案例不能证明任意传球方向或双边泛化。
