# S236–S237：保留旧技能、真实 Core 更新审计与迁移检查

日期：2026-09-09。仅限仿真，完整 4v4 尚未通过，没有发布新宣传视频。

## 已确认的局部进步

S236 的混合参考 PPO 完成 64 次更新、4096 个训练回合，保持一半旧参考回放。随后用独立 CPU MuJoCo 3.12 / 原 ONNX 完成两组各 16 个情境的检查：

| 执行参考 | 身体安全 | 静止球稳定有效触球 | 滚远球稳定有效触球 | 静止/滚远有效方向均速 |
|---|---|---|---|---|
| 旧参考 + 新 actor | 16/16 | 8/8 | 0/8 | 3.326 / 0 m/s |
| 接近参考 + 同一个新 actor | 16/16 | 8/8 | 8/8 | 2.603 / 3.310 m/s |

这里没有把两个专家的最好结果拼成一个虚构策略：第二行确实是**同一个 actor 与同一条接近参考**通过两类球位。旧参考也单独复查，未因接近技能学习而失去静止球通过项。

证据：`s236-mixed-reference-ppo-v1/`、`s236-cpu-mixed-exam-v1/`（均在 `/code/rosclaw/phase8_evidence/`）。候选 `actor-064.npz` 哈希：`sha256:3ef5ec0d3ac63bba84588107eadac5c5176ef07d1a1d5294fdc348f32a75f7b0`。

## 工程闭环这次前置执行了

`s237-core-audited-ppo-v1/` 在实际更新前调用 Core `PlasticityLease`，再执行真实 PPO，更新后用实际张量内容哈希检查：

- 可训练：`g1.ball_residual`（包含 actor、critic、探索方差）。
- 冻结：`g1.sonic_foundation`（编码器、解码器、量化常量，实际张量逐次核对）。
- 每次绑定实际 rollout NPZ、场景契约、身体与源码树；保存更新前优化器/RNG 状态。
- 4 次更新，每次实际 104 个优化器步，4 次 Core 审计全部通过，冻结教师哈希不变。
- 首次更新从实际数据、优化器状态和 CPU/CUDA RNG 重放：**全部权重逐位相同，最大误差 0**；重放起止张量哈希也匹配前置 Core 绑定。见 `replay-strengthened.json`。

这是一个学习者与冻结教师的权重边界，不是已经实现 8 个独立球员同步学习。学习租约也不是 rosclawd 的运动租约或硬件 Permit；没有任何 REAL 权限或自动晋升。

可复用实现 `training/full_body_ppo.py` 将优化器步骤从实验脚本中抽出：检查 rollout 形状、有限性、存活掩码、禁止回合内暗中复活、核对起始 actor 的动作概率和 critic 数值、限制优化器只能拥有当前学习者参数、拒绝非有限损失/梯度。单元测试覆盖原样重放、陈旧/损坏数据及额外参数注入。

## 统一场景后，射门精度仍未达标

`G1VectorMotorBatch` 新增显式 `goal_spec` 和绑定身体/几何的 `world_hash`，保留旧默认场景不变。S237 改为真实物理球门 x=7.5 m、3×2 m，目标 y=0.8 m、z=1.5 m、精度半径 0.1 m，不再拿 x=5 的旧夹具冒充新射门距离。

接触奖励限定在首个真实脚触球后 0.2 s，非地面/非脚正碰撞会关闭归因窗口。独立 CPU 的父策略和新候选均：身体 16/16、局部有效触球 16/16、球心进入物理门框内 8/16、**高目标 0.1 m 精度 0/16**。进门球仍接近地面，候选目标误差约 1.39–2.44 m。

证据：`s237-cpu-goal-exam-v1/`。所以不能把“会稳定踢出滚远的球”包装成“高死角精准射门”。这也是目前不制作新整队突破视频的原因之一。

## 跨起始姿态：没有跳过坏结果

`s237-initial-pose-transfer-v1/` 检查 2 种参考 × 4 种起始身体状态 × 静止/滚远 × 4 个球偏移，共 64 个情境。身体 64/64 保持安全，但接近参考在记录姿态上通过 8/8，在球队默认站姿上仅 3/8；原参考在默认站姿上 0/8。

此实验原始父脚本的汇总 `groups` 字段仍沿用了之前分组，**不适用于此试验**；分析只采用明确标注姿态的逐世界 `transfer` 记录。不能引用旧分组来证明迁移。

接着进行了 `s238-posture-conditioned-reference-screen-v1/`：用球队默认站姿重新调用冻结动作规划器，64 个组合均保持身体安全，但只有 17 个静止球局部候选，滚远球没有通过。动作规划器目前固定沿 +X 给出参考，尚未根据球的横向速度引导接近。下一步需要验证感知驱动的接近方向和姿态条件，而不是继续增加同一姿态的训练轮数。

## 代码检查

带本机 G1 资产的全量测试：**1305 passed、13 skipped、11 个既有历史外部证据绑定失败**（`s237-full-pytest-v1.log`）。新增 PPO 测试 10 passed；未放宽历史证据断言。所有阶段仍保留失败记录与未晋升状态。
