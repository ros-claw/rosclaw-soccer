# S916–S926：恢复训练对照、团队技能接续与斜向射门

截至本记录：仍未达到可宣传的完整自主 4v4 比赛标准。所有实验为 SIM_ONLY；没有硬件执行，也没有自动模型晋升。

## 1. 恢复学习：保留负结果

S916 从 S908 的解析初始化（冻结射门前段、事件后关闭关节残差头，保留学习基础控制器与参考方向头）开始原生 CPU MuJoCo / CPU ONNX PPO。4 次更新，各 32 世界 × 200 帧；5 次评估均为 29/32 速度成功、22/32 精度成功、32/32 安全、0 自身非脚触球。

S917 重算优势值后，新增通用可选学习窗口，详见 `causal-learning-window.zh-CN.md`。S918 使用完全相同的初始化、随机种子、奖励、物理和门限，只对因果恢复窗口做 PPO 优化及优势归一化。初始评估与初始训练的物理轨迹都与 S916 逐字节相同。

| 开发评估代数 | S916 全回合优化恢复成本 | S918 窗口优化恢复成本 |
|---|---:|---:|
| 0 | 0.740199 | 0.740199 |
| 1 | 0.738564 | 0.737764 |
| 2 | 0.740573 | 0.740511 |
| 3 | 0.738961 | 0.744205 |
| 4 | 0.731313 | 0.747646 |

成本越小越好。S918 最终比初始化更差，拒绝替换当前模型；不能把优势值标准差差异直接解释成已经解决的训练瓶颈。两个试验各四次优化器重放参数误差均为零；全部九条轨迹的物理、因果前缀、私有历史、恢复成本审计通过。射门标签保持一致不代表恢复进步。

通用学习窗口功能默认关闭。它验证所有存活样本的概率/价值证据，保留完整 GAE，不改变 alive 或奖励；支持不同任务选择阶段性学习样本，不包含足球目标或机器人驱动。

## 2. 团队控制接续：导航恢复了，球技没有凭空增长

S912 的部分中场/后卫接近球至 1.2 米后，永久退出 SONIC 导航。本次 S919 在同一八人世界测试：连续五帧球距超过 1.6 米、机体直立、具备原有避碰后的导航命令、没有占用中的接球/出球技能时，从当前测量重新建立私有导航状态；最多四次。不复制旧历史、不移动球或机器人、不改角色目标。

| 角色 | S912 学习导航帧 | S919 学习导航帧 |
|---|---:|---:|
| 红方后卫 | 451 | 804 |
| 红方中场 | 433 | 580 |
| 蓝方中场 | 318 | 666 |

第一次重新接续发生在第 524 帧。两场前 525 个控制前状态逐字节相同；三次接续的五帧距离、机体高度/朝向及回执时钟都独立复核。八个身体安全，0 控制器故障，0 机器人间接触。

重要限制：两场整个球的 q/v 完全相同！39 次传球意图、16 次射门意图、18 次扑救意图也没有变化。这些是意图计数，不是成功传射扑救。不能用额外 848 个导航帧或旧 `all_roles_autonomous` 标记声称攻防进步。红方前锋仍整段没有射门意图。

实验回执没有保存完整触发观测，因此 S922 只能独立复核几何和时钟，不能完整重建其中的 observation/cleared-command 哈希。S919 仍是外部实验适配器，不作为已晋升的通用角色调度实现。原 collector 的“永久退出”旧文字由外部实验 wrapper 覆盖，两份不可变源码均保留。

## 3. 斜向射门：把较大失败拆成可验证课程

S921 用既有冻结 actor 扩大参考方向筛查：目标横向 ±1 米，各 16 个方向中心，仍无 0.1 米精度成功；32 身体安全、12 达到球速门限。不能声称已攻克死角。

S923 构建脚本缩进出错，在物理启动前失败，原文件保留。S924 使用新脚本修复，预先声明较小的横向目标 0.5、0.625、0.75、0.875 米和八个参考方向，共 32 个配置。32 达到球速门限且身体安全，11 个同时满足精度门限。它们是配置搜索，不是 32 场比赛或新 PPO 训练。

冻结其中目标 0.75 米、参考中心 0.02 rad 的候选（开发误差 0.017185 米、球速 3.727872 米/秒），再进行 S925 新种子 92502 测试：

- 32/32 球速 ≥3 米/秒且到点误差 ≤0.1 米。
- 平均误差 0.045738 米，最大误差 0.097051 米。
- 32/32 身体安全，0 自身非脚触球。
- 一条原始 S869 入球状态加 31 条局部合成扰动，保留此前九条真实历史；不是独立比赛或不同起跑路线。
- 使用 S908 protected actor；前段继承 S843，后段仍是解析初始化，不冒充本轮 RL 新学到的肌肉记忆。

S925 物理重算通过。下一关是 S926 实际 NumPy 控制接口及原八人场景的防守/门将；不得由隔离精度直接推断能进球。0.75 米是中等偏角，不是 ±1 米、死角或高球目标的达成。

## 4. 证据与视频

主要证据均在 `/code/rosclaw/phase8_evidence/` 下相应 `s916-*` 至 `s926-*` 目录。联合审计：`s922-window-and-team-reentry-audit-v1/result.json`。

S920 内部评审视频：`/code/rosclaw/phase8_evidence/s920-measured-reentry-internal-review-v1/measured-reentry-review.mp4`，1920×1080、50 fps、881 帧、17.62 秒，全部连续原速物理帧。已看接触表：三名角色后续跑动增加，但没有新增有效球路，不能作为宣传成片。未循环拼长、删防守或改物理数据。

全量测试：2410 passed、29 skipped、11 failed；失败项与 S912 完全相同，都是既有外部证据校验。没有删测试、放宽门限或声称全绿。定向 PPO/protected actor 56 项通过，全仓 ruff 通过，定向 format/mypy 通过。仍依赖固定旧 Core 源码；不是当前 Core 的生产集成验证。

系统盘曾满，未清理或停止用户服务；新产物改存 `/code`。截至 S919 的 Python 源码归档：`s566-s919-python-sources-20260912.tar.gz`，SHA256 `7eff17558a311c806cb28c2e04798cc7d5db8b37300da567b1ba82e0f7838f41`。后续脚本需随下一阶段追加新归档，不能覆盖旧证据。
