# S224：把有效触球接力合约用于八角色在线采样与 PPO 续训

## 固定实验协议

- 父 checkpoint：S221 第 48 代；4 次完整的八角色更新，目标第 52 代。
- 每代双方各四角色开球，共 8 个采样回合，12 秒，重复 CPU MuJoCo 仿真。
- 保留冻结步行底座、12 维腿部残差、逐角色 Core PlasticityLease 和速度约束。
- 每段数据必须声明并实际使用 launch_relative_foot_only_v1 合约。
  审计同时核验采样、奖励和验收的任务接力配置；不同合约不能混算。
- 训练球位沿既定课程；最终验收采用双方四角色 ±0.10 m，父子各 16 场，
  均关闭探索噪声。该组球位不参与本轮更新，不能用验收反馈回头选本轮参数。

新增单元测试构造了准备 2 秒、踢出 2.5 秒后接球的轨迹：新计时合约只给
传球者和接球者各 +1，其他六人不获得完成奖励；中间非脚碰球则没有奖励。
浮点数组伪装成布尔合约标记也被拒绝。

视频区分 WORLD SAFE 与 FOOT CONTROL 状态，避免身体没有倒下却小腿碰球
的回合被一个笼统的 SAFE 标注误导。视频不用于验收，也不触发策略晋升。

## 执行状态

实验完成：32 组训练与 32 组验收，共 128 条 CPU MuJoCo 轨迹，均完成严格重放。
12,461 个有效角色样本，32 个 Core 私有策略更新证明经独立复算。八名球员的
actor 和 critic 都有数值更新，但这不是技能提升的证明。

| 指标（父子各 16 场） | 父策略第 48 代 | 候选第 52 代 |
| --- | ---: | ---: |
| 世界稳定性通过 | 16 | 15 |
| 整回合只用脚控球 | 6 | 6 |
| assessment 传球事件 | 6 | 3 |
| 因果诊断确认接球 | 4 | 1 |
| 联合严格合格传球 | 0 | 0 |
| 完整比赛通过 | 0 | 0 |

结论：**候选退步，不晋升，不把第 52 代设为比赛冠军。** 合约修复让延迟奖励
能进入更新，并不保证 PPO 单次或多次更新单调变好。下一轮从固定第 48 代重新
比较更大、覆盖更完整的训练批次，避免将失败续训解释成成功。

另一个验收限制也被保留：旧 assessment 可将触球后战术 tick 才建立的接力
关联到最早接触，诊断则从接力后接触开始计时。例如父策略红方门将负球位，
两者源触球为 1.28 / 1.32 秒。当前联合指标要求同一时间身份，因此会保守拒绝。
不能直接把容差放大或把诊断数改名为合格传球；后续应建立共同的触球事件身份。
这不改变候选稳定性和非脚碰球问题，也不构成放宽验收的理由。

## 完整回合对照视频

`keeper-courses-parent-candidate.mp4`：双方门将负球位的父子对照，共四段完整
回合、约 37.97 秒、1920×1080、30 fps、1×速度。回合出界时按真实终止记录
结束，未补帧伪装完整 12 秒，也未删去失败后的动作。字幕明确区分身体稳定、
非脚碰球、出界；它是问题诊断片，不是宣传成果片。

证据目录：`$ROSCLAW_SOCCER_EVIDENCE/s224-launch-credit-private-ppo-v1/`。
原始轨迹、checkpoint、日志均不进入 Git；未改变 ROSClaw Core 的脏工作区。

合并 S225 后回归：1134 passed、13 skipped、11 failed；失败仍是既有历史
外部证据绑定测试。ruff、mypy（404 文件）、compileall 通过。不宣称全量绿灯。
