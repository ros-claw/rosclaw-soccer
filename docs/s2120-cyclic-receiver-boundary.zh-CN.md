# S2120：接球模块的新约定续接边界

SIM_ONLY。已实现有限技能实例的续接机制，但尚无真实第二次接球成功证据。

新增 `TeamMotorRearmContext` 与 `CyclicRecurrentReceiver`。世界只在显式打开
`cyclic_receive_motors`、接球约定上下文、正常退役机制时检查续接：新约定接受
帧必须晚于旧实例退役帧，属于同一个接收者，原生射门当前空闲，旧实例没有
故障。候选实例必须全新、保持相同内容契约且不属于任何其他球员。

续接重新创建的是有界接球残差实例，不重置物理身体或世界基础运控 LSTM。
新实例仍需观测到真实朝向自身的来球才启动。已消费约定不能重复重启；加载
或验证异常在世界中锁存故障，不会被新约定清除。

单元测试以两段连续基础观察验证两个有界技能，验证旧约定、原生射门忙、
异常完成、重复工厂状态均不能绕过边界。这是合成观察测试，不是两次物理接球。

实际八体世界比较：仅前锋可续接/中场和前锋可续接，各自开启与关闭世界续接，
四组各独立重复一次，共八次。固定 S2118 的接球后导航诊断，未训练新权重。
全部身体安全，初次接球保留；但八次均没有新约定触发续接。
启用与关闭时对应所有原有物理数组逐值一致，不能把无触发对照称为实战循环。

相关测试 32 passed；全量 3782 passed、29 skipped、11 项既有外部归档失败。
新增及变更源文件 mypy、Ruff 通过。

证据：`/data/rosclaw_overflow/s2120-cyclic-receiver-world-v1`。
清单 SHA256：`b740e073c9d1c026ad6d42db5264d2368f4e75d29ade06d2006b9e8a7f585c7f`。
默认世界身份保持不变；没有冠军晋升，没有真实机器人操作。
