# S231：传射控制冲突、模块接续与角色学习（开发中）

## 结论与边界

目前仍没有达到完整 4v4 宣传比赛的效果。局部门将动作选择已经产生学习权重并回到物理世界复验；完整球队的问题主要仍在近球控制、真实传接与后续射门，不能用扑救发球器实验代替球队成绩。

本阶段只在 CPU MuJoCo 共享八 G1、单足球世界中运行。冻结走路策略仍是底层基础，角色决策仍含规则，新增训练不是全身端到端力矩小脑。无硬件操作、模型自动晋升或默认策略替换。视频没有用成功片段掩盖整场失败。

## 做了哪些工程改动

1. 增加只读力矩分解：每个 50 Hz 帧保存最后一个 2 ms 控制子步的原跟踪力矩、触球残差、跟踪调整、保护后实际力矩、任务力及脚踝位置。有效标记区分未调用教师与真实采样，不能把活跃帧数写成传球数。
2. 增加默认关闭的触球腿刚度对照：只降低所选腿六个关节的比例跟踪项，保留其阻尼、对侧支撑腿和下游力矩/关节保护。范围 0.4–1.0，默认 1.0 不改变原控制。
3. 允许显式提前介入关节保护：安全余量仅允许从原 0.04 rad 增至最多 0.10 rad，不降低位置、倾斜或力矩合格阈值。默认配置哈希保留历史身份。
4. 新增 `ContactControlProfile`，将同一实验控制配置传入既有角色课程、PPO 采集与考试。复用已有近球站位、预判接触与严格接球交接，不为训练和考试分别偷偷设置不同环境。
5. 增加实测 50 Hz 关节余量轨迹及 `contact_safety_v1` 奖励：在有限回合势能塑形基础上增加本智能体的关节限位风险惩罚。它不是安全证明，也不代替物理保护。旧奖励模式不变；新模式缺少余量证据就不能计算。

相关实现集中在 Soccer 的 `growth/locomotion_contact_teacher.py`、共享世界控制循环、`training/team_contact_diagnostic.py`、`training/contact_control_profile.py` 和既有私有 PPO/角色课程中。足球实现没有继续塞入 Core；Core 复用的是通用可塑性边界审计。

## 实际诊断与受限对照

证据根：`/code/rosclaw/phase8_evidence`。

`s231-torque-attribution-v1/red` 中 965 个有效采样，约 94.2% 原跟踪力矩在触球残差方向上相反，原跟踪投影中位数约 -17.81 Nm，残差向量范数中位数约 26.57 Nm，保护后投影约 8.11 Nm。反向力矩可能承担平衡职责，不能直接认为全部应该去掉。逐时段检查也显示后半段脚踝目标误差较大。

随后 `s231-stiffness-ablation-v1` 固定其他条件，对红/蓝开球各运行 1.0、0.8、0.6、0.4 四档、每回合 25 秒：

| 刚度比例 | 红开球峰值球速 m/s | 红身体安全 | 蓝开球峰值球速 m/s | 蓝身体安全 |
|---|---:|---|---:|---|
| 1.0 | 0.566 | 否 | 0.319 | 是 |
| 0.8 | 1.391 | 否 | 2.676 | 否 |
| 0.6 | 0.834 | 否 | 0.983 | 否 |
| 0.4 | 1.255 | 否 | 0.832 | 是 |

没有一档在两侧同时通过，不能选最高球速当冠军。部分球运动来自小腿接触，甚至存在足球移动但没有合格脚接触帧的回合。对照是固定开发初态，不是随机成功率估计。

`s231-stiffness-guard-v1` 在 0.8 档上考查 0.08/0.10 rad 提前保护，两侧四次均通过身体检查，但仍未形成合格比赛。0.08 档红/蓝峰值球速为 1.981/0.579 m/s，0.10 档为 0.771/0.098 m/s；保护过早也会妨碍触球。0.08 档的接触回放明确查到中场小腿先碰球，未把它改分类成脚。

上述旧诊断结果用 `asdict(result)` 保存全部原始字段，未包含计算属性 `safe`；初次批处理的打印因此中止，但该次轨迹和结果已经完整落盘，余下候选另行完成。安全结论由原始逐角色字段重建并调用原结果类计算，没有改写历史 JSON。新诊断已改用正式 `to_dict()`，直接包含这些属性。

## 复用既有模块后的完整接续

前一组诊断没有启用已有近球站位策略，默认传射站位离球约 0.72 m，不能作为训练版本的最终组合。

`s231-integrated-contact-v1` 接入既有 `OwnedBallContactPolicy`、预判接触、基础接传角色绑定、严格接球交接，使用新控制配置；红/蓝各运行两次 25 秒精确回放：

- 两侧均精确复现；红侧身体安全，蓝侧前锋出现踝关节限位失败，无机器人之间碰撞。
- 红侧评估器记录一次传球事件，但严格交接诊断发现中间非脚部接触，拒绝认定合格传接。
- 蓝侧出现争球和带球事件，仍没有合格传球。
- 两侧均没有高质量射门和扑救链，不能录成完成的宣传比赛。

这说明解除一个模块瓶颈之后还必须检查接续，不能把单模块参数改进直接写成球队成长。

## 下一轮实际训练

已启动 `s231-contact-profile-ppo-v1`：2 次更新、每回合 15 秒、4 个 CPU 采集进程，红蓝四种角色课程，私有腿部残差 PPO、长时信用分配、严格接球交接及 `contact_safety_v1`。训练/考试复用同一显式控制配置。父模型与候选均要考未用于训练的偏移球位，原失败证据保留。

训练结果待完成后更新本节；启动进程、优化器损失或策略文件存在均不等于已学会。所有候选继续标记不可晋升。

第一批 8 个角色课程已完成精确回放，均身体安全。红中场训练回合同时出现评估器传球事件、接球诊断确认、全回合仅脚控球，成为一个局部传接的正样本；蓝中场虽接球确认，但全回合存在其他非脚接触，不能算同等合格。该批是父策略加探索的采样，不能把它归功于尚未检验的更新权重。已完成第一代八个私有策略更新，等待第二代及父/候选考试。

代码复查日志 `s231-full-pytest-v2.log`：1220 passed / 13 skipped / 11 个相同的既有历史证据绑定失败，已指定可用 G1 资产。全仓 ruff、416 个源文件 mypy 通过。新增回归覆盖触球腿隔离、不放宽保护范围、历史默认配置哈希、训练/考试控制配置一致性及本智能体关节风险奖励；改动相关 38 个测试通过。

### v1 已完成的学习与检验

`learning-audit.json` 从原始记录重算并验证 16 个训练对、16 次 Core 私有更新；训练共 32 次物理运行，父/候选考试另有 64 次物理运行。每个角色实际更新 actor 与 critic，并非只更改角色标签。

| 固定检验 16 回合 | 父策略（零残差） | 第 2 代候选 |
|---|---:|---:|
| 身体安全 | 16 | 16 |
| 全回合仅脚控球 | 7 | 7 |
| 联合合格传接事件 | 0 | 1 |
| 完整比赛通过 | 0 | 0 |

新增合格事件来自红中场 +0.10 m 检验球位：同一脚接触事件身份同时被比赛评估器与严格接球诊断确认，且身体安全、全回合仅脚控球。该偏移未用于该轮优化，但已成为开发检验点；后续若据此调参，不再把它宣传为独立盲测。

`core-replay-after-profile-audit.json` 还从已绑定数据精确重建了第一代 checkpoint，八次 Core 审计通过。新增审计显式检查训练和考试的实际分离距离、关节保护余量与触球腿刚度是否符合声明的控制配置。

这是实际但很有限的传接进步，不能叫整队突破，更不支持宣传视频。已保留第 2 代，继续 `s231-contact-profile-ppo-v2`：从它开始 4 次更新，每次最多 16 个优化 epoch，仍受 PPO KL 提前停止和 Core 步数预算约束。所有训练/考试数据与旧模型保留，没有默认部署。配置解析和重建链已同时支持显式 epoch 上限，避免训练用了新预算而验证仍假定 4 步。

后续代码复查 `s231-full-pytest-v3.log`：1225 passed / 13 skipped / 同样 11 个既有失败；相关 39 个单测、全仓 ruff 与 416 文件 mypy 通过。

### v2 完成：数量增加，但发生遗忘，拒绝直接替换

`s231-contact-profile-ppo-v2/learning-audit.json` 重算验证 32 个训练对、32 次 Core 私有更新。每次最多 16 epoch、共四次更新，父模型为第 2 代，最终候选为第 6 代。`core-replay.json` 保存第一轮优化器的精确重建。

父/候选各 16 个开发检验回合均通过身体检查；联合合格传接从 1 增至 2，完整比赛仍均为 0。问题在于新增的是红后卫 +0.10 m、红中场 -0.10 m，原红中场 +0.10 m 反而丢失。因此不能说持续学习已解决遗忘。

新增独立 `training/role_retention.py` 对已校验学习审计进行逐场比较：相同课程必须保留原来的身体安全、仅脚控球、完整比赛通过及合格传接数量；其他球位增加不能抵消已会球位丢失。它只决定研究继续使用哪个候选，永远不授予部署权限。`retention.json` 已明确拒绝第 6 代直接替换第 2 代。11 个新回归测试覆盖计数掩盖遗忘、证据篡改、重复/缺失课程、非有限输入和不合格正样本。

中间第 3/4/5 代正在 `s232-intermediate-retention-v1` 检查三个已知相关球位。该组是选择用开发筛查，不是盲测；筛查成功也仍需完整配对考试。

同时发现控制覆盖缺口：v2 四个前锋开球考试中，750 帧 × 8 个角色的 `residual_active` 全为 false，足球完全未移动。共享世界的近球学习残差只允许 RECEIVE/INTERCEPT/PASS/CARRY，未包括 SHOOT。前锋在其他课程中更新权重，不能证明其射门动作实际用了学习模型。这是下一阶段要单独打通和检验的实施缺口，不以训练迭代数掩盖。
