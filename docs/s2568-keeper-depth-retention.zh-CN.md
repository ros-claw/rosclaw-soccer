# S2562–S2568：真实 G1 射门下的门将站位与失败保留

2026-09-16，SIM_ONLY。本轮完成 21 个 25 秒课程，每个独立执行两次，
共 42 次 CPU MuJoCo 物理执行；零次优化器更新。不是门将 RL 新训练，
也没有达到自主连续比赛或宣传片验收。

## 对照方法

冻结 S2536 的 `parent-6` 射门模型、接球专家组合、其它球员及物理参数。
射门来自 G1 的实际 29 维动作采样，不使用发球器、不改写球状态。
所有对照均显式启用既有手臂扑救适配器，不改碰撞体或增加外力。
改变的是 S2552 中已说明的观察触发站位：观察传球后，对手近球且球慢下来，
才启用球门角度覆盖。触发条件和课程内锁存机制保持不变。

“防守深度”是相对门线朝场内的目标距离，不是瞬移或机器人初始位置。
原固定深度为 0.48 米，候选为 0.30、0.40、0.60 米。
候选参数、配置、角色身份及实际世界配置均绑定证据摘要。
接球网络所用的内层报告 seed 不代表外层射门采样 seed；射门以
`sampling_seed`、`native_checkpoint_hash` 与 `native-samples.npz` 为准。

## S2562：同一射门，原站位与 0.48 米覆盖

四个公开开发种子 253600–253603，均已在 S2536 使用，不是独立留出集。

| 防守方式 | 四球进门数 | 全部八人通过原安全门的课程数 |
| --- | ---: | ---: |
| 原站位，启用手臂适配器 | 4 | 4 |
| 观察触发后的 0.48 米覆盖 | 0 | 3 |

四个覆盖课程均有实际门将接触，接触前球、球速度、前锋骨盆与关节轨迹
与配对原站位精确一致。不能把改变射门造成的偏出算成扑救。

种子 253602 中门将挡出后倒地，最低骨盆高度约 0.066 米，存在关节越界，
25 秒结束时仍未起身。因此不能声称完成“允许扑倒、随后起身继续”的链路。

## S2564：失败种子与原成功案例一起搜索

每个深度均测种子 253602 和 S2527 确定性原成功案例，各执行两次。

| 深度 | 253602 | 原成功案例 | 全队安全 |
| --- | --- | --- | --- |
| 0.30 米 | 接触后仍进门 | 接触后仍进门 | 两组均通过 |
| 0.40 米 | 挡至边线 | 挡至边线 | 两组均通过 |
| 0.60 米 | 挡至底线外 | 挡至边线 | 两组均通过 |

0.30 米有手部接触，但不等于扑救成功。0.40 米在已知失败种子中最低
骨盆高度约 0.741 米、最大倾角约 0.210 rad，无关节或力矩越界。
这仅解决了一个开发失败案例，需要保留旧球路复验。

## S2566：0.40 米的保留性复验

| 射门种子 | 覆盖后结果 | 全队安全 |
| --- | --- | --- |
| 253600 | 边线出界，无进球 | 通过 |
| 253601 | 底线出界，无进球 | 通过 |
| 253603 | 边线出界，无进球；门将倒地 | 不通过 |
| 256600 | 底线出界，无进球 | 通过 |
| 256601 | 边线出界，无进球 | 通过 |

两个新采样种子额外执行原站位配对，两者原本均进门且全队安全。
这是新动作噪声种子，不是新开球位置、距离、左右脚、球队或场地泛化。

0.40 米修复了 253602，却使原本在 0.48 米安全的 253603 退化。
后者约 14.60 秒骨盆降至 0.55 米以下，最终仍倒地，故不采用为通用替换。
仅从观测看，扑球后仍有位置追踪命令；尚未通过消融证明它是倒地原因。
不能凭时间相关性断言“减速就能解决”。

## S2568：独立二次审计

重新读取全部三份考试清单，核对规范化摘要；逐数组比较 21 组主执行与
独立重放，均精确一致；每条 1250 控制帧、25 秒，数值有限。
从球位置和场地几何重新判定首次出界，而非直接信任总结中的进球标签。
核对八人安全报告、实际 config hash、射门样本摘要、门将决策摘要。
所有有防守接触的覆盖课程均重新比较接触前的配对攻击轨迹，精确一致。
该审计没有新增物理执行或重新训练，也没有将报告的安全统计冒充独立动力学模型。

外部证据根目录为 `/home/dell/rosclaw_soccer_evidence/`：

| 清单 | SHA256（规范化内容） |
| --- | --- |
| `s2562-paired-live-shot-defence-v1/exam.json` | `31cd829728057dc970b7c6e2ff49cc1358baff652b033bb7a3191b8ef0315106` |
| `s2564-keeper-failure-depth-curriculum-v1/exam.json` | `e2aee1c72df51ad55f686f531d5bd3c2b47452bf7bec7c93e15f50b25841bf11` |
| `s2566-keeper-depth-retention-v1/exam.json` | `b66129a5e7e73bab950cf57a0ccee5fabe72e2d8504c8dc4644c8b83a479eee2` |
| `/data/rosclaw_overflow/s2568-keeper-depth-independent-audit.json` | `fb488e9f4e704b7839e650dc7fba1871a8245a220dacc7f943427e7aa0b68388` |

物理所用 main/src 摘要：
`2e30842bac285db484fb67e0aed63df021e839e25a7018a52df7831a27ada7d0`。
公共代码基线提交为 `06c1d62`，实验收集器及其摘要保留在外部证据链。
没有修改持久冠军，没有将本轮候选合入默认运动控制。

## 距离连续比赛的差距

这些仍只有一次传接射门和防守；出界后没有合法重新开球和下一个完整回合。
0.40 米的五次封堵不代表“五场连续比赛”或普遍扑救率。
旧课程中的前锋慢衔接、射门误差、小腿接触等限制仍存在。
现有 S2552 的 25 秒 1080p 视频仅为开发回看，不是合格连续比赛宣传片。

下一步先完成 0.60 米在同组实际射门上的复验，保留所有失败，
再决定是否需要扑后平衡/起身策略及多轮防守生命周期的专项开发。
不靠扩大手套、修改出界规则、隐藏倒地片段或拼接独立开局来通过验收。
