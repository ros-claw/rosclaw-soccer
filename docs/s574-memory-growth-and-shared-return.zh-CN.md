# S574–S587：动作记忆持续学习与真实队友回传诊断

本记录接续 S566–S573。结论是**隔离转传局部改善，真实球队链路仍未通过**。
不交付新的宣传视频，不把开发集高分、动作执行完毕或单次脚触球写成比赛成功。
全部为 CPU MuJoCo 仿真，没有硬件动作，没有四卡训练或端到端关节力矩模型的声明。

## 真正执行了哪些学习

底层循环运动网络冻结，每控制帧只更新一次历史状态；上层策略前50帧产生29关节有界残差，之后160帧使用明确的固定恢复。新增可塑动作记忆冻结经验中心与原始动作，只训练有界修正，同时更新 critic 与探索标准差。实际恢复回报折入最后一个真正执行的策略动作，避免把未执行的恢复期随机动作拿来训练。

| 轮次 | 训练数据和更新 | 实测结果与处理 |
| --- | --- | --- |
| S574 | 57条成功经验起点；16轮×32新扰动；128优化器步，23,771有效前缀样本 | 训练成功73/512；旧开发集23→21/32，最高24；冻结第3代在新宽扰动集7/32，未优于旧记忆 |
| S575 | 只加入上述73条成功训练轨迹，经验库57→130；不加入评估轨迹 | 32邻居旧开发集26/32；宽开发集10/32，旧记忆7/32 |
| S576 | 相邻相位检索消融，允许±2帧 | 没有优于同相位检索，不采用 |
| S578 | 冻结130经验、32邻居，种子57801配对检验 | 窄扰动19/32，对照17/32；宽扰动7/32，对照3/32；均身体安全32/32 |
| S579 | 130经验起点再执行16轮×32真实PPO | 训练成功89/512；旧开发集最高29/32；预选第3代在宽新样本6/32且身体安全31/32，不替换7/32且安全32/32的参考策略 |
| S583 | 实际每个世界不同目标：X偏移±0.15m、Y偏移±0.45m；16轮×32 | 65/512训练回合严格成功；旧固定目标开发集最高29/32、最终27/32；不是不同目标都已学会 |
| S586 | 将89+65条成功训练经验并入130条，得到284条；固定32邻居、新种子58601、实际四倍扰动 | 原目标11/32，对照6/32，双方身体安全31/32；Y+0.4m目标8/32，对照7/32，身体安全32/32对照30/32。仍不足以晋升 |

S574、S579、S583首次更新均从原始采样、恢复归因、优化器状态与随机状态独立复算，所有参数和缓冲区最大误差为0。这证明更新可审计，不证明球技一定提高。

成功经验增长并不等于单调进步。当前小扰动只有毫米级，四倍扰动也只是球位置±2cm、球速度±0.04m/s、关节±0.002rad；还集中在一个身体起点上下文。不能把这样的测试叫作广泛比赛泛化。

## S585配置错误与实际轨迹核验

S585外层脚本替换发生在内层源码生成之前，因此声明的 seed58501、四倍扰动没有实际生效。实际沿用已消费seed56301和小扰动。观察到球XY初始跨度约0.0098m/0.0095m，而非约0.04m。

其原目标32/32、偏移目标13/32不能算新困难集成绩。保留原始文件并添加 `CONFIGURATION-AUDIT.md`，没有静默覆盖证据。S586改在正确的内层源码生成边界注入设置，并对实际轨迹初始球位置跨度执行断言。后续必须核验实际采样，而非只相信结果JSON里的标签。

## 八机器人共享世界：链路断在哪里

### S582：球到点，队友不在点上

将130经验记忆接入蓝方中场，其他角色、物理碰撞、力矩与场地守卫保留。球在7.198秒被中场实际脚触碰并转传；8.782秒到固定目标的最近误差为0.0546米。但前锋已移离约0.4米，随后球被红方后卫身体挡住。

八个身体的各自姿态/关节/力矩检查均安全，不是中场自己倒了；但共享世界仍有145个机器人间接触计数，整体 `safe=false, passed=false`。几何落点精确不等于队友接球，更不等于合法完成传球协议。

原始S580运行期间发生源码变更，收尾源码哈希校验失败，未成为有效绑定证据。冻结源码重跑S582后校验通过，与S580保存轨迹逐项qpos/qvel一致。保留两次结果，明确区分。

### S584：前锋开始争取自由球，但接球后仍丢球

发现原前锋接球模块只接受有接球约定的来球。中场转传后，前锋被战术层选为自由球追逐者，却没有启用接球小脑。实验复用既有前锋接球策略，仅在前一运动释放、无活动射门、被选为追球者、球无主且实际接近、安全身体条件下启用，不伪造传球约定、接球租约或控球权。

7.9秒合法进入该实验接球流程，8.448秒右脚真实接触球，峰值约53.9N；不是手工改变球速度。但接球动作100帧用尽时球已侧向离开，无法衔接原射门门限，50次射门尝试都被姿态/速度门限拒绝。记录中的 `finisher-reception-completed` 仅代表控制时域结束，**不代表接球成功**。

整体仍 `safe=false, passed=false`。不能以射门意图数上升替代射门完成，也不能为拍视频放宽准入门限。

### S587：仅换新记忆，检查真实共享迁移

完成同一八身体环境的配对探针：只将130经验替换为284条训练经验，不添加提前量偏置、不重新调导航。前锋仍在第395帧进入接球，真实脚触球峰值约62.7N；但整体仍未通过，机器人间接触计数187（S584为145），没有证明球队改善，不替换整队冠军。隔离环境成绩上升并没有自动转化为整队效果。

## S588之后：直接学习实际前锋失败上下文

从S584提取前锋第395帧的完整身体、球、底层LSTM、上一动作与反射模式。新建显式配置的训练脚本，不再依赖多层字符串生成来声明新实验参数。

首先使用原始导航命令做物理桥接复现（仅复现使用记录命令，不算闭环学习）：连续100控制帧的qpos最大误差2.05e-6、qvel最大误差1.05e-4，达到预设复现容差。身体安全且脚触球，但严格控球成功为0，与真实链路失败一致。

训练则使用当前来球方向的因果转向与邻居避让，保留的其他球员位置是记录中的外生上下文，不称为多智能体自博弈。S588已开始小探索PPO，S589增加探索作为独立对照；S590尝试时间上连贯的整段动作偏置搜索，明确属于CEM而非神经网络权重更新。后两者仍经过原有残差幅度、变化率、力矩和关节保护。

S588、S589均完成16轮×32训练回合，严格控球成功仍均为0/512，固定开发集也仍0/32。两次首次PPO更新已独立复算，参数最大误差为0，但不能因此声称学习有效。

S590在16轮×32个候选中找到4个严格成功：第13/14/15代各1/1/2个。它是整段潜在动作偏置的CEM搜索，不是PPO权重已学会；早期探索有较多身体失稳，失败样本保留。不能把“每个世界一个不同候选”的搜索命中率当作单一策略成功率。

S591冻结最早最佳候选，新种子59102配对测试：严格控球13/32，旧策略0/32；双方身体安全32/32、脚触球32/32、非脚触球0。S592将这29个学习参数融合到标准神经网络最后一层bias，其他actor/critic参数不变；相比运行时加偏置，实际观测上的最大输出误差5.96e-8。然后在另一个新种子59202上得到16/32，旧策略0/32，双方身体安全32/32。这是黑箱CEM学习神经网络参数，不是PPO成功，也不是完整小脑网络重新训练。

普通权重文件为 `s592-finisher-fused-exam-v1/train/fused-actor.npz`，SHA256为 `a67d5e1367ac50627daeef93eb2f1028eb83954922ff900bcfa40cec9accd191`。运行时不需要额外动作偏置脚本。复查发现该文件还沿用了实验初始化的logstd=-4：它在当前确定性运行中未使用，但相比原S388文件，不能声称整个checkpoint只改变了29个数。S595的导出已改为保留原始logstd，严格检查只有最后一层bias变化。

S593已将S592标准权重接回S584八机器人场景，保持中场130经验记忆、导航和所有原门限不变。**此次真实共享世界的回传控球通过**：8.324秒实际脚触球，此后同一观察窗口的球速最大0.251m/s、最近脚踝距离最大0.318m，身体安全、无球碰身体、球在界内。原S584球速最大0.427m/s、距离最大0.578m，控球未通过。

审计没有用画面或“接球时域结束”打分：1000个真实物理步中，每10步的状态与已绑定哈希的完整世界下一控制帧状态严格一致，qpos/qvel最大误差均为0，再用相同模型正运动学计算脚距离。结果保存在两目录的 `capture-audit.json`。

但是整队仍 `passed=false`，机器人间接触计数145，35次后续射门尝试全部被距离条件拒绝。不能把回传控球的局部通过说成比赛或宣传片通过。

新样本中成功控球后的球相对身体平均仍为世界坐标约(+0.384,-0.335)m，而蓝方攻击方向为负X。接球成功也不代表已经面向对方球门、具备起脚姿态。控球后的转身、推进和射门衔接仍是下一项明确欠缺。

S594进一步让每个候选同时面对4个配对训练上下文，而不是只对单个扰动选优。24批、每批8候选×4上下文，找到4/4满足控球条件的候选；768次搜索回合身体安全全部通过。但S595新种子59502上原策略0/32、S592旧候选15/32、新候选14/32，身体安全均32/32。**不替换旧候选**；再一次说明多轮搜索高分不等于泛化提升。

新增 `training/actor_output_offset.py`：把有界、有限、无梯度的学习偏置导出到完整克隆的actor，原actor、critic、缓冲区和优化器参数不被修改。不引用G1关节名或比赛角色，不给候选授予执行权限；明确要求重新验证浮点融合后的策略。15项测试覆盖原模型不变、独立存储、输出等价、NaN/Inf/尺度/维度/类型/源模型损坏拒绝。

## 进一步的球队级失败归因

S593的机器人间碰撞集中在8.10–8.46秒，双方是红方前锋与蓝方中场，而不是正在控球的蓝方前锋。现有控球确认器在任意机器人碰撞时都会中断待确认窗口；这段碰撞恰好覆盖蓝方前锋首次脚触球，后续没有新的脚触球事件，不能重新获得待确认状态。因此离线严格控球通过，但运行时未持续确认为控球者。不能人为授予控球权来掩盖差异。

S596检验对两名前锋使用相同的近身导航边界：其他身体进入1.4米时，让尚未进入活动踢球/接球的SONIC导航单向让位，继续使用本来就在观测真实运动的循环平衡底座。结果反而变差，碰撞计数269、射门意图0，没有发生后继前锋接球，**拒绝采用**。不能把有历史状态等同于两个运动控制器能安全直接切换。

S597尝试0.6秒连续过渡，但初始化发现红方旧提供器没有可选readiness接口，实验未进入有效仿真。S599保留旧提供器的默认可选接口语义，只在过渡期间显式阻止新运动承诺，碰撞计数降为11，但蓝方中场倒下，整队未通过。另一个S598对照不切换控制器，保留SONIC历史，只让原有避让约束更早生效，机器人间碰撞降为0，但中场右踝俯仰超过关节下界约0.0259rad（最低骨盆0.662m、最大倾斜0.291rad），不能笼统写成它也摔倒了。整队仍未通过。二者都是分析式边界试验，不是新学出的技能，没有放宽原碰撞与安全失败判据。

## S600之后：成功起点的PPO与训练覆盖范围

S600以S592成功候选为起点，实际更新完整actor、critic与探索标准差，16轮×32个训练回合，其中218/512严格成功。开发集起点15/32，最高为第11代24/32，最后第16代为23/32；这次PPO有成功经验作为起点，不再是连续0成功的原地优化。首次更新独立复算误差0。S603按预设规则选择最早最佳的**第11代**，新种子60302配对评估：原策略0/32、CEM起点13/32、PPO候选16/32，均身体安全32/32。实际初始q/v逐字节核验来自声明采样。已有小幅独立改善，但不能直接引用开发分作为泛化结果。

S600全部16次PPO更新已逐次独立复算，模型参数误差均为0；模型与优化器状态连续性、存储rollout的旧logp/value也通过校验。此项证明训练执行可复算，不单独证明物理成绩。

S604连接器错误地预期最后第16代，选择绑定断言拒绝，未执行错误权重的比赛。S606改为读取已验证报告中的整数代号，并继续按候选哈希绑定实际文件。第11代已完成真实共享世界迁移：8.328秒脚触球，观察窗口最大球速0.2702m/s、最近脚踝距离最大0.3113m，严格控球通过；与绑定世界状态的q/v误差均为0。整队仍未通过，不能称为完成射门链路。此处以完整JSON选择记录为准，不根据被截断的终端输出推测最佳代号。

S598/S599中场失稳并非足以证明避让规则无效：这些规则改变来球和身体状态，窄范围动作记忆被用于没见过的入口。标准化入口距离诊断：S593约0.0096；S598约657；S599约347。而训练经验留一最近邻距离最高约0.034。必须先拒绝盲目外推，再收集这些真实新身体上下文继续学习。

新增 `training/entry_support.py` 以训练样本留一距离标定覆盖范围，中心与尺度保存为不可变元组，拒绝非有限、错误维度、过量分配和非法参数；不输出安全概率或运行权限。15项测试通过。它不引用机器人关节名、足球角色或执行器，是在Soccer验证的通用训练候选路由构件，尚未宣称为Core正式能力。

S601验证覆盖检查在熟悉的S593场景上不改变行为：入口距离0.0096小于标定阈值0.116，完整共享qpos/qvel最大误差均为0。S602在S598新场景中拒绝记忆（入口距离657），单次选用原始133维接球网络并保留恢复尾段；没有模型故障、机器人间碰撞为0，但右踝仍超过下界约0.0220rad，整队不通过。覆盖门正确拒绝不熟悉的候选，不等于后备策略已经安全。

S605在同一场景将不受支持入口的任务残差置零，实时循环运动底座不变。八个身体安全、机器人间碰撞0，世界safe=true；但无射门且仍有前锋运动故障，passed=false。这是旧接球残差影响右踝越界的消融证据，不是零残差已经学会足球。

S607提取S602中场实际第318帧的身体、球、循环状态，以旧策略复现100控制帧，q/v最大误差分别4.89e-6和1.57e-4。S608进一步用当前身体状态、当前足位置及原因果导航计算器替换记录命令，误差分别2.17e-6和1.54e-4，仍复现旧策略右踝不安全。邻居和角色上下文来自记录，不称为自主多身体训练。

从这个真实失败入口探索安全转传，而不是扩大旧记忆的覆盖阈值。S609读取实验元数据时键名错误，尚未执行rollout即退出；保留失败目录，S610修正为实际trace字段。目标明确要求自身真实脚触球产生朝目标的速度增量、到达几何目标和全时域身体安全；两秒局部探索不是队友已接住或长时恢复通过。

S610从零初始化的任务输出头搜索29维固定潜在动作偏置，底层实时循环策略不变。16轮×32候选，定向触球开始出现，但两秒内到达目标为0；最佳候选结束时球距目标约0.602m且仍以约1.6m/s滚动。S611增加110帧固定残差衰减/零导航恢复，目标必须在完整4.2秒身体安全下通过，16轮×32候选只有第3轮出现1次严格成功。记录结束后的邻居固定在最后位置，是明确的外生假设，不冒充完整比赛延续。

S612对S610冻结候选做新种子61202的4.2秒配对测试：成功1/32但身体安全仅12/32（存在真实摔倒），不能采用。零残差身体安全32/32但非脚球接触32/32，不会踢球。S613对包含恢复评价的S611候选用新种子61302测试：身体安全32/32，但成功0/32，脚触球7/32、定向触球6/32、非脚接触20/32；同种子零残差身体安全31/32、成功0/32。候选改善部分接触行为，不足以成为可靠传球技能。

S614以S611候选初始化标准actor输出bias，然后实际更新完整actor、critic和探索标准差。100帧后不再执行新策略输出，110帧恢复奖励通过已测试的fixed_recovery_credit折回最后实际策略动作，避免把恢复成败归功于未执行动作。16轮训练完成，开发集起点与最高仅1/32、最终0/32，仍未改善；首次更新复算参数误差0，不能以优化器正常工作冒充球技进步。

S615单世界重新执行S611第3代第6个成功样本，记录2100个物理微步：0.752秒脚部接触，0.754秒满足定向冲量，2.668秒进入0.3m目标范围，身体安全且无非脚接触。成功可重复，但批量与单世界浮点轨迹并不完全一致：q最大差0.0115、v最大差3.548，不能标成严格状态复现。S616改用真实共享入口的无扰动初态，候选安全但未脚触球，出现非脚接触，不具备返回比赛的条件。

S617对同一个S610候选、同一批新种子61702初态，比较固定100帧动作与实际定向脚触球后开始衰减任务残差。严格转传均4/32，身体安全从17/32升至24/32，非脚接触均0。此项是事件式控制终止消融，不是新学习权重；说明执行过久可能损害恢复，但24/32安全仍不可接受。

新增 `training/event_recovery_credit.py`：每个世界可有不同的实际策略结束帧，将之后真实恢复奖励折回各自最后一个已执行动作，填充区的alive、next_alive、观测、动作和奖励均置零，不把未执行样本加入PPO。该构件仅做已验证数据上的折扣运算，调用方必须保证尾段确实不执行新策略输出，不产生执行授权。16项测试覆盖不同事件时刻、与固定尾段逐世界等价、终态不复活、输入不修改、不合法数据和短前缀溢出。世界维向量化，避免每个世界分别运行完整Python回溯。

S618用这一构件完成16轮事件式恢复PPO，保留210帧完整证据和按实际事件折叠的训练证据；新开发/训练种子与S617不同。事件最多100帧，之后不执行新策略样本。开发集4/32到最高5/32；预设按成功、身体安全、较少非脚接触、最早代号选择第11代。S619新种子61902配对测试：旧事件式CEM和PPO候选均6/32成功，身体安全23/32到25/32，**没有证明转传成功率提升**。两者仍不够安全，不晋升。

S614和S618全部16次优化器更新已逐次复算，模型参数误差均0、优化器状态连续；完整210帧奖励到固定/事件前缀的16次折叠也逐项精确。此项仅证明数学更新与归因记录正确，不能代替接触物理或比赛成功。

S620/S621进一步让每个候选同时面对4个入口，按最差成绩加0.1倍均值筛选，不奖励只押中某一个扰动。S621明确包含原比赛无扰动入口，加3个扰动入口；实际保存的每组首个q/v与原始记录逐字节相同。S620搜索已找到4/4通过的候选，但不是新样本成功率；S621原入口仍更难，继续保留未通过记录。准备S622独立扰动测试与S623无扰动入口检查，尚不返回整队或录制宣传片。

### 独立改善、局部精修与实际八身体回测

S622新种子62202，同一32个初态：旧S610候选成功2/32、身体安全25/32；S620为7/32、27/32；S621为16/32、30/32。S623无扰动原入口中，S621安全且定向脚触球，但最近误差0.304844m，仍不满足0.3m门限，没有放宽阈值。

S624在相同四个开发入口上缩小探索范围，找到4/4成功，最佳组的最近误差约0.0107、0.0804、0.1289、0.1496m。S627单世界原入口通过。但S626另一个新种子62602中，旧S610为3/32、身体安全23/32，S621为19/32、28/32，精修S624为16/32、28/32。精修改善已知原入口，不代表总体泛化优于S621；保留两者，不整体替换冠军。

S625将S621普通133维神经网络参数和基于真实定向脚触球的终止机制接回八身体，S628只进一步换成S624参数。二者均八个身体安全、机器人间碰撞0、运动提供器故障0；不是通过移除机器人碰撞或放松关节约束得到。S625中场在7.398秒触发定向终止，S628为7.384秒，均实际执行52帧策略后衰减。保持旧窄范围记忆的入口拒绝，不静默扩大覆盖范围。

**两次旧版世界指标均passed=true，但足球链路未通过。** 该旧指标包含身体安全和意图活跃度，不证明成功射门/扑救。S625前锋8.534秒脚触球，之前8.414秒已球碰自身身体；后续观察窗口最大球速1.524m/s、脚踝距离0.428m，控球失败。S628前锋8.720秒脚触球，无非脚接触，最大球速0.365m/s，但脚踝距离拉开到1.147m，仍失败。两次接球微步与绑定世界状态q/v误差均0。10/16次射门意图不当作实际射门，没有制作宣传视频。

保留原capture-audit.json，并另存 `capture-football-audit.json`，用 `legacy_world_gate_passed` 和 `football_chain_verified=false` 分开表达，避免旧字段whole_match_passed误导。后者不是新比赛通过证据。

S629从泛化较好的S621开始事件式PPO，每个训练批次包含8个原入口副本和24个扰动，开发集为1个原入口加31个扰动。16轮共209/512训练回合成功，其中有明确的重复原入口，不是512个独立泛化样本。原入口从初始未达标变为第1代起均达标；开发最高17/32。按既定规则选第4代，S634新种子63402完全去除原入口过采样后，S621为15/32、PPO为16/32，身体安全均29/32，只是小幅差异。16次模型/优化器更新及事件奖励折叠均独立精确复算，不据此夸大球技提升。

### 前锋继续学习新的真实回传入口

S630针对S625前锋第405帧做隔离复现，后半段接触轨迹漂移，q最大误差0.0281、v最大误差3.740，未通过原复现容差，不训练该入口。S631匹配共享世界的原生double PD、关节投影及循环底座输入后有所改善，但q约0.00512、v约1.198，仍未通过，不放宽容差。

S632改对S628实际第400帧使用同样精确数值路径，q最大误差1.19e-7、v最大误差4.63e-7，通过原门限，复现身体安全/脚触球/无非脚接触但控球失败。原轨迹清楚显示触球后身体从x约1.981移动到1.348，而球从2.388到2.609，不能把低球速误当作球在脚下。

S633在这一已通过复现的入口做有界CEM，找到严格控球样本。S635将学习偏置融合为普通网络权重，用新种子63502进行配对验证：原策略0/32，新候选3/32，身体安全均32/32。成功率仍低，需继续验证和学习，尚未称为球队完成传接射门。

### S636–S644：真正接住回传，但还没有接上传射

S636无扰动入口中，S633仍未达标：最大球速0.525m/s、脚踝距离0.361m，未放宽0.5m/s和0.35m阈值。S637采用每候选“原入口+3个扰动”的稳健搜索。S639新种子63902配对验证中，旧S592、S633、S637分别0/32、2/32、4/32严格控球，身体均安全；S640原入口只有S637通过。4/32仍远不足以宣称泛化成功。

S641将S624中场与S637前锋普通网络权重放回真实八身体共享世界：中场实际定向转传后，前锋8.812秒脚触球，严格观察窗最大球速0.4488m/s、最大脚踝距离0.3219m，通过持续控球；无非脚接触，八身体安全、机器人碰撞0、运动提供器故障0。接球审计与共享世界绑定状态q/v误差0。这是已验证的局部传接改善，**不是完成射门或比赛**：22次高层射门意图对应55次提供器请求，55次均不满足距离与横向条件，14次还不满足前向速度，没有实际射门。保留为当前传接基线，不用宣传片掩盖后续未完成。

发现两个不同问题：第一，停住球的训练终态往往把球留在身体后侧，不能直接启动已有射门参考；第二，首传运动提供器在球已离开后仍等待接球队友脚触球，混淆自身运动完成与对方传球确认。S642只在连续0.15秒实际安全离球证据成立后，有界移除自身残差，不伪造接球确认或所有权。5.632秒开始释放，原150帧任务缩至62帧；八身体安全且无机器人碰撞，但新位置使前锋接球失败，出现非脚接触，不能整体替代S641。

S638用原射门物理入口连续3帧达标作为学习目标，512个搜索回合无成功。S643增加三个有界导航参数，和29个关节残差参数共同搜索，出现几何达标样本，但最佳样本朝向约-0.665rad，而蓝方参考朝向约2.879rad；旧入口谓词没有检查朝向支持，因此**几何达标不能称为参考适配、射门授权或射门完成**。这些是CEM搜索，不是新一轮全网络PPO。S638边界文案曾错误写成事件后恢复3秒，已在原证据目录增加不可覆盖的BOUNDARY-CORRECTION.md：实际是含接近阶段的总计3秒，没有最短事件后恢复保证。

新增训练观察构件 `strike_entry_geometry.py`，精确表达旧版G1+球的镜像物理入口，拒绝非有限/异常尺寸/非归一化根姿态，返回独立只读数组。它明确不检查意图、所有权、运动就绪、参考覆盖、执行许可或成功射门，19项测试通过。该足球特定构件留在Soccer，不假称Core通用产品已完成。

S644分析式提前转身/站位对照完成20秒共享世界：八身体安全、0机器人碰撞，但0射门意图、没有射门。回传后9.04–9.16秒球身距离和横向位置达标时，身体沿攻击方向速度仍为负（约-0.40至-0.18m/s），错过入口；原先2.06–2.18秒的几何达标属于首传之前，不能混作回传后的成功。下一轮单独提前迎球启动，保留原身体、所有权和射门门控。分析式站位不是学习得到的规划器，也没有伪造传球协议。

上述均为CPU MuJoCo、隔离训练最多32世界；不是4-GPU训练。共享世界的旧 `passed=true` 仍仅是旧安全/活跃度检查。尚无新宣传视频，也不以个别搜索样本宣称整队比赛成功。

S645把按来球测量触发的迎球启动窗口从0.55秒提前到1.1秒，原关节/身体/所有权门控不变。回传之后9.36–9.44秒共5个连续控制帧满足旧射门物理入口，前向速度约0.10–0.14m/s；八身体安全、零机器人碰撞和提供器故障。仍0射门意图：原 `anticipatory_contact` 只在自由球速度≤0.5m/s时考虑射门。读源码定位这一决策边界，并没有把几何达标当作射门完成。新增审计核对下一帧pre-control状态与前一帧post-control轨迹逐字节一致；S645有982个绑定帧。初版审计文案写死999，已另存v2加入实际帧数，原报告保留，不能将20秒上限当实际帧数。

新增默认关闭的 `moving_ball_finish_intent` 战术配置：仅在原anticipatory_contact启用、稳定前锋为自由球追球者且距离≤1m时，对高度≤0.3m、平面速度≤1.5m/s的球允许提出射门意图。接球承诺与恢复仍优先，其他角色不扩展，所有物理执行检查不变。配置显式入哈希；关闭时不写入旧序列化，保持默认哈希。它是分析式SIM_ONLY意图候选，不是新学习权重，也不是来球射门策略已获验证。

S646实际共享世界产生11次射门意图，55个射门请求全部仍被旧物理入口拒绝：47次距离不符、6次横向不符、31次速度不足；拒绝原因可重叠。八身体安全、0机器人碰撞、0提供器故障，仍没有实际射门。决策变化影响了共享轨迹，因此不能沿用S645几何达标结论：S646回传后没有完整几何达标帧，位置达标时前向速度只有0.025–0.056m/s。734帧状态绑定精确。保留失败，S647只将已启用迎球阶段的名义前向速度从0.25改为0.4m/s，保持1.1秒提前窗口与原避碰、身体、扭矩和射门检查，作为单参数诊断而非学习突破。

相关四模块82项测试通过；唯一未配置资产的跳过已单独用真实本地G1资产重跑通过，验证八身体模型232执行器与双球门。两个改动模块聚焦mypy通过，改动的五个Python文件格式检查通过。全仓库Ruff检查通过；全仓库格式检查发现69个既有文件需格式化，未进行无关大规模改写，不能写成全仓库格式检查通过。

S647完成后仍无实际射门：10次射门意图、50个请求全部拒绝，其中速度不足降为0次，但41次距离不符、28次横向不符。八身体安全、0机器人碰撞、0提供器故障不等于球技通过。回看实际接触，8.84/8.86秒球先碰蓝方前锋非脚部（采样力约133/167N），8.88/8.90秒才出现脚部接触。当前球位追踪使身体提前移进来球轨迹，后来球在射门距离内却已横向偏离0.30m以上。因此不能只提高奔跑速度、忽略来球碰撞；S648对照改用有界恒速预测的射门平面球位作为横向建议，原物理与意图检查保留。这仍是分析式预测假设，不声称已经学会连续射门。

S648首次在该真实转传链路的9.32秒通过原入口，启动既有神经射门提供器，13.32秒经过原恢复交接返回导航；八身体安全、0机器人碰撞、0提供器故障。质量记录完整2000个2ms微步，但前锋没有脚触球、定向射门峰值0、连续前行不通过。其后11.98秒球接触红方门将，不可宣传成前锋射门后的扑救；质量跟踪器的other-contact包括其他机器人的接触，不能把该字段一概说成前锋碰腿。S648没有先前S647记录到的前锋碰腿事件，但只是接近链路改善，射门仍失败。

检查旧参考库发现参考29有30帧/0.6秒跑动前段。对持续远离脚部的滚动来球，这可能造成执行时机不匹配。S649比较同kick-offset60、heading-1的参考11（前段0.2秒）；S650比较参考2（前段0秒，但该库零前段heading为0，明确不是纯时长单变量）。均是给实时神经跟踪器不同参考输入，不向物理世界复制姿态，不是新PPO训练、也不预先声称旧残差权重已适配参考。后续必须按真实触球、定向速度与恢复判定。

S649仍无脚触球；S650也无脚触球，且全程世界安全失败、机器人间碰撞80次，不能据四秒射门窗口的body_safe字段宣称整段安全。两者均拒绝。不是简单缩短参考就能解决。

S651将S648实际第466帧射门入口变换到原参考坐标系，用相同ONNX、普通133维残差和原生double物理投影隔离复现两秒，q最大误差1.07e-13、v最大误差2.08e-12。S652在同一入口用独立历史、共享只读ONNX会话筛查30个现有参考及2个重复控制：32/32身体安全，但全部没有脚触球；原参考重复仍保持相同精度。50Hz记录姿态的精确碰撞形状距离审计显示，最接近的参考2仍有约0.309m表面间隙。该距离不是微步接触力证据，不将未接触算踢球。

S653显式初始化课程：球身纵向距离0.2/0.4/0.6m、线性与角速度同时乘0/0.5/1、参考2/11/29，共27个课程加5个原入口控制；机器人初态不变，回合内不移动球。所有身体安全，9个回合脚触球，只有距离0.4m的静止球参考2/11满足干净脚触球、向前球速≥2m/s、脚触球关联增量≥0.1m/s。距离0.2m的原速来球参考2已产生1.773m/s和0.310m/s增量，但仍未达2m/s门限。这些较简单初始化不是原比赛成功率改善。

据此增加**单独命名、非旧门控**的短距离移动射门候选域：距离0.18–0.32m、横向≤0.30m，沿攻击方向球速>0.5m/s且三维球速≤1.5m/s，球高0–0.3m，身体朝向指定参考误差≤0.35rad，保持原身体高度/直立/向前速度条件。只给观察结果，不授予所有权、运动许可或策略覆盖结论。该域的来源是课程假设，不是已通过的策略契约。14项新增测试与原19项共33项通过。

S654真实共享回测安全、无机器人碰撞，但65个射门请求全部被新域拒绝；球到新距离时身体仍反向移动。S656只提前迎球启动，70个请求仍全部拒绝，出现新的横向偏移，未将身体安全当球技成功。复查发现规划仍以旧0.59m而新域以0.25m中点为目标；S658把站位、来球时间和横向预测统一到0.25m，继续作为未晋升候选验证。

S655为可训练Torch133维actor建立数值桥：对S653原速近球样本，q误差3.22e-6、v误差1.78e-4，通过原容差。S657启动完整actor/critic/logstd PPO，不是CEM常数偏置：固定参考2，四组每组8世界，分别0.2m原速、0.25m原速、0.4m半速、0.4m静止。初始评价32世界均脚触球且身体安全，分组成功0/0/0/8。批量桥q误差5.83e-6、v误差7.07e-4，通过后才允许优化。每组1个精确课程控制+7个扰动，因此总计4个控制+28个扰动；原结果边界文案误写8/24，已在证据目录另存BOUNDARY-CORRECTION.md，保留原脚本与数据。训练结果未完成前不声称移动球进步，更不声称4v4宣传效果达成。

### S658–S663：受压触球开始接通，仍未达到比赛宣传标准

S658统一到近球站位后没有改善：八身体安全、零机器人碰撞，但55个新域请求全部拒绝，8.80/8.82/9.16/9.20/9.24秒出现前锋非脚接触。检查实际避碰输出发现，对手约0.85–0.99m的逼近把请求的0.4m/s迎球速度限制到约0.05–0.095m/s。因此不能只提高名义速度，更不能删除对手或绕过避碰后宣称比赛成功。

新增默认关闭的 `allow_planted` 观察选项：只在绝对前向速度≤0.05m/s且平面速度≤0.2m/s时额外承认近乎原地入口，其余球位、来球速度、参考朝向、身体条件保持；它明确不是跑动射门，也不是执行许可。34项观察构件测试通过、改动文件Ruff/格式及聚焦mypy通过。第一次测试漏配兼容Core源路径而收集失败；补齐既有依赖路径后重新执行通过，未删除依赖检查。

S660采用这一单独标记的受压原地处理假设，在8.94秒实际启动前锋射门提供器，0.198秒后脚触球，干净定向峰值1.992786m/s；完整四秒质量窗口身体安全，全共享回合八身体安全、零机器人碰撞和提供器故障。但未达强射门要求、未保持连续前行，不能称为跑射成功。9.72秒已有对手脚触球，随后9.74秒发生对手非脚接触；质量中的other不应误称为前锋碰腿。旧世界passed=true仍不是完整足球链路通过。

S657完整8次actor/critic/logstd PPO已结束，最终四课程成功数仍0/0/0/8，与初始相同，没有提高移动球成功率。8次优化器更新均独立重新计算，参数误差全部0、优化器连续性通过；数学正确不等于球技进步。S659另做8代有界29维CEM，每个候选面对4个已知课程，找到个别移动球通过样本并导出普通actor；不同候选的成功不能合计为一个策略成功率。S661已启动冻结原策略、PPO末代、CEM导出的新种子配对测试，包含4个重复精确控制与28个新扰动，静止球保留组单列。

S662直接复现S660未经重置修改的实际第447帧入口，0.6秒窗口包含前锋真实脚触球、截止于后续对手接触之前；q最大误差3.83e-14、v最大误差1.23e-12，通过原容差。它不证明隔离环境能重现之后的多身体交互。S663据此启动实际入口有界残差学习，每个候选面对原入口加3个小扰动，完整四秒身体与非脚接触检查；不再用移动初始球位的课程代替真实入口。仍须冻结独立验证并返回八身体，尚未晋升或录制宣传片。

### S661–S672后续结果：局部学习可复现，宣传验收仍不通过

S661配对初态逐字节一致：原策略和S657 PPO均8/32，全部成功来自静止球保留组；S659冻结导出12/32，分组1/0/3/8。剔除每组重复精确控制后，移动球新扰动成功仅3/21，不能将12/32称为移动球成功率。

S663实际入口4代稳健CEM完成。S664固定取第0代导出的普通actor（SHA256 `47126d9d0a9c42217637c691863638dbbfe341ad950efa38c976dfed059f219b`），没有等训练结束后按测试集挑代。新种子66402、原入口加31个新扰动：原策略1/32，新候选32/32，均安全、无自身非脚接触；原入口由失败转通过，扰动成功1/31变31/31。配对q/v初态完全一致。这里的范围很窄：球XY±0.005m、水平速度±0.01m/s、关节角±0.0005rad。S667另用新种子66702把这三项扰动扩大10倍，原策略13/32，新候选22/32，均安全、无自身非脚接触；仍包含一个重复原入口，不是32个全部独立的新场景。

S665把同一普通网络权重接回原八身体、原对手和原球位；直至第447帧射门入口，全部六个场上球员的pre-control q/v与S660逐字节一致，排除提前改球或改接近路径。实际脚触球提前至动作后0.190秒，干净定向峰值从1.992786提高到2.348711m/s。9.60秒球碰红方后卫非脚部，之后12.72秒球从y=3.1254m出边线，正常终止。八身体安全、零机器人碰撞及提供器故障，但射门质量仅1890/2000微步，四秒窗口未完成，强射门与连续前行均不通过。**不是完成射门/扑救链路，更不符合宣传验收。** 没有为了补满窗口而忽略球出界。

S666将每个世界独立的SONIC推理放入4个CPU线程，所有32世界×4秒的q/v、微步接触、成功、安全和速度指标与原串行版本逐项完全相同，所有误差0，单次rollout耗时45.50秒（不含模型初始化）。没有配对的原串行计时，故不声称精确加速倍率。工程化为 `IndependentSonicCpuBatch`：有界世界/线程数、输入先整体校验、独立状态副本、历史/动作共享拒绝、严格连续帧、提交或推理失败时等待所有已接受任务结束、全批故障锁存，只有显式新回合才重置。S670对该正式组件再跑完整物理对照，所有记录仍逐项相同，43.78秒；19项单测及聚焦mypy/Ruff通过。不是硬件控制器或新的执行授权通道。

S668把学习门槛由2提高至3m/s，在原实际入口加3个已知小扰动上继续8代CEM，初代种子与S663原物理轨迹误差0，但全部代都未产生3m/s合格样本。S669固定实际入口与已验证actor，筛查30个既有动作参考加2个重复控制，只有参考2的3个副本触球，均未达3m/s。S672对参考2做32个速度/相位输入候选，12个触球，也无3m/s成功；未改物理姿态或施加额外球力。两次保留控制均精确复现S664，不能把失败解释为数值桥坏掉，也不能把简单加快参考当作动作已经改善。

S671启动实际入口完整actor/critic/logstd PPO：保留原残差/扭矩/关节限制，提高探索标准差初值（logstd从-3.5到-1.5），每批1个原入口、7个窄扰动、24个宽扰动，3m/s目标与2m/s保留指标分开记录，四秒物理恢复不缩短。初始确定性开发集3m/s成功1/32、2m/s保留28/32；首次随机探索批3m/s成功4/32，所有身体安全，但不同初态/采样不能当作策略提高。使用已通过物理等价验证的CPU批处理组件；截至本段记录仍在训练，不预报成功或晋升。

新批处理组件、既有SONIC向量/Torch/历史交接、射门入口与actor融合六组聚焦测试共109 passed，3.25秒；全仓库Ruff通过。此前全仓库2002/29/11记录仍是此前快照，不将本次聚焦结果拼接成新的全量通过数。

### S673–S679：数据修正接近姿态，再学习更强的一脚处理

S671完整8次PPO最终仍只有1/32达到3m/s，2m/s保留由初始28/32变27/32，不能晋升。所有8次模型/优化器更新独立精确复算，参数误差0；这是训练实现正确但任务未改善的明确反例，不将随机训练批的偶然成功当作模型成长。

S674增加同一正式组件的单线程物理对照，全部轨迹与接触记录仍完全一致，rollout耗时143.14秒；S670四线程43.78秒，本次观测约3.27倍，均不含模型初始化。不是跨硬件吞吐保证，也不是4-GPU训练。

S673读取S667已冻结actor的成功入口，取三个≥3m/s且安全的高速度样本（行3/1/29）的横向相对位置中位数，得到有界站位修正0.036964m，规划足旁偏置从0.25变0.213036m；只改变经避碰与边界约束后的导航建议，不改球或物理姿态。**S667在这一新规划器中转作开发数据，不再是它的独立测试。** 实际八身体中8.90秒启动、0.170秒后脚触球、定向峰值2.767375m/s；四秒2000微步完整，八身体安全、零机器人碰撞。仍非连续跑射，也没有达到3m/s强触球标准，更不代表突破防守或完成射门扑救链路。

S675复现上述实际第445帧、0.4秒含脚触球窗口，q误差6.64e-14、v误差3.75e-12；窗口在后续对手接触前结束，不宣称隔离环境复现之后对抗。S676从该真实新姿态继续29维CEM，4代出现≥3m/s样本，但候选按“最低分+均分奖励”筛选时仍可能用三个入口通过补偿原入口不足。S677改为直接优化最低物理球速加0.05倍均速，并要求所有上下文身体/接触约束通过，不再用分段成功奖励掩盖最弱入口。四个已知入口的最佳最低速度达到3.029273m/s，种子物理轨迹与上一阶段精确一致。

S678固定S677导出（SHA256 `83c62051b93847e2e812c8770df0a27f5747b86f6499410bf8b2bfcb96d44f9f`），新种子67802配对测试：原入口加31个新毫米级扰动，旧策略1/32，新策略22/32，均安全且无自身非脚接触。原入口由失败变通过，新扰动1/31变21/31；配对q/v初态完全一致。新策略原入口沿攻击方向球速3.086117m/s。此处是隔离4秒、≥3m/s及脚触球关联增量≥0.1m/s，不是原世界目标方向速度、目标精度或全部比赛通过。

S679开始把该普通actor接回S673原八身体场景，并增加被动2ms射门数据记录，保留完整接触对象归属，供独立审计向指定目标的速度、脚触球造成的增量及完整恢复。原地一脚处理与旧连续跑射门控明确分开：旧门控要求全程前向速度>0.1m/s，原地策略不能声称通过它；新审计也不授权硬件、不晋升策略或声称进球。尚未以此制作宣传片。

### S679–S682：纠正速度奖励的方向偏差

S679已结束，独立审计被动记录的2000个2ms物理微步，并与200个控制帧的共享世界姿态/速度逐项绑定。入口与S673相同，脚触球在启动后0.172秒，完整四秒恢复、八身体安全、零机器人碰撞或运动提供器故障。沿场地攻击轴峰值3.086945m/s，但朝实际声明目标的峰值只有**2.974246m/s**，因此新原地处理审计仍然**FAIL**。自身脚触球的目标方向速度增量0.647711m/s；0.540秒后的other接触属于红方后卫，不是自身腿碰球。旧连续前行门控也未通过。不能以坐标轴速度超过3替代目标方向通过。

S680将训练奖励改为实际声明目标方向，限制在首次脚触球后的0.2秒归因窗口；同时保留攻击轴速度、正向脚触球增量、四秒身体安全和自身非脚接触约束。四代有界CEM没有找到四个已知入口全通过的候选。最佳效用3.092867是“最低速度+0.05倍均速”，**不是最低速度达到3.09m/s**。导出普通actor SHA256 `d3ac1c935cfd5e130f58158d490f19c3decc3d5fb86ee8be1aed87e1b33dd825`，不晋升。

S681冻结导出的独立配对测试已结束：窄扰动0/32变12/32，原始精确入口仍失败；10倍宽扰动6/32变7/32，两策略均有3个自身非脚接触，不能声称稳健范围扩大。每组含原入口加31个新扰动、同初态对照，全部身体安全，但身体安全不抵消接触质量失败。S682另启动12代较宽、有周期重启的有界搜索，保留原动作/扭矩约束和最弱入口指标，不通过扩大碰撞体、额外球力或改物理初态改善结果。这些均为CPU MuJoCo实验，不是4-GPU训练、完整在线PPO或已完成自主比赛。

增加与机器人型号无关的Soccer测量构件 `sim.target_motion.measure_target_motion`，统一训练与回放的实际目标方向速度、同方向速度增量。批次必须同形、有限、有界，输出独立只读，禁止隐式广播；距离目标不足1cm时显式标记方向未分辨，不把近零向量归一化成虚假证据。它不判断接触归因、球门精度、安全或成功，更不执行动作。31项单测通过，Ruff及聚焦mypy通过。S683在已记录S679全部2000微步上只读复算，速度最大误差4.44e-16、增量误差1.73e-18，并复现原2.974246m/s失败结论。数值桥通过不是新物理回合通过。

S684预先固定S682第3代导出（SHA256 `2f9b91846f4a363fe1c89923303bf5c75ba69a5aaa88c9ae59a71328fda8f9ba`），不等待后续搜索结果按新测试挑代；新的窄/宽配对考试使用上述测量组件，原策略原入口完整四秒q/v复现旧记录误差0。窄扰动0/32变21/32，宽扰动4/32变11/32，原始入口都由失败转通过；窄范围均无自身非脚接触，宽范围两者都有3个自身非脚接触，不能晋升为宽范围稳定技能。S685仅在窄考试和原始入口通过后接回原八身体世界，继续独立2ms审计，未进入宣传流程。

S682完整12代结束，开发上下文的最佳最低目标/攻击轴速度3.221621m/s；末代普通actor SHA256 `c29db3c4380e802851082f49a5c52e3cbf88ccfd38d8a3528f454568f8116298`。不同候选×上下文的32个通过不是一个策略的32个独立测试。S687另以新宽扰动种子比较原策略、预选第3代和末代，尚未据此声称泛化。

复查隔离轨迹发现，速度虽提高，穿过实际声明目标的X平面时横向误差仍在约半米以上。S686因此转为8代落点学习：对实际记录的20ms轨迹检测首次向前穿过该虚拟平面、线性插值横向误差，不外推、不改球；优化最差误差，并惩罚低于3m/s的短缺，保留原安全/非脚接触约束。这个平面在实际球门之前，**不是球门线，不是进球判定**；0.1m误差与速度同时通过另列计数，旧`success`字段仍只表示速度/触球约束，不重新解释为精度成功。尚在训练，未晋升。

朝目标方向的球速仍不等于球门平面精度、进球或突破防守；后续多人回测必须检验这些实际足球结果。未制作或交付宣传片。

### S685–S690：共享触球通过，转向真实落点误差

S685独立2ms审计完成：实际入口与S679逐字段相同，首次脚触球0.168秒，攻击轴峰值3.256509m/s、朝声明目标峰值3.120546m/s、脚触球目标方向速度增量0.697544m/s；完整2000微步、200个控制帧q/v与姿态绑定，八身体和整个回合安全，零机器人碰撞或提供器故障，自身无非脚接触。**单独命名的原地一脚触球审计通过**。旧连续前行仍失败，0.498秒后对手后卫挡球；没有进球/扑救链路或宣传晋升。物理文件SHA256 `f33d7520325eb807e53e4f7a7122003a0c3cc2daba8b7c3e12fdd38bd882b69f`。

S687新宽扰动配对三策略：原策略8/32、第3代13/32、末代16/32，原始入口由失败转通过；全部身体安全，但每个策略仍有2个自身非脚接触。因此末代有该样本集的局部增益，不是宽范围稳定通过，更不是比赛成功率50%。

S686完整8代精度CEM结束，最好的个别样本误差仍大于0.5m，所有代的“≤0.1m且速度/触球合格”计数均0。没有把原`success`中大量3m/s成功改称精度成功。继续单纯微调同一参考的有限残差不足以满足精度要求。

S688筛查32个神经运动参考朝向/渐变组合，实际初始身体和球完全相同，只旋转参考四元数与参考路径，不写物理姿态、不推球。0偏移完整四秒q/v与冻结策略考试精确一致。32个身体安全且无自身非脚接触，14个满足速度/触球要求，但精度全失败；原参考横向误差0.713333m，参考朝向-0.2rad/0.08秒渐变时0.438778m且速度合格，-0.4rad无渐变时0.316924m但速度失败。说明方向控制有帮助，同时存在力度损失，不是已解决精度。

S689继续有界朝向筛查；S690固定已测量-0.4rad参考重新做8代精度/力度残差学习，必须先数值复现S688同入口对照。两者未完成前不宣称结果；也不将独立参考候选的数量当成一个策略的成功率。

### S689–S697：正视精度失败，增加目标条件和延迟反馈学习

S689完成，-0.55rad无渐变参考的虚拟平面横向误差降到0.062132m，但速度/脚触球综合要求失败，不能作为0.1m合格射门。32身体安全、无自身非脚接触，27个触球、5个速度合格（包含4个相同零朝向控制），精度与速度同时合格仍0。

S690在首批物理模拟后因数值桥失败而停止，未输出合格候选：q最大误差0.000448m在容差内，但v误差0.023053超过0.01。重建旧网络加偏置与直接执行已融合float32导出并非逐字节同算，差异在四秒物理继续中扩大。没有放宽容差；S691改为直接加载同一普通导出再学习零中心增量，种子q/v精确一致。S691完成8代，虽然恢复了一部分速度，≤0.1m且速度合格仍全部0。S693联合搜索29维残差与参考朝向，也完成8代而没有精度综合通过，不以更多参数自动等于能力成长。

S692完整133维actor/critic/logstd PPO使用实际2ms虚拟目标平面落点反馈，S694将学习率从3e-5提高到3e-4做配对比较，第一批物理状态、动作、观测、原奖励完全一致。两者均完整8次更新、每次4个epoch上限并保留KL检查；最终速度/精度成功仍0/32，平均横向误差分别从0.303653变0.333520/0.389442m，均拒绝晋升。两条优化器链各8次独立复算，参数误差全部0、优化器连续性通过；实现正确不能掩盖任务失败。

检查S692初始轨迹：触球到目标平面反馈中位间隔1.728秒；按gamma=0.995、lambda=0.98、20ms控制步，该延迟奖励项直接沿GAE递推传回的系数约0.1132，纯gamma系数约0.6485。**这是延迟归因的诊断线索，不是全部梯度或唯一失败原因的证明。** 参考[RUDDER奖励重分配研究](https://arxiv.org/abs/1806.07857)，新增 `delayed_component_credit`：只把显式标记的正/负结果分量按gamma折扣移动到声明的较早有效动作，逐回合验证折扣总回报在数值舍入内不变。与此前固定恢复尾段不同，它不删除或伪造任何踢后有效控制步，身体奖励和故障惩罚留在原位置。18项单测覆盖多事件、正负反馈、活跃边界、拒绝未来索引/重启/非有限输入等。它不推断因果，也不声称实现完整RUDDER或证明最优策略不变。

S696在相同133维模型上将实际落点反馈和终态精度/速度奖金分量归给最近实际脚触球控制帧，保持身体动作活跃和原折扣总回报；与原第一批物理/动作/未重分配奖励配对。初步独立审计6个已完成rollout，实际2ms脚接触归属索引精确一致，折扣总回报最大差9.10e-7；这只是部分会计/接触索引审计，不是训练完成或球技成功。后续审计同时保存读取时的报告快照，避免持续增长的报告被覆盖后无法复核。

新增 `goal_reference_actor`：旧133维关节网络外增加三维目标/参考上下文，输出29维关节残差与一维参考朝向。零初始化的新分支保留旧矩阵计算形状，默认不扰动旧动作；参考朝向只改变下一控制步的神经参考输入，默认[-0.6,-0.2]rad、每步≤0.02rad、平滑系数0.2。它不是直接关节力矩策略，不修改物理姿态，不给予任何执行权。25项单测通过。PPO仅新增显式(136,30)维度合同，原数学不变；新增2项优化器精确重放/过期朝向动作拒绝测试通过。

S695对零修正的新网络做32世界×4秒完整物理复现，q/v、初态和全部接触逐项误差0；这是结构迁移通过，精度仍失败。S697在此基础上开始带目标上下文、可学习参考朝向和显式延迟分量的完整PPO；尚未完成，不声称任意目标、在线比赛自学习、跑射或宣传效果已经实现。

### S696–S700：完整训练审计、连续优化器训练与踢后交接实验

S696和S697均完成8次PPO更新。S696末代平均虚拟平面横向误差0.329172m，S697末代0.281347m（共同初值0.303653m）；二者末代32个身体安全，但速度与精度合格计数均0，不能晋升。两条优化器链各8次逐参数独立重放，误差均0；全部17个评估/训练rollout的延迟分量审计完成，实际脚接触索引和奖励数组一致，折扣总回报最大舍入误差分别9.103e-7和9.101e-7。审计保存读取时的原报告快照，不将持续写入的文件末态冒作早期输入。

S698明确改变即时奖励：不再只奖励正向冲量，改为奖励实际干净脚接触前后球速向“朝目标3.2m/s”的平方误差下降，可正可负；随后只对声明的延迟分量进行保留折扣总回报的移动。**新即时奖励不与旧奖励等价**。第一批非奖励训练数组、q/v、接触和参考朝向与S697逐项一致。8次更新后平均横向误差从0.303653降至0.241617m，但32个样本速度/精度综合成功仍0；目标方向峰值范围约2.21–2.60m/s，未满足3m/s。全部8次优化器重放误差0，17个rollout会计审计完成，最大舍入差9.111e-7。这是局部方向改善，不是合格射门、物理进球或宣传突破。

S700启动32次额外更新：从S698最后一次更新前的安全数值checkpoint重放到末态，精确核对末代actor后保留完整优化器和随机数状态，训练扰动改用新种子；初始完整物理仍须复现父实验末代。开发评估样本继续复用，明确不是独立考试。没有重置探索方差后冒称连续学习，也不以启动训练当成完成。

另一条线S699测试更早的踢后导航交接。S685回放中，前锋在踢后约1.522秒已满足连续0.12秒的低速度、低角速度与直立条件，旧技能仍持有控制至4秒。新实验只根据当下完整接触和身体观测做交接，要求已有实际脚触球、至少0.5秒恢复、身体高度>0.7m、直立分量>0.98、平移速度<0.2m/s、角速度<0.4rad/s；交接前补齐同一身体的教师历史，保留完整4秒被动物理记录和质量观察。它不改物理姿态、不剪掉恢复尾段、不取消对手，也不直接宣称自然或安全。实验仍在运行，须独立复查交接后的稳定性、导航历史和球队效果。

S699完成后的独立审计**不通过**：1.54秒触发交接，八身体安全、零机器人碰撞/电机故障，原触球速度保留；但实际被动物理只有770微步，而非2000。原因是世界层只向持有关节覆盖目标的技能发送物理观察，技能退出后回调停止；上层打算保留记录不等于实际记录完整。报告中的旧质量统计也明确`complete=false`，没有补造缺失微步。

为修复这个生命周期问题，比赛仿真新增显式`persistent_physics_observer_ids`，仅可声明已注册且实现只读观察接口的运动选项；观察集合独立于关节覆盖目标，默认空集合维持旧行为。活跃且持续订阅只送一次，退出后仍送观察，故障继续锁定并使结果不通过，不增加任何运动目标或写权限；声明绑定到结果哈希，未知/重复/未绑定订阅拒绝。S701将重跑相同提前交接，仅启用这个被动物理通道，再检查完整尾段。

### S701–S706：恢复闭环通过，但争球与精度仍未闭环

S701完成：持续观测保留2000个2ms样本、200个控制帧完整q/v绑定；原地单触球独立审计通过，仍是原攻击轴3.256509m/s、实际目标方向3.120546m/s，旧连续前行门不通过。1.54秒交接后的身体和整个回合安全，零运动故障/机器人碰撞。S702复核：交接前770微步q/v与S685完全相同；新增观测通道前后S699/S701全回合数值轨迹和motor-state q/v完全一致，仅非物理的传球准备回执JSON因合同哈希不同而变化。不是向运动目标集合添加一个假目标来凑观测。

同一1.54–4秒窗口，S685前锋骨盆路径长0.050969m，S701为0.910272m；后者最低高度0.720529m、最低直立分量0.988717，保持安全。角速度RMS从0.044087升为1.293077rad/s，包含重新走动/转向，不能当成“抽搐减少”或“自然度改善”的证据。更早移动不等于更有效争球：S701球仍在19.42秒从边线出界，后续没有再满足旧/新射门入口。内部六帧图位于 `/home/dell/rosclaw_soccer_evidence/s704-recovery-visual-audit-v1/`，仅用于检查姿态，未作为成功宣传视频交付。

检查后续失败，发现球员在无人控球时仍可能按传球目标绕行并追逐当前位置。新增独立`world/ground_intercept.py`提供短时恒速截球几何：默认0.8秒预测、0.7m/s根部速度、0.28m接触区域，球必须比根部更慢，明确区分“时域内几何可达”与“只给有界前瞻”。它不执行、不判定脚触球、不确立控球，不是已训练的运动模型。19项单测覆盖最早可达解、旋转/平移等变性、区域内不要求重定位和非有限/越域输入拒绝，聚焦mypy/Ruff通过。

S703只在首次实测恢复交接后用该几何提议尝试自由球获取，完整20秒安全且没有出界，但未晋升：前锋在踢后2.826秒出现自身非脚接触，完整原地触球审计因而拒绝。还发现旧`prospective_contact`参数是全局启用标志，并不代表“当前这个球员获选争球”；本轮1851条提议覆盖5个场上角色，不能叫“一名获选球员”的对照。S706进一步限制每队一名几何近端导航拦截者、0.15m切换余量并覆盖静止球；它是显式的下游导航对照，不授予正式控球、传球或射门权限，也不是已经学会的球队策略。仍需实测。

S700续训的中间数据表现为方向误差下降但球速亦下降，未到综合成功。S705从相同S698末态和优化器启动16次额外更新对照：增加有实际脚触球时的终态速度缺口惩罚`-20*max(0,3-min(目标方向峰值,攻击轴峰值))`，该负分量也按实际脚触球时刻折扣归属；无触球原惩罚保留。它明确改变奖励，不是与S700奖励等价，更不是形式化约束MDP或拉格朗日最优性保证。训练仍在进行，不把中间结果或奖励上涨当成精准射门。

## 工程加固与尚未采用的方向

`local_action_memory` 增加读取阶段修正值、归一化尺度有限性检查，避免Inf被tanh饱和隐藏，或尺度损坏产生表面正常输出。新增Inf/NaN用例。

`rendezvous_target` 是有边界的恒速接应预测基线，只给目标建议，不执行、不授予动作权限。回看S582，用过去0.2秒速度预测实际前锋位置误差0.506m，反而大于静态目标的0.442m；没有激活为冠军。运动员的意图与后续导航变化不能可靠地由瞬时速度外推，后续应绑定接应意图、空间目标与时间窗口。

训练机制仍在Soccer孵化，不冒称已成为Core正式通用能力。当前优先补齐：实际自由球获取后的可控姿态、接球到下一运动的条件转换、接应目标时空承诺，再训练多身体竞争。不是继续堆同一个固定目标的PPO轮数。

## 验证与本地证据

S701持续物理观察通道加入后，全仓库为 **2127 passed、29 skipped、11 failed**（50.55秒）；11项仍为下述同名旧外部证据问题，JUnit `/home/dell/rosclaw_soccer_evidence/s701-repository-tests.xml`。观察/接触/世界合同聚焦为71 passed、1个未配置资产跳过；包含退出后观察不授予动作、活跃与持续订阅去重、故障锁定、未绑定订阅拒绝和结果哈希绑定。世界模块聚焦mypy通过，全仓库Ruff通过。尚不将S701仿真启动当成完整尾段验证完成。

最新全仓库（基于d2b6ba6，包含目标/参考网络、延迟分量和新增PPO合同的工作树）：**2112 passed、29 skipped、11 failed**，51.25秒；11项仍为已在原始27c5b72基线复现的同名旧外部证据绑定失败，没有删除或绕过。此前1950、1981、2002、2036和2067通过的记录保留于s597/s627/s646/s680/s684-repository-tests.xml，最新JUnit为 `/home/dell/rosclaw_soccer_evidence/s695-repository-tests.xml`。29项跳过包括外部资产/旧视频环境和5项显式GPU检查，不冒称全部集成路径已运行。配置资产后额外执行的八身体模型测试通过不重复加入此全量计数。

新模块使用 `mypy --follow-imports=silent` 聚焦检查通过。事件恢复构件加入后的最新六模块聚焦测试为83 passed，全仓库Ruff通过。直接运行默认导入跟踪会报告64个依赖文件中的403项错误，不能写成全仓库mypy通过；`--follow-imports=skip` 则因跳过Torch类型产生Module为Any的错误，也不作为通过记录。测试通过不替代球技验收。

主要本地目录位于 `/home/dell/rosclaw_soccer_evidence/` 下：

- `s574-plastic-memory-ppo-v1/train`
- `s575-expanded-memory-exam-v1/train`
- `s578-expanded-memory-fresh-v1/train`
- `s579-expanded-memory-ppo-v1/train`
- `s583-variable-goal-memory-ppo-v1/train`
- `s585-variable-memory-exam-v1`（含配置错误审计）
- `s586-variable-memory-wide-v1/train`
- `s526-public-frame-receiver-runtime-v1/s582-memory-return-probe-frozen`
- `s526-public-frame-receiver-runtime-v1/s584-loose-return-acquisition`
- `s526-public-frame-receiver-runtime-v1/s587-variable-memory-shared`

脚本在 `/code/rosclaw/phase8_evidence/s532-delayed-handoff-cpu-v1/`，大型轨迹、权重和视频不入Git。

截至S621构建脚本的不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s621-experiment-sources-20260912.tar.gz`，SHA256 `1d52312e1018f58bafcecd31515638103291ca89dfb48f3026a6ab416a25cea5`。此压缩包不包含后来创建的S622/S623脚本，也不含原始训练数据/轨迹；不覆盖较早S566–S596副本。

截至S647构建脚本的新不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s647-experiment-sources-20260912.tar.gz`，SHA256 `dd08c05bd2c28747021f25652afbaf5228f255db0e3b1c6cd219b256b7bc3e26`。包含该外部脚本目录的新实验与审计构建代码，不含原始轨迹/权重，也不意味着归档中每个实验都已完成或通过。

截至S672构建脚本的不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s672-experiment-sources-20260912.tar.gz`，SHA256 `d1f8801b36c597199b39109e826bcca2a343a25eef030e80bbb8fe0323b2955b`。排除Python字节码、轨迹与权重；包含尚在训练的S671构建代码，不将归档当成完成或通过证据。

截至S686构建脚本的不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s686-experiment-sources-20260912.tar.gz`，SHA256 `c17550af863a9f8b2c032c45484bf600c83322fe1d688ea13d99e7858bbdea05`。不含后来创建的S687脚本；归档时S685/S686尚未完成，不将脚本保存当成实验通过。

截至S690构建脚本的不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s690-experiment-sources-20260912.tar.gz`，SHA256 `a1a9e2d4fbd980581f858ea2a4a034f07c13dc54840250a13bcff956f917b6a5`。包含尚在运行的S689/S690构建代码，不含原始轨迹和权重；不覆盖旧归档。

截至S697构建脚本的不可变副本：`/home/dell/rosclaw_soccer_evidence/s566-s697-experiment-sources-20260912.tar.gz`，SHA256 `1be2908b89daa38fcafa4cc9f639856be261eac883c43265973030e0e8e83d12`。归档时S696/S697仍在训练，不含后来S698–S700脚本，也不代表训练通过。

截至S701构建与审计脚本副本：`/home/dell/rosclaw_soccer_evidence/s566-s701-experiment-sources-20260912.tar.gz`，SHA256 `b52ab01896c1aed1e657392ed9962d3e4ad9d16d0d3df81decfc78df3e82574a`。包含S700优化器续训与其32次更新审计脚本；归档时S700/S701仍在运行，不代表训练或完整比赛已通过。
