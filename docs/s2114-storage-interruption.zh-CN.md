# S2114：首轮候选拒绝，磁盘中断与断点核验

2026-09-15。SIM_ONLY；连续 4v4 比赛宣传片仍未达标。没有候选晋升。

## 实际完成

沿用 S2110 的局部三维动作基和 S2112 初始潜变量策略；冻结接球主网络、
基础运控及其他球员，只更新前锋的局部协同 actor/critic。

- 父模型四个已知开发场景均身体安全，干净接球 1/4，前锋后续原生射门均为零。
- 首轮使用 500 个控制样本，完成 16 次优化器步骤。
- 首轮候选开发检查身体安全，但失去干净接球，故拒绝；接受代仍为 0。
- 第二批物理采集中遇到 `OSError: [Errno 28] No space left on device`。
  四轮计划没有完成，不能报告 64 次实际更新或最终候选成绩。

## 保存与恢复验证

原始中断目录保留在外部 evidence root 的
`s2114-motor-synergy-gated-ppo-v1`，没有覆盖或删除损坏文件。
原 `progress.json` SHA256：
`1a7853af81c9d6540817311f1ab72d139be9feb0c92b538e7d7b98c24aca0d17`。

恢复目录为 `/data/rosclaw_overflow/s2114-motor-synergy-recovery-v1`，
恢复程序为同级 `resume_s2114.py`，日志为 `s2114-recovery.log`。
恢复程序核验已完成报告、文件哈希及物理轨迹摘要，并从原批次重新执行首轮
优化：全部权重逐值相同，ROSClaw Core 更新证明及优化统计完全相同。
原始不完整采集没有复用，而是重新运行。

恢复后发现备用盘可用空间从约 56 GB 快速降至约 23 GB；当时本任务恢复目录
仅约 116 MB，不能靠继续转移输出解决整机存储压力。已对本任务父进程发出
中断，并终止其两个物理采集子进程；确认三个进程均退出。未触碰其他任务。
恢复进程退出码 130，不是成功完成标记。

恢复 `progress.json` SHA256：
`590de3a283f34d8b9516e0a1548a7195e18ffea5b17b5f2c7e15507485fef571`。
其中只有首轮更新，第二轮未执行优化；不能把零散采集算作已完成训练轮次。
完整的第二批个别报告仍保留；无完整报告的目录须按未验证处理。

## 续跑要求

1. 先获得稳定、足够的持久存储空间，并检查根分区不再写满。
2. 核验上述断点、冻结模型及源码身份，保留所有失败档案。
3. 使用新恢复目录；当前恢复脚本故意使用排他 `mkdir`，原样再次执行会拒绝
   覆盖已有目录。续跑须显式设计断点读取，不能直接删除目录重来。
4. 从已接受的 generation-000 继续，而不是采用被拒绝的 generation-001。
5. 完成剩余采集、优化逐值回放、物理奖励重建和候选复评后，才讨论技能改善。

这次新增的是局部学习路径、退化拒绝和可核验恢复，不是足球能力突破。
后续仍必须解决接球出口、重复技能循环和完整球队协作；内部单次接球视频
不替代用户要求的连续比赛宣传片。
