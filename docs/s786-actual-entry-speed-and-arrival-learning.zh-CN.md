# S786–S804：实际入场学习与接球路径闭环

## 边界

本阶段仍未达到完整 4v4 宣传片标准。没有发布新宣传视频，没有晋升候选，也没有放宽 3 m/s、0.1 m、身体安全及非脚接触验收。以下“精度”仍指既有虚拟 XY 目标平面，并非高球死角或实际进球。

## 实际入场的 PPO 学习，及原生物理复核

S786 使用 S763 实际走到的身体/球状态，中心 -0.36 rad、偏移 ±0.1 rad 的明确参考配置，128 世界、32 次 actor-critic PPO 更新。动力学仍是已声明不满足严格 CPU 等价性的近似 GPU 训练域。

| 验证 | 速度合格 | 精度合格 | 平均虚拟平面误差 |
| --- | --- | --- | --- |
| S786 训练域初始评估 | 93/128 | 0/128 | 0.71405 m |
| S786 最终评估 | 128/128 | 0/128 | 0.64634 m |
| S793，原生 CPU 对照父模型 | 19/32 | 0/32 | 0.70947 m |
| S797，原生 CPU 新模型 | 32/32 | 0/32 | 0.64438 m |

S793/S797 使用完全相同的初始状态：1 个实际入场状态及 31 个新扰动。两个模型的身体安全均为 32/32、非脚接触均为 0。S798 独立核对显示，19 个原有速度合格样本全部保留，新增 13 个，31 个样本误差下降、1 个上升。

这是实际入场域的一项有限改进，**不是精度成功，也不是完整比赛成功**。

S786 的 32 次优化器精确重放、65 条微步物理重建、65 条延迟奖励检查通过；最大奖励回报重建差约 9.16e-7。另用独立 NumPy 公式核对全部 32 条训练回合、各 200 帧的动作滤波、参考朝向、观测历史与记录，最大差 2.98e-8。运行接口及测试见 [冻结小脑参考契约](s773-frozen-goal-motor-runtime.zh-CN.md)。

此前 S782 增大探索的 32 次更新最终仍是速度/精度均 0/128，平均误差 0.59102 m；优化器、微步与奖励审计均通过。它没有被挑选为成功策略。

## 对失败候选的反证

S789 在旧实际入场状态下优化候选偏置，16×128 次开发试验的最佳误差约 0.43261 m、速度约 3.002 m/s。导出普通数值模型后，S792 原生 CPU 新扰动只得到 9/32 速度合格，对照 S793 为 19/32。

虽然全部 32 例误差下降，平均误差从 0.70947 降至 0.45370 m，但丢失 14 个旧速度合格例、只新增 4 个。S795 成对审计通过，**候选不晋升**。不能用更小的误差掩盖速度鲁棒性损失。

S789 的搜索及 16 条微步审计通过。第一个搜索审计包装器的 schema 名称错误，在重放前拒绝，原文件和拒绝记录保留；更正包装器后才得到通过结论。

## 回到共享世界：避免小腿挡球，而非移动仿真身体

S791 保留 S763 的等待位置与向前追球，来球越过身体的场地 X 平面后，才逐渐加入从合格课程统计出的横向走位修正。仍通过原步行策略、原 0.3 m/s 横向上限、避碰和原力矩约束执行；没有修改身体/球状态。

S802 复核：

- 全部 6 个场上球员及球的预控制 q/qvel，在第 0–436 帧与 S763 逐值一致，第 437 帧才开始变化。
- 本次每控制帧接触账本没有前锋的非脚碰球记录；此前 S770/S778 的右小腿碰球没有在该账本重现。这不是独立的碰撞几何重算证书。
- 8 台身体安全，无电机故障，但仍没有启动已准入的射门。
- 第 446–452 帧，其它射门几何条件、SHOOT 意图和争球者提名均满足，前进速度却约为 0.059–0.086 m/s，处于原“站稳 ≤0.05”与“跑动 >0.1”两个已定义域之间。

没有因此直接扩大准入域。先用这些真实状态继续做独立物理试验，避免把更宽的 if 条件当作技能成长。

## 时机、参考相位与节奏的对照

| 实验 | 设计 | 结果 |
| --- | --- | --- |
| S794 | 实际帧 446–453 × 相位偏移 0/2/4/6 | 32 例均未同时达到速度/精度；提前参考相位常降低触球速度 |
| S796 | 实际帧 450–453 × 8 种参考中心 | 仍无合格；有约 2.6 cm 误差但仅约 2.50 m/s 的例子 |
| S799 | 实际帧 450/451 × 4 个中心 × 1/1.15/1.3/1.5 倍参考节奏 | 未合格，过快参考常错过球；不是视频加速 |
| S800 | S799 完全相同的状态/参考配置，换成已训练 S786 模型 | 仍未合格，说明速度技能不能直接跨入场状态视为通用成功 |

所有试验均通过原生 CPU MuJoCo 和原 CPU ONNX 教师完成完整 4 秒恢复。参考节奏只改变教师输入，物理仍每 2 ms 步进，既有关节/力矩限制不变。S790 另保存了内部定帧恢复姿态图，观察到后仰与横向调整，不把“不倒地”当作自然动作证书。

S804 对上述全部 128 个配置案例重建微步指标并核对实际初始状态；8 个节奏为 1 的对照完整复现原 q/qvel/接触轨迹，S799/S800 的初始状态和初始参考配置逐值一致。审计通过不改变这些试验没有技能合格例的结论。

## 从新状态继续学习

S801 从 S791 实际第 451 帧、参考中心 -0.1 rad、S786 模型开始，做 4×128 次候选搜索。最后开发候选速度约 **3.056 m/s**、误差约 **0.177 m**，身体安全且无非脚接触，仍不符合 0.1 m 要求。

四批搜索的随机候选、分数、搜索状态、普通数值模型导出及微步物理检查通过。

S803 继续利用这四批成功/失败样本形成的候选均值、方差和最佳偏置，另做 16 批暖启动搜索。新随机种子，旧最佳候选重新评估，不沿用旧分数。它是数据驱动的教师搜索，**不是额外 PPO 更新、在线部署或每个运行案例的人工动作路由**。尚须导出模型、原生新扰动考试、恢复复核及共享世界返回。

## 本地证据

所有目录位于 `/home/dell/rosclaw_soccer_evidence/`，具体文件哈希及限制见各自 `result.json` 和审计记录：

- `s786-reference-center-actual-gpu-ppo-v1`
- `s789-reference-center-teacher-search-v1`
- `s792-reference-teacher-native-exam-v1`、`s793-reference-teacher-parent-native-exam-v1`
- `s795-reference-teacher-native-pair-audit-v1`
- `s797-reference-center-trained-native-exam-v1`、`s798-trained-reference-native-pair-audit-v1`
- `s526-public-frame-receiver-runtime-v1/s791-ball-passed-lateral-approach-shared`
- `s802-delayed-arrival-shared-audit-v1`
- `s794-actual-arrival-phase-screen-v1`、`s796-actual-arrival-reference-center-screen-v1`
- `s799-actual-arrival-reference-tempo-screen-v1`、`s800-trained-actual-arrival-tempo-screen-v1`
- `s801-arrival-entry-teacher-feasibility-v1`、`s803-arrival-teacher-warm-search-v1`

本阶段代码仍全部 SIM_ONLY，无真实机器人、ROS/DDS、硬件许可或传输操作。
