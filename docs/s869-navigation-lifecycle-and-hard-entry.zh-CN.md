# S865–S869：困难来球与导航生命周期复查

状态：开发验证中，SIM_ONLY，不具备宣传片验收资格。

## 为什么不能把孤立射门进步直接搬到比赛里

S843 的局部参数标定在 S845 的 32 个扰动世界获得 7 次精度达标，但踢后后退的中位数仍约 0.73 m。在实际比赛 S850 的新到达姿态上，同一模型完全没有触球。S853 原生 MuJoCo/CPU ONNX 的 32 个复现实验也全部未触球。因此，局部射门成功率不能代表“跑动—触球—恢复”的完整能力。

S868 使用原始物理轨迹，在 0.16、0.20、0.24、0.30 s 做了固定视角对比。前 0.8 s 内按 20 ms 采样，旧入口最近脚球表面距离为 -0.00666 m，新入口为 +0.06526 m；新入口最近距离主要沿横向。这里的最近距离是采样诊断，不是连续时间最小值；有无触球由原来的 2 ms 接触记录判定。图片只供内部诊断，不是新仿真或宣传视频。

S858 扫描参考朝向仍然 32/32 不触球。S866 在同一实际失败状态扫描参考相位 -16 至 +15 帧，仍然 32/32 不触球，31/32 满足原安全检查。没有采用这些扫描参数，也没有降低 3 m/s、0.1 m 或接触验收门槛。

## 训练对照

S865 对冻结的 S843 模型执行双端点原生验证，各 16 个世界，其中每组一个精确初态、15 个新扰动：

| 原生端点 | 脚触球 | 速度达标 | 精度达标 | 原安全检查 | 非脚接触 |
|---|---:|---:|---:|---:|---:|
| 旧 S791 入口 | 16/16 | 6/16 | 6/16 | 16/16 | 0 |
| 实际失败 S850 入口 | 0/16 | 0/16 | 0/16 | 16/16 | 0 |

旧入口过平面误差均值为 0.04422 m。新入口没有过平面，不能把旧报表中的 99 哨兵值当成实测 99 m 误差。物理文件 SHA-256：`0e82bf8ad68fae9ce222d64717f504444f3e72b1c949435e00cdb00a70322b91`。

S863 已完成 256 世界、32 次 PPO 更新的桥接课程，一半保留旧入口，一半逐渐接近实际失败入口。中间插值状态仅是合成训练初态，不能宣称已经由机器人自然跑到；评估一直使用两个真实端点。GPU 物理和 GPU 教师仍标记为近似域。输出仍是关节目标残差与参考朝向，不是直接力矩策略。

近似域最终旧入口为 69/128 速度、59/128 精度达标，困难入口 0/128 触球。全部 32 次优化器更新精确重放误差为 0；65 个回合的微状态与信用分配审计通过，回报重建最大误差约 1.811e-6；32 个训练回合参考历史重建最大误差约 2.98e-8；65 个回合的初态/目标/恢复代价重建通过，代价误差为 0。这些只证明训练记录可复核，不是技能验收。

S870 用最终冻结模型重新执行 S865 的相同 32 个原生端点：旧入口 9/16 速度、6/16 精度达标；困难入口仍 0/16 触球；全部 32 个满足原安全检查，没有非脚接触。旧入口过平面误差均值变为 0.05988 m。S873 逐个配对发现，精度保留 4 个、丢失 2 个、新增 2 个，不能称零遗忘。旧入口平均恢复代价从 4.81236 降至 3.24329，但并未解决困难来球，因此不替换比赛模型。无触球端点的恢复代价为零只表示事件未发生，绝不是恢复技能优秀。

最终模型 SHA-256：`567f6f53262aa5dcdfb544185ffb411a4f1ba71f92a0f01a1c594055f277d289`。原生物理文件 SHA-256：`b99edcb758999d0a45ff73811f6002928732d3bbeb13acad45ac028f40fd20e9`。

## S867 暴露的工程问题

传球结束后，原先的 SONIC 导航实例已经退出。S867 尝试在后续跑动阶段创建新的私有导航实例，但它默认只接受全局第 0 帧；实际首次调用是第 285 帧，于是原有时序检查正确锁止，比赛走了回退路径。不能把此试验当作成功复用 SONIC。

源代码新增 `G1SonicNavigation.start_from_observation()`：

- 只允许未使用、未故障、未退休且没有旧历史的实例。
- 接受当前测量及全局时钟；初始化使用当前测量填充冷启动历史，不冒称此前真实观测。
- 生成 SIM_ONLY 收据，绑定主体、帧、时间、观测、参考和导航契约。
- 第一次 `propose` 必须使用同一个完整观测；之后仍要求连续帧。
- 错误初始化或边界观测变更会锁止，不能靠重复初始化恢复。
- 同样加强已有 `start_from_history` 的首帧观测绑定。

提交：`454e584`。64 项导航及历史交接针对性测试通过；全仓 ruff、涉及文件格式检查、导航模块定向 mypy 通过。修复后全量测试为 2324 passed、29 skipped、11 failed，失败名称与既有基线一致；不能称全量全绿。JUnit：`/home/dell/rosclaw_soccer_evidence/s873-repository-tests.xml`。

S869 已完成使用新公开初始化接口重跑 S867 的完整比赛试验。它没有修改已完成的 S867 脚本或证据，也没有重置身体、足球或放宽触球入口。第 285 帧成功冷启动新导航，第 440 帧真实暖历史交接到射门，第 640 帧完成原四秒动作后恢复导航；没有电机选项故障，八个身体满足原安全检查。

实际右脚首次触球为射门后 0.198 s，清洁归因窗口内向目标峰值 3.77761 m/s，球门方向峰值 3.72407 m/s，脚部接触引起的速度增量 0.94941 m/s。但是连续向前指标仍未达标，球也没有通过目标平面。

S871 对完整接触身份逐条复核：0.472 s 红方后卫身体挡球；1.714 s 反弹球又碰到蓝方射手身体。旧审计的 `own_nonfoot=true` 是正确的，不能只看到第一次是对手接触就误认为标签错误。对手封堵后的球路不能继续算射手独立射门精度，也不能把封堵宣称为进球。宣传与完整链路验收仍未通过。

S872 正在试验事件驱动的射门退场：必须先有真实自身脚接触，超过原 0.2 s 归因窗口且球身距离超过 0.35 m，再观察到其他球员接触，才在下一个控制边界结束已失效的射门并通过原有历史交接恢复导航。这是显式解析生命周期试验，不是新训练的恢复模型；必须验证干预前轨迹不变和干预后的实际稳定性，不能只看动作提前结束。

## 本地证据与下一步

- 原生端点对照：`/home/dell/rosclaw_soccer_evidence/s865-curriculum-parent-native-endpoints-v1`
- 相位反例：`/home/dell/rosclaw_soccer_evidence/s866-missed-entry-reference-timing-screen-v1`
- 几何对比：`/home/dell/rosclaw_soccer_evidence/s868-contact-gap-diagnostic-v1/contact-gap-sheet.png`
- 导航失败与修复后试验：`/home/dell/rosclaw_soccer_evidence/s526-public-frame-receiver-runtime-v1/s867-successor-sonic-goal-reference-shared` 与 `s869-successor-observation-start-shared`
- 训练课程：`/home/dell/rosclaw_soccer_evidence/s863-actual-entry-bridge-curriculum-ppo-v1`

后续必须分别确认：导航连续调用是否真实恢复、有没有暖历史交接、实际脚球接触与射门精度、原生困难端点是否进步、旧技能是否遗忘，以及完整踢后恢复。当前没有新的可交付宣传 MP4。
