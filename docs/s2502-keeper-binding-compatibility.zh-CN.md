# S2494–S2502：稀疏结果学习与旧门将模型的严格兼容

2026-09-16，SIM_ONLY。没有新的比赛冠军或合格连续比赛视频。

## 稀疏奖励没有凭空产生进步

S2494 从 S2478 的 512 步接球反馈头开始，固定 log standard deviation 为 −6，做一代完整进攻二值结果 PPO：8 个探索课程、初始及最终无噪声考试，共 20 次实际物理执行。只有接球通过、全队安全、后续真实脚触球及实际进球同时成立，训练奖励才为 1。

探索接球 3/8，全队安全 6/8，但完整进攻奖励全部为 0。组内相对 actor 优势因此全部为 0。虽然实际执行 12 次优化器更新，但只有 critic 权重改变，actor 权重逐项未变、KL 为 0；最终无噪声进球是原有能力保留，不是新的 actor 提升。独立重算参数、优化器、RNG 和 20 个物理文件全部一致。

不能仅因“PPO 跑完、最终进球”就宣称学会了新动作。下一轮需要更有效的正样本获取和覆盖，不能重复堆全零奖励批次。

## 发现真实的门将加载兼容问题

S2496 在新学会的进攻上复用既有门将。旧肌肉模型哈希正确，但上下文选择器加载被拒绝。排查发现：

- 提交 `71f8269` 将原硬编码的入场高度 0.65 m 变为配置项；
- 随后 `5703d33` 将原任务空间最低伸手高度 0.72 m 变为配置项；
- 默认行为未变，但 `asdict()` 多出两项，旧门控所绑定的配置摘要不再相等。

旧配置摘要为 `408a0d4526ba935a993594fc3f90098b2da2aaf47fca07bbcdf2d52db87f3a52`；包含当前两个默认项的字面摘要为 `59d094ad69eb08c9f3c22a04775ceca4f3c983ed6e6749653db78a989218e890`。不是模型文件丢失，也不是需要取消校验。

## 修复规则

`KeeperContextGate` 新增可选的完整显式配置。必须先证明它与调用方声明的当前配置摘要一致，再考虑两种历史序列化形态：

1. 仅当最低伸手高度严格为旧默认 0.72 m，才允许省略这个后来新增的字段；
2. 在上一步成立后，仅当入场高度严格为旧默认 0.65 m，才允许再省略更早新增的字段。

不改变 `gate_config_hash()` 的当前字面摘要，现有新格式门控仍精确匹配。未提供显式配置时维持原严格行为。新增 `config_binding` 区分 `EXACT` 与 `LEGACY_DEFAULT_HEIGHTS`，保留实际绑定的原摘要。

其他参数、父模型、SIM_ONLY、禁止自动晋升、文件大小和数值范围验证均不变。修改高度或增益不能继承旧门控。没有修改旧模型/门控文件，没有重新签发权限，也没有扩大手套或放宽身体限制。

## 实际物理对照

S2496 前三组完成双执行，第四组在门控加载时中断，保留部分结果；不能算四组完成。

修复后 S2500 重跑四组，共 8 次物理执行：

| 配置 | 前锋接球 | 全队安全 | 防守门将上肢激活帧 | 手套触球帧 | 实际蓝方进球 |
| --- | --- | --- | --- | --- | --- |
| 原站位 + teacher | 通过 | 通过 | 0 | 0 | 是 |
| 只改防守方门将站位 + teacher | 通过 | 失败 | 25 | 0 | 否 |
| 双方门将改站位 + teacher | 通过 | 通过 | 0 | 0 | 是 |
| 只改防守方站位 + 旧 learned 模型 | 失败 | 失败 | 0 | 0 | 否 |

前三组修复前后的**所有轨迹数组完全相同**，证实兼容修复没有改变不使用旧门控的这些实际控制过程。第四组已能严格加载并完成仿真，但物理效果失败，不将加载成功当成扑救成功。

只改防守方 teacher 时，接球前的球与前锋状态保持不变；双方改站位或复用带基础上肢观测中和的 learned 模型时，接球前缀已改变，不能把它们当作完全相同来球下的扑救率对照。任何“未进球”也不能在接球失败或身体失败时冒充成功扑救。

## 验证与证据

- 针对门控和 SharedKeeperReach：29 项通过，新增 11 项。
- 修改文件 Ruff / format、两个源文件 mypy 通过。
- 全量：**4188 passed、18 skipped、11 failed，84.50 s**。失败仍为相同的历史外部证据绑定测试，未放宽或跳过。
- S2502 对真实归档门控验证兼容加载，并实证拒绝 0.64 m 入场高度、0.71 m 伸手高度和改变的增益；旧文件字节未变。
- 核心 ROSClaw 工作树未修改。

证据根 `/home/dell/rosclaw_soccer_evidence/`：

- `s2494-binary-goal-successor-ppo-v1/training.json`，规范化摘要 `0cb9beac70785fa5515495cea2191dbab95961e5bb78654e4d2652a06fedd485`。
- `s2494-successor-ppo-audit-v1/audit.json`，摘要 `a429048dbb7606ba0e1bcab1a92f870812788aa2047fac21b7b364d31d941527`。
- `s2496-keeper-against-learned-attack-v1/progress.json`，保留三个完成项，第四项加载失败。
- `s2500-keeper-default-height-compatibility-v1/exam.json`，摘要 `1bd49aaddfdad0c65f664bf66eb676608ed12c7bf58cb7208128de9a21620a75`。
- `s2502-keeper-gate-compatibility-preflight-v1/preflight.json`，摘要 `f129b5bc1acd600e13d9329b2427258064871784530e0bd9366cad795371272b`。

下一步还须验证进球后继续运行的身体恢复与新的传接球机会。13.62 s 的进攻在进球时结束，不证明之后仍稳定，更不证明比赛能持续往复。
