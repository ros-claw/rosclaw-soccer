# S372：接稳之后，单调走位参数仍不能完成回传

证据目录：`/code/rosclaw/phase8_evidence/s372-post-capture-outlet-feasibility-v1`。

从 S371 retained 的第 510 控制帧（接球神经网络刚完成）提取身体、球、上一动作及完整 LSTM hidden/cell 状态；不是把身体重置为理想站姿。隔离世界开始时初始化一次，之后不修改球或身体状态。

筛查冻结 RoboNaldo 基础运动模型配合不同解析准备动作：4 种站位深度、2 种左右偏置、4 种转身增益、2 种绕球方式，共 **64** 个设置。所有设置均保持原 0.7 m/s 移速上限、0.8 rad/s 转向上限、85% 力矩与关节保护，运行 5 秒。没有神经残差、脚部接触教师或优化器。

结果：64/64 身体安全，31/64 脚部接触球，14/64 出现非脚部接触；**0/64** 满足向队友方向至少 0.8 m/s、横向速度不超过 0.3 m/s 的干净、安全脚部出球条件。单纯触球没有转化成有效传球。

该任务需要显式出球目标和出球动作学习，不能只依赖接球的“减速停球”奖励。后续 S373 新增 136 维目标条件化观测：原 133 维身体/球/基础控制信息，加目标方向 XY 与期望球速；仍输出 29 关节目标残差，尚不是直接力矩策略。

记录瑕疵也保留：首次运行在保存时因 `safe` 字段重名失败，无结果文件；修复字段命名后完整重跑，原 `run.log` 保留。成功运行的结构化字段和 `plans` 长度均为 64，但 `boundary` 文字残留“128”；本报告明确纠正为 64，不篡改已绑定的原文件。这 64 个设置共享一个初始身体状态，不是 64 个独立比赛场景。

SIM_ONLY；未晋升模型或修改现役比赛配置。方向性脚部出球本身也只是下一传的必要条件，不等于队友成功接球，更不等于整场比赛通过。
