# S217：控制频率与延迟团队奖励的对照

仅改变 PPO 的 GAE 时间尺度：S216 为 γ=0.99、λ=0.95；本组 γ=λ=0.997。
控制频率仍为 50 Hz。对于实测 88 帧的传接球延迟，回传系数由约 0.004525
变为约 0.5893。该数学变化已测试，不等价于足球能力提升。

初始网络、12 代预算、红蓝球位、随机种子、近球站位、残差边界、PPO 学习率和
更新次数保持相同。最终代必须与零残差及 S216 最终代逐球位比较，不能从中途
挑一条好看的探索轨迹宣布成功。

外部证据目录：`$ROSCLAW_SOCCER_EVIDENCE/s217-long-credit-private-ppo-v1`。
每代有不可变 checkpoint、两次完整物理执行和私有角色更新记录。
完整结果以最终 `training.json`、`learning-audit.json` 为准；不预报实验成功。

5 个新增测试验证 88 帧延迟回传、无关角色零信用和非法系数拒绝。
本组新增参数默认关闭，历史 γ/λ 默认值不变。
