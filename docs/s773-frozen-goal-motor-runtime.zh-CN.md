# S773/S774：目标条件化小脑的冻结运行接口

## 已完成内容

新增 `providers/g1/goal_reference_motor.py`，将已有 136 维观察、30 维输出的训练模型转换为可复用的**只读、冻结、逐球员独立**推理接口。

这不是一次自动部署，也不是直接关节力矩控制。实际输出仍为 29 个受限关节目标残差，以及下一步教师参考朝向。调用方继续负责动作资格、教师历史、坐标变换、关节/力矩限制、接触证据和候选晋升。

## 接口与状态责任

- 构造时验证数值 NPZ、张量形状、文件大小和完整内容哈希，不加载 pickle。
- 契约绑定模型、冻结基础策略、参考库、坐标变换、默认关节角和两组动作限制。
- `begin_episode(target_position_xy=...)` 固定本回合目标，复制输入；回合哈希同时绑定目标和模型契约。
- 每个实例独立持有上一次关节残差和参考朝向；调用方不得在球员间共享实例。
- `propose()` 仅接受连续的第 0–199 帧。重复帧、跳帧、非法数值均锁住实例，直到显式新回合复位。
- 第 n 步消耗使用第 n 步朝向生成的教师目标，返回第 n+1 步朝向。模块不会自行修改教师或仿真器。
- 推理不依赖 Torch，不包含优化器、仿真步进或硬件传输；所有输出硬标记 SIM_ONLY。

固定的残差幅度、平滑和每步上限与现有训练契约一致；参考朝向为 0 ± 0.1 rad，每步最多 0.02 rad。不能把其他动作范围训练出的权重直接视为本接口的合格模型。

## 验证

17 个单元测试通过，包括完整 200 帧与 Torch 网络及两个历史滤波器的数值对照、无需 Torch 的推理、实例隔离、不可变输出、目标复制与哈希绑定、帧错误锁定、非法目标、非有限输入、错误模型哈希及数值溢出。

与滚动模型、目标参考网络和入场记忆网络联合测试共 **76 passed**。新模块定向 mypy、全仓 Ruff 通过。

S774 使用与 S772 完全相同的 32 个初始状态，在原生 CPU MuJoCo 和原 CPU ONNX 教师上回放全部 4 秒。实际关节与参考朝向命令均由新的数值冻结接口给出；Torch 网络仅保留用于诊断记录，不控制身体、不做训练。

| 指标 | 结果 |
| --- | --- |
| 速度/精度/身体安全/非脚接触判定 | 与原实现逐例一致 |
| 最大虚拟落点误差差异 | 1.5103e-6 米 |
| 前 200 ms 最大 q 差异 | 1.9119e-7 |
| 前 200 ms 最大 qvel 差异 | 9.2842e-6 |
| 接口复核 | 通过原 5 mm 落点差异与早期状态阈值 |

**这里通过的是接口复核，不是足球技能验收。** 该批次仍只有 18/32 达到 0.1 米精度，31/32 满足速度，1 例非脚接触；仍属于对齐初始化课程，不是完整球队比赛。未将它自动替换到任何球员的在线动作中。

## 证据

目录：`/home/dell/rosclaw_soccer_evidence/s774-frozen-goal-motor-native-bridge-v1/`。

- 模型 SHA-256：`dbf8d42497f8bc0e5020bb4cafb91f5d8d8fa8d144bf2d99383595f719492a06`
- 接口源码 SHA-256：`04873f2979858dc9deeb48eb1ca2b6819a245975dcd65834d278c360e4ffb287`
- 物理轨迹 SHA-256：`80577389a3359e335d0390da19a480ac98d0d0efc251d36b8704a7a0f3c1694e`

后续需要在真实共享世界产生的入场姿态上训练与复核，再经过现有技能准入逻辑返回比赛。接口完成不等于这些工作已完成。
